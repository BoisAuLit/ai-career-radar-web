# Daily Summary · 2026-07-08

First 2026-07-08 entry. No new TASK / RUN_REPORT /
DECISION loops today; a single E2E smoke test was
run against the freshly-pushed Candidate 1 +
Candidate 4 changes to validate their real-browser
behavior before letting them soak in production
usage. No repo files were modified by the test.
Pipeline repo remained fully untouched.

## E2E smoke test · Candidate 1 + Candidate 4

- **Test target**: `http://localhost:3000` (local
  dev server, `npm run dev`)
- **Browser**: Playwright headless Chromium
  (Playwright is a repo devDependency; no new
  install)
- **Result**: **all scenarios PASS**

### Scenarios

1. **Manual pasted resume + target role generated
   a full report successfully.**
   - No incomplete banner rendered
     (`stage === "done" && reportIncomplete` was
     `false` → Candidate 1 sentinel arrived
     normally).
   - Report contained all 5 required sections
     (Target role · What you already have · Top 5
     gaps · Skills you might be over-prioritizing ·
     Highest-leverage next action) plus the
     Evidence appendix.
   - Action bar buttons present after generation:
     **📋 Copy report**, **⬇️ Download .md**,
     **📊 Eval this report**, **↺ Start over**.
2. **Text-selectable PDF upload worked.**
   - Extracted 965 chars (well above the
     `MIN_EXTRACTED_RESUME_CHARS = 300`
     threshold).
   - Résumé field populated with trimmed
     extracted text.
   - Filename chip `loaded resume_text.pdf`
     appeared next to the "📎 Upload PDF"
     button.
   - No amber warning banner shown.
3. **Scanned / image-only PDF upload worked as
   intended.**
   - Résumé was **not overwritten** — pre-upload
     résumé state (empty) remained empty.
   - Filename chip did **not** appear (no
     "successful upload" affordance for a bad
     extraction).
   - Amber warning banner appeared with the
     Candidate 4 copy:
     `⚠ This PDF produced very little readable
     text. It may be scanned or image-only.
     Please paste your resume text manually or
     upload a text-selectable PDF before
     generating a report.`
   - No red `pdfErr` panel (the scanned PDF is a
     UX gate, not a server error, so amber not
     red is the correct affordance).
4. **Reset flows passed.**
   - **Load sample** (the persona picker's
     `Load sample →` button, which calls
     `loadSample()` in `src/app/page.tsx`) after
     a scanned-PDF upload cleared the amber
     warning, populated the résumé to 820 chars
     and the target to 237 chars.
   - **Start over** (the action bar's
     `↺ Start over` button, which calls
     `handleStartOver()`) after a successful
     generation followed by a second scanned-PDF
     upload cleared the amber warning, the
     résumé state, the target state, and hid
     the report card entirely.

### Cost

- Two real LLM generations (scenario 1's happy-
  path generate + classify pair, plus scenario
  4b's setup generation used to reach `stage ===
  "done"` before testing `↺ Start over`).
- Approximate total Anthropic API cost:
  **≈ $0.10**.

### Boundaries held during the test

- No `src/**` files modified by the test.
- No `src/data/**` modified.
- No `src/app/api/**` modified.
- No prompts modified.
- No pipeline repo modified (HEAD `b019786` at
  start and end).
- No collector run.
- No corpus refresh.
- No new npm dependency added; Playwright was
  already a devDependency.
- No `.agent/scripts/**` change.
- No `.github/workflows/**` change.
- No push, no manual deploy.
- Test artifacts (script, generated test PDFs,
  screenshots) live in the scratchpad directory
  outside the repo.

### Conclusion

- **Candidate 1 happy path is invisible** and
  the truncation-protection banner is ready to
  soak in real production traffic. If a real
  Anthropic stream truncation ever occurs, the
  amber banner will appear on the homepage
  report card exactly as the DECISION
  described.
- **Candidate 4 happy path is invisible** and
  the scanned-PDF warning works end-to-end,
  correctly preserving prior résumé state and
  refusing to show a "successful upload"
  affordance in the below-threshold path.
- **Real-report entrance reliability is now
  materially stronger**: silent stream
  truncation and silent empty-PDF résumé
  overwrites are both closed against
  real-browser behavior, not just static
  analysis.
- **No repo state was modified by the E2E
  test.**
- **Pipeline repo remained untouched.**

## Human decisions needed

- **Next step**: choose whether to keep letting
  Candidate 1 + Candidate 4 soak (default), or
  explicitly start **Candidate 2** (quote-
  integrity substring check on the eval path)
  as the next separate TASK + RUN_REPORT +
  DECISION loop. Not recommended in this
  cleanup turn: Candidate 5 sample-vs-real
  visual mismatch, any OCR addition, any
  backend PDF extraction change, any prompt
  change, any pipeline edit, any bundle swap,
  any AgentOps-2* work, any manual `vercel
  deploy`, any G2.1d work.
