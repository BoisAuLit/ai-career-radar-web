# Daily Summary · 2026-06-30

Fourth day under the AgentOps file protocol. **Two** real loops
shipped end-to-end to `origin/main` of the web repo:

- **AgentOps-2b · Automation runner design memo** — the parent
  memo scoping the *hypothetical future* Codex CLI + Claude
  Code automation runner. Four implementation options
  (A / B / C / D); Q1 = Option B chosen as future direction
  (NOT implementation approval); Q7 = Codex CLI via ChatGPT
  sign-in does NOT trigger BLK-0003 (API-key / SDK / HTTP /
  automation-token OpenAI usage remains blocked).
- **AgentOps-2c · Supervised runner dry-run design memo** —
  narrows Option B to a specific dry-run shape. Four
  candidate implementation *shapes for Option B*
  (A / B / C / D within Option B); Q1 = Shape B chosen as
  future direction (still NOT implementation approval);
  Q3-Q8 = first Shape B prototype envelope is **extremely
  narrow** (report-only, non-executing, no auto Codex /
  Claude / commit / push / product touch / `.agent/scripts`
  mod); Q10 = pause automation-infra expansion after
  AgentOps-2c and return to product work (P1.7b / P1.7c
  preferred).

The day deliberately produced **no runner**, **no daemon**, **no
scheduler**, **no cron**, **no GitHub Actions change**, **no
Codex CLI / Claude Code config change**, **no app code change**,
**no pipeline change**, **no OpenAI API introduction**, **no
deploy**, **no blocker resolution**, **no `.agent/scripts/**`
mod**. The three open blockers (BLK-0001 G2.1d / BLK-0002 full
automation activation / BLK-0003 OpenAI API standing, Q7-scoped)
remain `open`.

## Metadata

- **date**: `2026-06-30`

## Tasks attempted

| task_id | title | risk |
|---|---|---|
| `2026-06-29_run_03` | AgentOps-2b automation runner design memo | yellow |
| `2026-06-30_run_01` | AgentOps-2c supervised runner dry-run design memo | yellow |

> Note: `2026-06-29_run_03` retains the `2026-06-29` prefix
> because it was authored under that logical date; the
> day-boundary crossed during authoring. `2026-06-30_run_01`
> was authored fully on 2026-06-30 and uses the current date.

## Tasks completed

| task_id | verdict |
|---|---|
| `2026-06-29_run_03` | `approve` → web push of memo impl (TASK + memo + queue add) + RUN_REPORT + DECISION succeeded; 4 deliverable files live on `origin/main` at `baf2781`; QUEUE-0007 now `done`; new QUEUE-0008 AgentOps-2c added as `blocked_pending_human` |
| `2026-06-30_run_01` | `approve` → web push of memo impl (TASK + memo + QUEUE-0008 `blocked_pending_human` → `in_review`) + RUN_REPORT + DECISION succeeded; 4 deliverable files live on `origin/main` at `c5ac0c8`; QUEUE-0008 now `done`; **no** runner implementation queue item added |

## Commits created

### Web repo (`/Users/bohaoli/Desktop/ai-career-radar-web`)

**AgentOps-2b design memo (`2026-06-29_run_03`):**
- `00a98b2` Add AgentOps-2b automation runner design memo —
  TASK + memo (`.agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md`,
  821 lines, 19 H2 sections) + queue add (QUEUE-0007 `in_review`)
  in a single commit. Scaffolded via
  `python .agent/scripts/new_task.py --date 2026-06-29 --run 3 --title "..." --risk yellow`.
- `70cc6d9` Add RUN_REPORT 2026-06-29_run_03 — scaffolded via
  `python .agent/scripts/new_run_report.py --task-id 2026-06-29_run_03`.
  `ready_for_review: yes`, `requires_human_decision: yes`, 6
  risks, 7 follow-ups, 16/16 acceptance criteria verified.
- `baf2781` Add DECISION 2026-06-29_run_03 — scaffolded via
  `python .agent/scripts/new_decision.py --task-id 2026-06-29_run_03`.
  Verdict `approve`, `human_approval_needed: yes`, 10 risks, 0
  required fixes, 8 non-blocking follow-ups. Records **Q1 =
  Option B** and **Q7 = Codex CLI via ChatGPT sign-in OK**
  as formal decisions. **Eighth full dogfood loop of the
  helper triple.**

**AgentOps-2c design memo (`2026-06-30_run_01`):**
- `e24042f` Add AgentOps-2c supervised dry-run design memo —
  TASK + memo
  (`.agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md`,
  805 lines, 18 H2 sections) + queue update (QUEUE-0008
  `blocked_pending_human` → `in_review`) in a single commit.
  Scaffolded via
  `python .agent/scripts/new_task.py --date 2026-06-30 --title "..." --risk yellow`.
- `45345ce` Add RUN_REPORT 2026-06-30_run_01 — scaffolded via
  `python .agent/scripts/new_run_report.py --task-id 2026-06-30_run_01`.
  `ready_for_review: yes`, `requires_human_decision: yes`, 6
  risks, 5 follow-ups, **29/29 acceptance criteria verified**
  (including `.agent/scripts/**` empty-diff hard rule).
- `c5ac0c8` Add DECISION 2026-06-30_run_01 — scaffolded via
  `python .agent/scripts/new_decision.py --task-id 2026-06-30_run_01`.
  Verdict `approve`, `human_approval_needed: yes`, 12 risks, 0
  required fixes, 9 non-blocking follow-ups. Records **Q1 =
  Shape B** (future direction, NOT implementation approval),
  **Q3-Q8 = extremely narrow first-prototype envelope**, and
  **Q10 = return to product work** as formal decisions.
  **Ninth full dogfood loop of the helper triple.**

**Daily summary + queue cleanup (this commit):**
- `f985752` Update daily summary for AgentOps-2b — creates
  `2026-06-30_SUMMARY.md`, marks QUEUE-0007 `done`, adds
  QUEUE-0008 AgentOps-2c as `blocked_pending_human` (already
  landed 2026-06-30 earlier).
- *(forthcoming)* Update daily summary for AgentOps-2c —
  extends `2026-06-30_SUMMARY.md` (this file), marks
  QUEUE-0008 `done`, does **not** add any runner
  implementation queue item.

All web-repo commits pushed to `origin/main` after human
approval. No branch was cut (yellow `.agent/`-only doc work
goes directly to `main`, same pattern as AgentOps-2a /
MANUAL_DRY_RUN / Executive Digest / AgentOps-2b).

### Pipeline repo (`/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar`)

`none` — both AgentOps-2b and AgentOps-2c memos are web-only.
Pipeline repo HEAD = `origin/main` = `b019786` at start and
end of the day. Multiple read-only sanity checks ran
(`git status` + `git rev-parse HEAD`); zero pipeline-side
edits.

## Build status

`not-run` — yellow `.agent/`-only documentation change (both
loops). The TASKs explicitly waived `npm run build`. No app
code, no Python module, nothing that compiles or executes.

## AgentOps-2b · Automation runner design memo

First loop of this day. Pure web `.agent/` design memo +
policy-adjacent queue update. **No runner / daemon / scheduler /
cron / GitHub Actions / Codex/Claude config / OpenAI API
introduced.** Implements DECISION follow-up from the Executive
Digest run (`6c51db6`) which listed AgentOps-2b design memo as
one of the two recommended next steps.

**Host web repo files (all on `origin/main` at `baf2781`):**

- TASK: `.agent/tasks/2026-06-29_run_03_TASK.md` (311 lines, in
  `00a98b2`)
- Design memo:
  `.agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md`
  (821 lines, 19 H2 sections, in `00a98b2`)
- Queue add: `.agent/automation_queue.md` (QUEUE-0007
  `in_review`, `+40/-0`, in `00a98b2`)
- RUN_REPORT: `.agent/run_reports/2026-06-29_run_03_RUN_REPORT.md`
  (465 lines, in `70cc6d9`)
- DECISION: `.agent/decisions/2026-06-29_run_03_DECISION.md`
  (439 lines, in `baf2781`)

**Commits referenced:**
- `00a98b2` — implementation (TASK + memo + queue add)
- `70cc6d9` — RUN_REPORT 2026-06-29_run_03
- `baf2781` — DECISION 2026-06-29_run_03

**DECISION verdict**: `approve`

**`human_approval_needed: yes`** remains required for:

- AgentOps-2c design memo authoring (separate TASK + DECISION)
- Any runner implementation (Option B prototype OR Option C
  orchestrator, in any form)
- Any real Automation Window opening
  (`SLEEP_WINDOW` / `WORKDAY_WINDOW` / `WEEKEND_WINDOW`)
- G2.1d (red, BLK-0001)
- OpenAI API usage in any blocked sense per Q7 (SDK / key /
  HTTP / automation-token / CI OpenAI secret / imports)
- Resolving any of the 3 open blockers (BLK-0001 / BLK-0002 /
  BLK-0003)

### Key decisions recorded (Q1 + Q7)

**Q1 — Chosen path: Option B (Local supervised runner, dry-run
only).**

- This is the next **automation-infra direction**.
- Option B means the next automation-infra work may **design
  or scope a non-executing supervised dry-run**.
- Option B does **NOT** approve implementation.
- Option B does **NOT** allow automatic code execution, push,
  deploy, OpenAI API, or automatic Claude Code runs yet.
- The next concrete step following Q1 is an **AgentOps-2c
  design memo** that scopes Option B's *internal* loop before
  any code is written.
- Options A / C remain on the table for a later iteration.
- Option D (GitHub Actions / cron) remains **explicitly not
  recommended**.

**Q7 — Codex CLI vs BLK-0003 OpenAI API standing block.**

- Codex CLI authenticated via **ChatGPT sign-in** does **NOT**
  trigger BLK-0003.
- BLK-0003 still blocks OpenAI API usage in the following
  senses (all remain forbidden until BLK-0003 is explicitly
  resolved by separate human decision):
  - `OPENAI_API_KEY` anywhere in this project's environment /
    CI / shell
  - `openai` Python / JS SDK installed in `package.json` /
    `package-lock.json` / `requirements*.txt` or imported in
    any project code
  - HTTP calls to `api.openai.com` / Responses API from any
    project code or script
  - `OPENAI_API_KEY` in any `.env*` under the web or
    pipeline repo
  - GitHub Actions / CI calling OpenAI API (workflows
    injecting a key as a secret)
  - Repo code that imports an OpenAI API client
  - Background automation using API tokens (long-running
    non-interactive processes authenticating to OpenAI
    infrastructure)
- **BLK-0003 remains `open`** — Q7 is a scope clarification,
  not a resolution.

### Validation summary (per RUN_REPORT + DECISION audit)

- Scope clean: `git diff --name-only origin/main..HEAD` showed
  exactly the 5 expected paths (TASK + memo + queue + RUN_REPORT
  + DECISION) before push.
- Memo is design-only: `Status: draft_for_human_chatgpt_review`,
  `Scope: design only`, `Non-goal: no runner implementation` —
  verbatim in the memo's Title / Status section.
- 19 H2 sections in the memo (≥16 required; 16 spec sections +
  3 useful extras: Blocker handling, Risks summary,
  Acknowledgements / Sanity checklist).
- `.agent/`-only: no `src/**`, no app code, no pipeline files.
- No runner / daemon / scheduler / cron / GitHub Actions edit /
  Codex CLI config edit / Claude Code config edit / OpenAI SDK
  install anywhere.
- No new npm / Python dependency. No `python -m
  scripts.collector …`. No `npm run …`. No LLM call by any
  task in this loop.
- No manual `vercel deploy`. Vercel auto-trigger from
  `.agent/`-only web push is non-disruptive (served bundle
  byte-identical — only `.agent/` changed).
- No blocker resolution: BLK-0001 / BLK-0002 / BLK-0003 all
  remain `open`; verified by re-reading `blockers.md` after
  the queue edits and confirming zero diff in that file.
- QUEUE-0002 G2.1d remains `blocked_pending_human` red.
- QUEUE-0001 / QUEUE-0006 unchanged (both still `done` prior).
- 16/16 TASK acceptance criteria verified pre-DECISION.
- Pipeline repo untouched: HEAD = `origin/main` = `b019786` at
  every check across today's loop.

### Risks / follow-ups carried forward

1. **The memo is long (821 lines) and conceptual — does not
   prove real automation safety.** MANUAL_DRY_RUN proved the
   *reporting* contract; this memo scopes the *execution*
   contract; neither has been exercised by actual end-to-end
   automation.
2. **Option B is only a *design direction*, not implementation
   approval.** A future reader could mistake "Q1 = Option B" for
   "build Option B now"; the DECISION and this daily summary
   make the gate explicit.
3. **AgentOps-2c must be separately approved** and its first
   iteration must remain **non-executing**. If it ever
   transitions to implementation, that transition is a separate
   TASK + DECISION.
4. **Auto-push should remain disabled** for the first real
   automation experiments. Even Option B's dry-run should be
   commit-only; Bohao pushes during B-time.
5. **Claude Code quota cap must be defined** before real
   A-time automation. Any AgentOps-2c or later implementation
   TASK must specify a per-window budget (wall-clock and/or
   commit count until the token meter lands).
6. **Pipeline automation may need a separate memo** before any
   real automation touches pipeline files. Pipeline `corpus/` is
   BLK-0001-adjacent territory.
7. **G2.1d remains blocked by BLK-0001.** G2.1c eval set being
   live does NOT lift it.
8. **Full automation remains blocked by BLK-0002.** Opening
   any real Automation Window requires the runner to exist with
   its own DECISION AND explicit BLK-0002 resolution.
9. **OpenAI API remains blocked by BLK-0003 (standing).** Q7
   clarifies scope; Codex CLI via ChatGPT sign-in is permitted,
   but API-key / SDK / HTTP / automation-token OpenAI usage
   remains forbidden.

### Confirmed NOT done during this loop

- ❌ No automation runner / daemon / scheduler / cron created
- ❌ No OpenAI API SDK / key / `.env` entry / HTTP call
- ❌ No app code changed
- ❌ No pipeline-repo file changed
- ❌ No `.github/workflows/**` change
- ❌ No Codex CLI / Claude Code config edit
- ❌ No new npm / Python dependency
- ❌ No `python -m scripts.collector …` invocation
- ❌ No `npm run …` invocation
- ❌ No LLM call by this task
- ❌ No manual deploy
- ❌ No force push / history rewrite
- ❌ MANUAL_DRY_RUN report not retroactively edited
- ❌ AgentOps-2c not started (design memo authoring is a
  separate future TASK)
- ❌ Any runner implementation not started
- ❌ MANUAL_DRY_RUN-2 not started
- ❌ G2.1d not started
- ❌ Full automation not activated
- ❌ No blocker resolution

## AgentOps-2c · Supervised runner dry-run design memo

Second loop of this day. Pure web `.agent/` design memo +
queue status transition. **No runner / daemon / scheduler /
cron / GitHub Actions / Codex/Claude config / OpenAI API /
`.agent/scripts/**` change introduced.** Implements the
QUEUE-0008 slot added in AgentOps-2b's cleanup; Bohao
explicitly approved starting this TASK in the turn's
instruction. Narrows AgentOps-2b's Option B (Local supervised
runner, dry-run only) to a specific dry-run shape so
Human + ChatGPT can evaluate whether — and how — to
eventually implement a report-only prototype in a separate
downstream loop.

**Host web repo files (all on `origin/main` at `c5ac0c8`):**

- TASK: `.agent/tasks/2026-06-30_run_01_TASK.md` (342 lines,
  in `e24042f`)
- Design memo:
  `.agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md`
  (805 lines, 18 H2 sections, in `e24042f`)
- Queue transition: `.agent/automation_queue.md`
  (QUEUE-0008 `blocked_pending_human` → `in_review`,
  `+14/-9`, in `e24042f`)
- RUN_REPORT: `.agent/run_reports/2026-06-30_run_01_RUN_REPORT.md`
  (518 lines, in `45345ce`)
- DECISION: `.agent/decisions/2026-06-30_run_01_DECISION.md`
  (536 lines, in `c5ac0c8`)

**Commits referenced:**
- `e24042f` — implementation (TASK + memo + queue transition)
- `45345ce` — RUN_REPORT 2026-06-30_run_01
- `c5ac0c8` — DECISION 2026-06-30_run_01

**DECISION verdict**: `approve`

### Key decisions recorded (Q1 + Q3-Q8 + Q10)

**Q1 — Future direction = Shape B (Non-executing local
planner dry-run).**

- Shape B (Python stdlib, ~200-400 lines, reads state,
  writes 1 report, no LLM call, no subprocess) is the
  preferred future implementation shape if
  automation-infra work continues.
- **This DECISION does NOT approve implementation.**
- Any future Shape B implementation requires a separate
  TASK + RUN_REPORT + DECISION + human approval.
- Shape A (pure manual) remains a valid fallback.
- Shape C (Codex-authenticated interactive) and Shape D
  (Claude Code only simulation) are **not** chosen for
  the future direction; Shape D remains available as an
  ad-hoc MANUAL_DRY_RUN-2 side task if desired.

**Q3-Q8 — First Shape B prototype allowed autonomy is
extremely narrow.**

Allowed:
- Read `.agent/policies/**`, `.agent/automation_queue.md`,
  `.agent/blockers.md`, latest daily summaries, latest
  TASK / RUN_REPORT / DECISION metadata (recent 3-5).
- Read `git status` / `git log` short summaries only.
- Write **one** automation window report under
  `.agent/automation_runs/YYYY-MM-DD_<WINDOW>_REPORT.md`
  **and only when executing under an explicit approved TASK**.

Not allowed:
- Any automatic Codex CLI invocation (Codex CLI via
  ChatGPT sign-in remains human-interactive only per Q7
  from AgentOps-2b DECISION).
- Any automatic Claude Code invocation (no subprocess,
  no programmatic API).
- Any automatic commit / push / deploy.
- Any modification to `.agent/scripts/**` (the helper
  triple stays frozen for this DECISION's authorization;
  a Shape B prototype adding a new helper file is a
  separate future implementation TASK's concern).
- Any modification to queue status by the prototype
  (queue transitions remain Human-during-B-time).
- Any automatic TASK draft creation.
- Any product touch (`src/**` / `package*.json` /
  `.env*` / `vercel.json` / `.vercel/**` /
  `.github/workflows/**`).
- Any pipeline repo touch (read-only sanity only).
- Any OpenAI API call in the Q7 blocked sense (SDK, key,
  HTTP, import, CI secret, background token).
- Any Codex CLI / Claude Code / Vercel config edit; any
  cron / systemd / launchd / hook.

**Q10 — After AgentOps-2c promotion, return to product
work.**

- Pause automation-infra expansion.
- Preferred next product tasks:
  - **P1.7b · sync hero mock numbers with
    `web_bundle.json`** (yellow, web UI-only).
  - **P1.7c · model-string single source of truth**
    (yellow, small `MODELS_DISPLAY` const in a
    non-prompt module).
- Reasoning: **5 full automation-infra loops in 3 days**
  (MANUAL_DRY_RUN + Executive Digest + AgentOps-2a policy
  update + AgentOps-2b memo + AgentOps-2c memo). Product
  surface hasn't changed since P1.7 (`2026-06-27_run_01`).
  Product loops are shorter and lower-risk right now.
- **Not chosen**: Shape B implementation immediately,
  MANUAL_DRY_RUN-2 immediately (optional but not
  default), AgentOps-2d or any further scoping memo.

### Validation summary (per RUN_REPORT + DECISION audit)

- Scope clean: `git diff --name-only origin/main..HEAD`
  showed exactly the 5 expected paths (TASK + memo +
  queue + RUN_REPORT + DECISION) before push.
- Memo is design-only: `Status:
  draft_for_human_chatgpt_review`, `Scope: design only`,
  `Non-goal: no runner implementation` — verbatim in
  the memo's Title / Status section.
- 18 H2 sections in the memo (≥17 required; 17 spec
  sections + 1 useful extra Acknowledgements / Sanity
  checklist).
- Memo scopes **only Option B**; does not re-litigate
  Options A / C / D from AgentOps-2b.
- Memo compares 4 candidate implementation *shapes for
  Option B* (A pure manual / B non-executing local
  planner / C Codex-authenticated interactive / D
  Claude Code only simulation) with Shape B
  recommended as safest useful first experiment.
- `.agent/`-only: no `src/**`, no app code, no pipeline
  files, no scripts (empty diff verified across full
  3-commit loop via `git diff --stat` on both
  `.agent/scripts/` and `.agent/blockers.md`).
- No runner / daemon / scheduler / cron / GitHub
  Actions edit / Codex CLI config edit / Claude Code
  config edit / OpenAI SDK install anywhere.
- No new npm / Python dependency. No `python -m
  scripts.collector …`. No `npm run …`. No LLM call
  by this task.
- No manual `vercel deploy`. Vercel auto-trigger from
  `.agent/`-only web push is non-disruptive.
- No blocker resolution: BLK-0001 / BLK-0002 /
  BLK-0003 all remain `open`.
- QUEUE-0002 G2.1d remains `blocked_pending_human` red.
- QUEUE-0001 / QUEUE-0006 / QUEUE-0007 unchanged
  (all still `done`).
- **29/29 TASK acceptance criteria** verified
  pre-DECISION (per RUN_REPORT).
- Pipeline repo untouched: HEAD = `origin/main` =
  `b019786` at every check across today's loop.

### Risks / follow-ups carried forward

1. **The memo is still conceptual (805 lines) and does
   not prove real automation safety.** No runner has
   actually run. Mitigation: this DECISION explicitly
   forbids Shape B implementation; the next real proof
   requires a separate implementation TASK + DECISION.
2. **Shape B requires separate approval before
   implementation.** A future reader could mistake
   "Shape B recommended" for "build Shape B now"; this
   summary and the DECISION both make the gate
   explicit.
3. **Automation-infra has had enough loops for now** —
   5 full loops in 3 days. Q10 explicitly pauses.
4. **Product work should resume.** Preferred next
   task: **P1.7b hero mock numbers**. Alternative:
   **P1.7c model-string SSOT**.
5. **Do NOT start Shape B implementation** without a
   separate TASK + DECISION.
6. **Do NOT start MANUAL_DRY_RUN-2** unless explicitly
   approved (green side task, not default).
7. **Do NOT start G2.1d.** BLK-0001 still `open`.
8. **Do NOT activate full automation.** BLK-0002 still
   `open`.
9. **Do NOT introduce OpenAI API** in any Q7 blocked
   sense. BLK-0003 still `open`.
10. **Do NOT modify `.agent/scripts/**`** until a
    separate implementation task is approved.

### Confirmed NOT done during this loop

- ❌ No automation runner / daemon / scheduler / cron
  created
- ❌ **No `.agent/scripts/**` edit** (hard rule verified
  across full 3-commit loop; empty diff)
- ❌ No OpenAI API SDK / key / `.env` entry / HTTP call
  / import / CI secret / background token
- ❌ No app code changed
- ❌ No pipeline-repo file changed
- ❌ No `.github/workflows/**` change
- ❌ No Codex CLI / Claude Code config edit
- ❌ No new npm / Python dependency
- ❌ No `python -m scripts.collector …` invocation
- ❌ No `npm run …` invocation
- ❌ No LLM call by this task
- ❌ No manual deploy
- ❌ No force push / history rewrite
- ❌ MANUAL_DRY_RUN report not retroactively edited
- ❌ Shape B implementation not started
- ❌ No runner implementation queue item added (no
  QUEUE-0009 for "Shape B implementation")
- ❌ No AgentOps-2d or further scoping memo started
- ❌ MANUAL_DRY_RUN-2 not started
- ❌ G2.1d not started
- ❌ Full automation not activated
- ❌ No blocker resolution

## Forbidden / red-zone audit (across today's work)

### Web repo
- `src/**` / `src/lib/prompts.ts` / `src/lib/anthropic.ts` /
  `src/data/web_bundle.json` — all untouched
- `package.json` / `package-lock.json` — untouched
- `.env*` — untouched
- `vercel.json` / `.vercel/**` — untouched
- `.github/workflows/**` — untouched
- `.agent/policies/**` / `.agent/templates/**` /
  `.agent/README.md` — untouched (memo *cites* policy §6 / §11 /
  §12 / §13 + the Executive Digest template but does NOT amend
  either)
- `.agent/scripts/**` — untouched (helpers only invoked)
- `.agent/blockers.md` — untouched (no new blocker; no lift)
- `.agent/automation_runs/**` — untouched (MANUAL_DRY_RUN
  report unchanged)
- **Any executable runner / shell script / config / cron /
  hook file** — none created anywhere
- No new npm dependencies

### Pipeline repo
- All files untouched. Read-only sanity throughout. HEAD =
  `origin/main` = `b019786`.

### Both repos (process-level)
- No OpenAI API integration files (per Q7 blocked scope: no
  SDK, no key, no HTTP call, no `.env` entry, no CI secret,
  no import, no background automation via API tokens)
- No GitHub `@claude` integration / new workflows
- No daemon / watcher / scheduler / dashboard / TUI / web UI /
  autonomous loop
- No automation runner / daemon / scheduler / cron file created
- No Codex CLI config / Claude Code config mutation
- No force-push, no history rewrite, no destructive reset
- No manual deploy

## Open risks (carrying forward)

1. **Memos are contracts, not stress tests.** Both
   MANUAL_DRY_RUN (proved reporting), AgentOps-2b (scoped
   runner conceptually), and AgentOps-2c (narrowed Option B
   to a specific dry-run shape) are documents; no runner has
   actually run. Mitigation: per Q10, automation-infra
   paused; any future proof requires a separate Shape B
   implementation TASK + DECISION + human approval.
2. **Shape B chosen as future direction, not endorsement of
   implementation.** Every downstream TASK must re-read the
   Q1 and Q3-Q8 notes and refuse to conflate direction with
   permission. Q3-Q8's narrow envelope is binding.
3. **All 3 blockers remain open.** BLK-0001 (G2.1d) /
   BLK-0002 (full automation activation) /
   BLK-0003 (OpenAI API standing, Q7-scoped). None lifted
   by today's work; none should be lifted as a side effect
   of any future approval that doesn't explicitly name the
   blocker.
4. **Helper-script invariant holds.** `new_task.py` /
   `new_run_report.py` / `new_decision.py` are pure
   file-template helpers; today's runs were the **eighth
   and ninth** full dogfood loops with zero modifications.
   `.agent/scripts/**` empty diff across both loops
   verified (hard rule per Q3-Q8).
5. **Policy version stable at `1.1`.** No policy edit
   today. §7 addition from 2026-06-29 (Executive Digest)
   is the current baseline; neither AgentOps-2b nor
   AgentOps-2c proposes a new policy change.
6. **Codex CLI authentication clarity established (Q7 from
   AgentOps-2b DECISION, reinforced in AgentOps-2c).** Any
   future TASK proposing OpenAI API usage MUST distinguish
   between ChatGPT-sign-in-authenticated Codex CLI
   (allowed, human-interactive only) and API-key / SDK /
   HTTP / automation-token usage (blocked, standing).
7. **Shape B implementation surface not yet approved.**
   Even after Q1 = Shape B, adding ~200-400 lines of Python
   under `.agent/scripts/` is a separate future TASK's
   concern; today's DECISION explicitly does NOT authorize
   any `.agent/scripts/**` mod.
8. **Automation-infra pause is deliberate (Q10).** 5 full
   loops in 3 days; product surface untouched since P1.7.
   The pause is not a failure — it's the correct
   sequencing.

## Recommended next-day tasks

> Per **Q10 from AgentOps-2c DECISION**, automation-infra
> expansion is paused. The next recommended direction is
> **product work**, specifically P1.7b or P1.7c.

1. **P1.7b · sync hero mock numbers with `web_bundle.json`**
   — risk `yellow`. **Default recommended next task.**
   Read live values at build/render time instead of
   hardcoding `92` and `5`. Depends on: P1.7
   (`2026-06-27_run_01`). Small web UI-only change; no
   `src/lib/anthropic.ts` / no prompt / no pipeline touch.
2. **P1.7c · model-string single source of truth** — risk
   `yellow`. Alternative to #1. Export a
   `MODELS_DISPLAY` const from a non-prompt module (e.g.
   new `src/lib/models-display.ts`) so the homepage
   caption stays in sync with `src/lib/anthropic.ts` model
   strings. Must NOT touch `src/lib/anthropic.ts`'s
   model-selection logic.
3. *(Optional automation-adjacent side task)*
   **MANUAL_DRY_RUN-2** — risk `green` /
   `.agent/automation_runs/` only. A second hand-authored
   window report using today's AgentOps-2c loop as the
   exemplar, validating the Executive Digest field shape
   end-to-end. Not the default; available if Bohao wants
   to exercise the digest without stepping away from
   automation-adjacent work entirely.
4. *(Deferred, requires separate approval later)*
   **Shape B prototype implementation TASK** — risk
   `yellow`. ~200-400 lines Python stdlib under
   `.agent/scripts/`. Would need to honor the Q3-Q8
   narrow envelope (report-only, non-executing, no auto
   Codex/Claude/commit/push/product touch). **Explicitly
   NOT the next task** per Q10.
5. *(Path A later step, NOT urgent)* **G2.1d · classifier
   prompt rewrite + scoring dry-run** — risk `red`,
   **blocked_pending_human** (QUEUE-0002 + BLK-0001).
   Stays blocked.
6. *(Possibly later, NOT urgent)* **`check_loop.py` audit
   helper** (QUEUE-0005, `deferred`). Skip until missing
   files actually show up.

## Human decisions needed

- Pick the next task. **Default per Q10 = P1.7b hero mock
  numbers** (product work). Alternatives: P1.7c
  (product), MANUAL_DRY_RUN-2 (green
  automation-adjacent), or something else Bohao chooses.
  Explicitly NOT recommended: Shape B implementation
  (requires separate TASK + DECISION),
  MANUAL_DRY_RUN-2 as default, AgentOps-2d or any
  further scoping memo.
- (No red-zone approvals pending right now. G2.1d will need
  one when we reach it; full automation activation will need
  one before any real Automation Window opens; OpenAI API
  usage in any blocked sense per Q7 will need one before it
  can proceed. All three are tracked in `.agent/blockers.md`.)
