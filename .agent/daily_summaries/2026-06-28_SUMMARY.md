# Daily Summary · 2026-06-28

Second day under the AgentOps file protocol. **Four** real tasks ran
end-to-end: **AgentOps-1c** added the parallel helpers for RUN_REPORT
and DECISION files (helper triple now complete and dogfooded), **G2.1b**
— the first cross-repo AgentOps task — shipped a frozen taxonomy spec
into the pipeline repo, **G2.1c** — the second cross-repo task —
shipped the v1 hand-labeled taxonomy eval set (37 entries) into the
pipeline repo as ground truth for the future G2.1d classifier dry-run,
and **AgentOps-2a** — the Codex CLI + Claude Code automation policy
(policy / documentation only, no runner) — landed all 5 files on
`origin/main`.

## Metadata

- **date**: `2026-06-28`

## Tasks attempted

| task_id | title | risk |
|---|---|---|
| `2026-06-28_run_01` | AgentOps-1c · run_report and decision helper scripts | yellow |
| `2026-06-28_run_02` | G2.1b · taxonomy spec doc (first cross-repo task) | yellow |
| `2026-06-28_run_03` | G2.1c · hand-labeled taxonomy eval set | yellow |
| `2026-06-28_run_04` | AgentOps-2a · Codex + Claude Code automation policy | yellow |

## Tasks completed

| task_id | verdict |
|---|---|
| `2026-06-28_run_01` | `approve` → ff-merged to main → pushed → post-merge smoke check passed (8/8) |
| `2026-06-28_run_02` | `approve` → web push + pipeline ff-merge + pipeline push all succeeded |
| `2026-06-28_run_03` | `approve` → web push + pipeline ff-merge + pipeline push all succeeded; eval set live in pipeline `main` at `b019786` |
| `2026-06-28_run_04` | `approve` → web push of impl + RUN_REPORT + DECISION succeeded; 5 policy files live on `origin/main` at `f1bab0f` |

## Commits created

### Web repo (`/Users/bohaoli/Desktop/ai-career-radar-web`)

**AgentOps-1c (`2026-06-28_run_01`):**
- `8f50ff4` Add TASK 2026-06-28_run_01 — TASK file scaffolded by
  `python .agent/scripts/new_task.py --date 2026-06-28 --title "..." --risk yellow`
  (first task to use the AgentOps-1b helper).
- `2b5c1dd` Add new_run_report.py + new_decision.py helpers (AgentOps-1c)
  · branch `agent/2026-06-28_run_01`.
- `20b25d1` Add scripts/.gitignore + RUN_REPORT 2026-06-28_run_01
  · branch `agent/2026-06-28_run_01`. RUN_REPORT scaffolded by
  `python .agent/scripts/new_run_report.py --task-id 2026-06-28_run_01`.
- `617a182` Add DECISION 2026-06-28_run_01 · branch
  `agent/2026-06-28_run_01`. DECISION scaffolded by
  `python .agent/scripts/new_decision.py --task-id 2026-06-28_run_01`.

**G2.1b (`2026-06-28_run_02`):**
- `8a3d0b3` Add TASK 2026-06-28_run_02 (cross-repo task)
- `7928fc3` Add RUN_REPORT 2026-06-28_run_02
- `b0b12ff` Add DECISION 2026-06-28_run_02

**G2.1c (`2026-06-28_run_03`):**
- `4b1ca02` Add TASK 2026-06-28_run_03 (cross-repo, scaffolded by `new_task.py`)
- `a612712` Add RUN_REPORT 2026-06-28_run_03 (scaffolded by `new_run_report.py`)
- `1668807` Add DECISION 2026-06-28_run_03 (scaffolded by `new_decision.py`)

**AgentOps-2a (`2026-06-28_run_04`):**
- `5189c07` Add TASK 2026-06-28_run_04 (policy/automation; pre-impl)
- `798bf69` Add Codex Claude automation policy (5 files; +896/−3)
- `de662a3` Add RUN_REPORT 2026-06-28_run_04
- `f1bab0f` Add DECISION 2026-06-28_run_04 (verdict `approve`)

**Daily summary updates:**
- `7c75119` Update daily summary for G2.1b
- `141ebdf` Update daily summary for G2.1c
- *(forthcoming)* Update daily summary for AgentOps-2a (this commit)

All web-repo commits pushed to `origin/main` after human approval.
Branches `agent/2026-06-28_run_01`, `agent/2026-06-28_run_02`, and
`agent/2026-06-28_run_03` all deleted locally (fully merged).

### Pipeline repo (`/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar`)

**G2.1b (`2026-06-28_run_02`):**
- `f833bfd` Add G2.1 taxonomy spec — added `corpus/taxonomy/G2.1_spec.md`
  (543 lines) and `corpus/taxonomy/README.md` (7 lines) on branch
  `agent/2026-06-28_run_02`. Fast-forward merged into `main` and pushed
  to `origin/main` after human approval. Branch deleted locally
  (fully merged).

**G2.1c (`2026-06-28_run_03`):**
- `b019786` Add G2.1 taxonomy eval set — added 4 files (`README.md`
  88 lines, `labeling_guide.md` 137 lines, `eval_set.jsonl` **37
  hand-labeled entries**, `examples.md` 44 lines auto-generated
  summary table) under `corpus/evals/taxonomy_eval/G2.1/` on branch
  `agent/2026-06-28_run_03`. Fast-forward merged into `main`
  (`f833bfd..b019786`, no merge commit) and pushed to `origin/main`
  after human approval. Branch deleted locally (fully merged).

## Build status

`all-pass`

- AgentOps-1c: `npm run build` not required (no app code edited).
- Post-merge smoke check of both new helpers on `main`: 8/8 pass
  (see "AgentOps-1c" section below).
- G2.1b: `npm run build` not required (documentation-only; no app code).
  No pipeline runtime invocation. Spec file structural review only.

## AgentOps-1c · new_run_report.py + new_decision.py

Third real AgentOps loop. Pure protocol-tooling — no app code touched.

**Files created on main:**

- `.agent/tasks/2026-06-28_run_01_TASK.md` (TASK, committed as `8f50ff4`)
- `.agent/scripts/new_run_report.py` (helper, 88 lines, stdlib only)
- `.agent/scripts/new_decision.py` (helper, 86 lines, stdlib only)
- `.agent/scripts/_common.py` (shared `resolve_task_id`, 47 lines)
- `.agent/scripts/.gitignore` (5 lines — scopes `__pycache__/` ignore to
  this directory; the repo-root `.gitignore` was deliberately untouched)
- `.agent/README.md` (extended "Helper scripts" section with two new
  subsections + a "Shared internals" note)
- `.agent/run_reports/2026-06-28_run_01_RUN_REPORT.md`
- `.agent/decisions/2026-06-28_run_01_DECISION.md`

Combined size of the two new helpers: **174 lines** (target ≤ 200).
Each helper: under the 120-line per-script cap.

**Loop:**
- Human approved the TASK content.
- Claude implemented both helpers + `_common.py` on
  `agent/2026-06-28_run_01`, ran a 12-step smoke test (9 standard + 3
  bonus edge cases), wrote the RUN_REPORT (`20b25d1`).
- ChatGPT wrote the DECISION (`617a182`) — `approve` with
  `human_approval_needed: yes` for merge/push, explicitly accepting
  the mid-run `_common.py` extract and the scoped `.gitignore` add.
- Human explicitly approved merge + push.
- Fast-forward merge succeeded (`8f50ff4..617a182`).
- Push to `origin/main` succeeded.

**Post-merge smoke check on main (8/8 pass):**

| # | check | result |
|---|---|---|
| A | `python .agent/scripts/new_run_report.py --help` + `new_decision.py --help` both render `--task-id` / `--date` / `--run` and "Local-only" footer | ✓ |
| B | `new_run_report.py --task-id 2099-01-01_run_01` → creates `2099-01-01_run_01_RUN_REPORT.md` | ✓ |
| C | `new_decision.py --task-id 2099-01-01_run_01` → creates `2099-01-01_run_01_DECISION.md` | ✓ |
| D | Rerun `new_run_report.py` → `error: refuse to overwrite` + exit 2 | ✓ |
| E | Rerun `new_decision.py` → `error: refuse to overwrite` + exit 2 | ✓ |
| F | DECISION contains `based_on_run_report: .agent/run_reports/2099-01-01_run_01_RUN_REPORT.md` | ✓ |
| G | `rm` both smoke artifacts | ✓ |
| H | `ls .agent/run_reports | grep 2099` + `ls .agent/decisions | grep 2099` both return empty | ✓ |

**Helper triple now dogfooded end-to-end.** Today's run is the first
where every step of the AgentOps file protocol was scaffolded by one of
the helpers we wrote:

```
TASK         → python .agent/scripts/new_task.py        --date 2026-06-28 --title "..." --risk yellow
RUN_REPORT   → python .agent/scripts/new_run_report.py  --task-id 2026-06-28_run_01
DECISION     → python .agent/scripts/new_decision.py    --task-id 2026-06-28_run_01
```

**Audit:**
- No app code touched.
- No `package.json` / `package-lock.json` edits.
- No prompt / classifier / extractor / model selection edits.
- No `.github/workflows/**` / cron edits.
- No pipeline repo files accessed.
- No `vercel.json` / `.vercel/**` / deploy config edits.
- **No OpenAI API integration** introduced (explicitly forbidden by the TASK).
- **No GitHub Actions / `@claude` integration** introduced (explicitly forbidden).
- **No daemon, watcher, scheduler, dashboard, autonomous loop, or
  general orchestrator** introduced (explicitly forbidden).
- No manual `vercel deploy` invocation. Vercel auto-deploy fired from
  the push as expected but served bundle is byte-identical to the
  post-P1.7 build.

## G2.1b · taxonomy spec doc (first cross-repo AgentOps task)

Second real loop of the day. The first AgentOps task whose
**implementation file lives in a different repo** than the
TASK/RUN_REPORT/DECISION trio. Pure documentation — no runtime change
in either repo.

**Host web repo files:**
- `.agent/tasks/2026-06-28_run_02_TASK.md`
- `.agent/run_reports/2026-06-28_run_02_RUN_REPORT.md`
- `.agent/decisions/2026-06-28_run_02_DECISION.md`

**Target pipeline repo files:**
- `corpus/taxonomy/G2.1_spec.md` (543 lines) — frozen reference taxonomy
- `corpus/taxonomy/README.md` (7 lines) — folder index

**Source-of-truth lineage:**
- G2.0 design memo · 2026-06-24 · pipeline repo
  `corpus/reports/G2_TAXONOMY_REFINEMENT_DESIGN_20260624.md` (commit
  `2b4c75f`)
- G2.1a design memo · 2026-06-28 · web repo
  `.agent/design_memos/2026-06-28_G2.1_taxonomy_refinement.md` (commit
  `22ea63d`, approved + pushed earlier today)
- G2.1b spec doc (this task) · pipeline repo
  `corpus/taxonomy/G2.1_spec.md` (commit `f833bfd`)

**Reviewer decisions baked into the spec (4):**
- Path A confirmed (spec → eval set → prompt dry-run).
- `data_eng_for_ai` and `eval` remain **separate** (not merged).
- Reject categories `non_ai_swe` and `ai_buzzword_only` are
  **sidecar / dry-run only** for now (no main-schema change, no DB
  columns).
- Manual dry-run only (no cron automation). Source-quality dashboard
  deferred.

**13-category taxonomy** (frozen for G2.1c–f reference):
- Included (11): `applied_ai`, `ai_product_engineering` (NEW),
  `agent_engineering`, `llm_infra`, `ml_platform`, `eval`,
  `data_eng_for_ai`, `forward_deployed`, `research_engineer`,
  `ml_engineer`, `ai_devrel`.
- Reject sidecar (2): `non_ai_swe`, `ai_buzzword_only`.
- Deferred (2): `llm_application_engineer`, `ai_solutions_engineer`.

**Promotion path (all 3 steps succeeded):**
1. Web repo push: `22ea63d..b0b12ff` to `origin/main` ✓
2. Pipeline repo ff-merge: `65c3f9b..f833bfd` (Updating `65c3f9b..f833bfd`, no merge commit) ✓
3. Pipeline repo push: `65c3f9b..f833bfd` to `origin/main` ✓

**Final synced states:**
- web `main` = `origin/main` = `b0b12ff`
- pipeline `main` = `origin/main` = `f833bfd`
- both working trees clean
- both `agent/2026-06-28_run_02` branches deleted locally (fully merged)

**Cross-repo audit (post-push):**

- **Web forbidden** — `src/**`, `src/lib/prompts.ts`,
  `src/data/web_bundle.json`, `package.json`, `package-lock.json`,
  `.env*`, `vercel.json`, `.vercel/**`, `.github/workflows/**` — all
  **unchanged ✓**.
- **Pipeline forbidden** — all `scripts/collector/*.py` (classifier,
  extractor, db, review_queue, etc.), `corpus/schema/**`,
  `corpus/state/**`, `corpus/web_bundle*.json`, `corpus/collector.db`,
  `corpus/_inbox|raw|processed|evals|reports|runs/**`, `sources.yaml`,
  `.github/workflows/**`, any prompt / model selection / cron file,
  any generated corpus file — all **unchanged ✓**.

**Confirmed NOT done during the run:**

- ❌ No `python -m scripts.collector ...` invocation (no pipeline
  collection, no classifier run, no extractor run).
- ❌ No `npm run build`.
- ❌ No manual Vercel deploy. Vercel may have auto-triggered from the
  web push, but the served bundle is byte-identical to the post-P1.7
  build (only `.agent/` changed in the web repo).
- ❌ No manual GitHub Actions trigger. Pipeline daily cron next fires
  at tomorrow 06:00 UTC on its normal schedule.
- ❌ No force push. No history rewrite. No rebase.

## G2.1c · hand-labeled taxonomy eval set (second cross-repo AgentOps task)

Third real loop of the day. Same cross-repo pattern as G2.1b — TASK /
RUN_REPORT / DECISION in the web repo's `.agent/`, implementation in
the pipeline repo under `corpus/evals/taxonomy_eval/G2.1/`. Pure
data + documentation; no runtime touched in either repo.

**Host web repo files:**
- `.agent/tasks/2026-06-28_run_03_TASK.md` (`4b1ca02`)
- `.agent/run_reports/2026-06-28_run_03_RUN_REPORT.md` (`a612712`)
- `.agent/decisions/2026-06-28_run_03_DECISION.md` (`1668807`)

**Target pipeline repo files** (committed in `b019786`):
- `corpus/evals/taxonomy_eval/G2.1/README.md` (88 lines)
- `corpus/evals/taxonomy_eval/G2.1/labeling_guide.md` (137 lines)
- `corpus/evals/taxonomy_eval/G2.1/eval_set.jsonl` (37 hand-labeled entries)
- `corpus/evals/taxonomy_eval/G2.1/examples.md` (44-line readable summary, auto-generated from the JSONL)

**Source-of-truth lineage:**
- G2.0 design memo (pipeline repo, 2026-06-24, `2b4c75f`)
- G2.1a design memo (web repo, 2026-06-28, `22ea63d`)
- G2.1b taxonomy spec (pipeline repo, 2026-06-28, `f833bfd`) — the
  spec G2.1c's labels reference
- G2.1c eval set (pipeline repo, 2026-06-28, `b019786`)

**Validation summary** (re-verified at DECISION time, all passed):
- JSONL parse: 37 rows, 37 unique `eval_id`s, all required fields, all valid enums.
- Verbatim-quote check: **0 misses** (89 quotes across 37 entries all appear literally inside their `jd_excerpt`).
- Excerpt length: min 231 / max **371** / mean 289 chars (cap 650).
- Composition minima all met: TP ≥5 for each of `applied_ai`,
  `ai_product_engineering`, `agent_engineering`, `forward_deployed`;
  FP-trap ≥5; FN-trap ≥5; all 6 long-tail archetypes (`llm_infra`,
  `eval`, `data_eng_for_ai`, `ml_platform`, `research_engineer`,
  `ai_devrel`) ≥1; both reject categories (`non_ai_swe`,
  `ai_buzzword_only`) surfaced via `expected_rejection_reason` ≥1.

**Promotion path (all 3 steps succeeded):**
1. Web repo push: `7c75119..1668807` to `origin/main` ✓
2. Pipeline repo ff-merge: `f833bfd..b019786` (drift guard ran first;
   `git pull --ff-only origin main` returned "Already up to date";
   merge is a clean fast-forward, no merge commit) ✓
3. Pipeline repo push: `f833bfd..b019786` to `origin/main` ✓

**Final synced states:**
- web `main` = `origin/main` = `1668807` (then advances by one more
  commit for this daily-summary update)
- pipeline `main` = `origin/main` = `b019786`
- both working trees clean
- pipeline branch `agent/2026-06-28_run_03` deleted locally (fully merged)

**Cross-repo audit (post-push):**

- **Web forbidden** — `src/**`, `src/lib/prompts.ts`,
  `src/data/web_bundle.json`, `package.json`, `package-lock.json`,
  `.env*`, `vercel.json`, `.vercel/**`, `.github/workflows/**` — all
  **unchanged ✓**.
- **Pipeline forbidden** — all `scripts/collector/*.py` (classifier,
  extractor, db, review_queue, …), `corpus/schema/**`,
  `corpus/state/**`, `corpus/web_bundle*.json`, `corpus/collector.db`,
  `corpus/_inbox|raw|processed|reports|runs/**`,
  `corpus/evals/golden_set/**` (and any other non-G2.1 path under
  `corpus/evals/`), `corpus/taxonomy/G2.1_spec.md` (spec read-only),
  `sources.yaml`, `.github/workflows/**` — all **unchanged ✓**.

**Confirmed NOT done:**

- ❌ No `python -m scripts.collector …` invocation (no collection /
  classifier / extractor run).
- ❌ No LLM call (no `anthropic` / `openai` SDK invocation, no HTTP
  request to any AI provider).
- ❌ No `npm run …` invocation.
- ❌ No manual Vercel deploy. Vercel may have auto-triggered from the
  web push; served bundle byte-identical (only `.agent/` changed in
  the web repo).
- ❌ No manual GitHub Actions trigger.
- ❌ No force push. No history rewrite. No rebase.
- ❌ AgentOps-2a implementation **not started yet at this point in
  the day** (TASK file `5189c07` was committed only; the 5 policy
  files were written in the following loop — see AgentOps-2a section
  below).

## AgentOps-2a · Codex + Claude Code automation policy

Fourth real loop of the day. Pure web `.agent/` policy + documentation.
**No runner / daemon / scheduler / cron / GitHub Actions / Codex config /
Claude Code config / OpenAI API introduced.** Policy is a contract a
future runner (AgentOps-2b, not yet scoped) must obey.

**Host web repo files (commits pushed to `origin/main` at `f1bab0f`):**

- TASK: `.agent/tasks/2026-06-28_run_04_TASK.md` (`5189c07`)
- Impl: `798bf69` Add Codex Claude automation policy — 5 files:
  - `.agent/policies/automation_policy.md` (402 lines, NEW) — 12 sections
    + change control. A/B time model
    (`SLEEP_WINDOW`/`WORKDAY_WINDOW`/`WEEKEND_WINDOW` +
    Non-Automation Time), 7 roles, Codex CLI rules (GPT-5.5 +
    reasoning effort `high`, never `extra-high`), Claude Code rules,
    Green/Yellow/Red risk table (with full Red-zone enumeration:
    classifier.py · extraction.py · schema · `src/lib/prompts.ts` ·
    `sources.yaml` · GitHub Actions · generated corpus · production
    bundle · deployment config · auth/payment/database · OpenAI API
    introduction · autonomous daemon), automation window report
    policy, queue policy, blockers policy, daily review workflow, 12
    stop conditions, Non-goals.
  - `.agent/templates/automation_window_report_template.md` (188
    lines, NEW) — 17-section per-window report shape; copy-paste-
    friendly for ChatGPT Chat review.
  - `.agent/automation_queue.md` (168 lines, NEW) — Codex backlog with
    6 seed items: QUEUE-0001 AgentOps-2a (`in_progress` → now
    `done`), QUEUE-0002 G2.1d (`blocked_pending_human`, red),
    QUEUE-0003 P1.7b hero mock (`candidate`, yellow), QUEUE-0004 P1.7c
    model-string SSOT (`candidate`, yellow/red TBD), QUEUE-0005
    `check_loop.py` (`deferred`, green), QUEUE-0006 MANUAL_DRY_RUN
    report (`candidate`, green).
  - `.agent/blockers.md` (113 lines, NEW) — schema + 3 initial open
    blockers: BLK-0001 G2.1d red-zone approval, BLK-0002 full
    automation activation, BLK-0003 standing OpenAI API block (open
    by design).
  - `.agent/README.md` (+25/−3) — directory tree extended; added
    "Automation policy (AgentOps-2a)" section linking the 4 new files.
- RUN_REPORT: `.agent/run_reports/2026-06-28_run_04_RUN_REPORT.md`
  (`de662a3`) — `ready_for_review: yes` / `requires_human_decision: yes`;
  5 risks enumerated; 3 follow-ups.
- DECISION: `.agent/decisions/2026-06-28_run_04_DECISION.md`
  (`f1bab0f`) — verdict **`approve`**, `human_approval_needed: yes`
  (for MANUAL_DRY_RUN, AgentOps-2b runner, G2.1d, full automation
  activation, OpenAI API usage, resolving any of the 3 open blockers);
  6 risks enumerated; 0 required fixes; 4 non-blocking follow-ups.

**DECISION verdict**: `approve`

**`human_approval_needed: yes`** remains required for:
- MANUAL_DRY_RUN (QUEUE-0006) execution
- AgentOps-2b runner scoping or implementation
- G2.1d classifier prompt rewrite + scoring dry-run (red, BLK-0001)
- Opening any real Automation Window
  (`SLEEP_WINDOW`/`WORKDAY_WINDOW`/`WEEKEND_WINDOW`)
- Any OpenAI API usage (BLK-0003 standing block)
- Resolving any of the 3 open blockers (BLK-0001/0002/0003)

**Validation summary (re-verified pre-push):**
- Scope clean: `git diff --name-only 141ebdf..HEAD` = exactly 7
  paths, all under `.agent/` (impl 5 + RUN_REPORT + DECISION).
- `.agent/`-only: no `src/**`, no app code, no test code.
- No pipeline-repo changes (pipeline HEAD pinned at `b019786` start
  to end).
- No OpenAI API: no SDK install, no key, no `.env` entry, no HTTP
  call. OpenAI mentioned only in `deferred` / `non-goals` / blocker
  contexts in the policy file (as prohibition language).
- No runner / daemon / scheduler / cron created. Sanity grep: no
  `## Cron job` / `## Implementation runner` / `## Daemon` /
  `## OpenAI integration` headings present.
- No `.github/workflows/**` changes. No new workflow files. Existing
  cron untouched.
- No Codex CLI config edit. No Claude Code config edit. No
  `~/.codex/config.toml` or `~/.claude/settings.json` touched.
- No manual deploy. Vercel auto-trigger from push is non-disruptive
  (served bundle byte-identical — only `.agent/` changed).
- No force push. No history rewrite. No rebase.

**Promotion path (all 3 steps succeeded):**
1. Web repo push: `141ebdf..f1bab0f` to `origin/main` ✓ (3 commits:
   `798bf69` impl + `de662a3` RUN_REPORT + `f1bab0f` DECISION)
2. Pipeline repo: **not touched** (read-only sanity at `b019786`
   start to end). Cross-repo ff-merge **not applicable** —
   AgentOps-2a is web-only.
3. Post-push cleanup commit (this commit): daily summary update +
   QUEUE-0001 marked `done`.

**Final synced state (after push):**
- web `main` = `origin/main` = `f1bab0f` (then advances by one more
  commit for this daily-summary cleanup)
- pipeline `main` = `origin/main` = `b019786` (unchanged from G2.1c)
- both working trees clean
- no `agent/2026-06-28_run_04` branch was cut (yellow policy/doc
  task; same pattern as the TASK commit, which landed on `main`
  directly)

**Next recommended task: QUEUE-0006 MANUAL_DRY_RUN report.** A
hand-filled example automation window report against a real recent
day (e.g. today) that validates the contract end-to-end — without
needing a runner to exist.

**Explicitly NOT started / NOT active:**
- ❌ Full automation is **NOT active yet** (BLK-0002 blocks
  activation until MANUAL_DRY_RUN reviewed + AgentOps-2b runner
  exists with its own scope-and-approve loop).
- ❌ AgentOps-2b runner is **NOT approved yet** — out of order until
  MANUAL_DRY_RUN validates the contract first.
- ❌ G2.1d remains **blocked_pending_human + ChatGPT review**
  (BLK-0001 + QUEUE-0002 marked `blocked_pending_human`); Codex MUST
  NOT self-promote even though G2.1c eval set is now live in the
  pipeline.
- ❌ OpenAI API remains **blocked** (BLK-0003 standing block, open
  by design).
- ❌ No real Automation Window opened (`SLEEP_WINDOW`,
  `WORKDAY_WINDOW`, `WEEKEND_WINDOW` all remain conceptual).
- ❌ No MANUAL_DRY_RUN started yet — that is the next safe task,
  but executes only with explicit human GO.

## Forbidden / red-zone audit (across today's work)

### Web repo
- `src/**` — untouched (across both runs)
- `src/lib/prompts.ts` — untouched
- `src/data/web_bundle.json` — untouched
- `package.json` / `package-lock.json` — untouched
- `.env*` — untouched
- `vercel.json` / `.vercel/**` — untouched
- `.github/workflows/**` — untouched
- No new npm dependencies

### Pipeline repo (now accessed via G2.1b)
- All `scripts/collector/*.py` (classifier, extractor, db, review_queue, runner, snapshot, …) — untouched
- `corpus/schema/**` — untouched (no enum extension, no schema bump)
- `corpus/state/**` — untouched (canonical dump untouched)
- `corpus/web_bundle.json` (V1 hand-curated) — untouched
- `corpus/web_bundle_pipeline.json` — untouched
- `corpus/collector.db` — untouched (gitignored runtime state)
- `corpus/_inbox|raw|processed|evals|reports|runs/**` — untouched
- `sources.yaml` — untouched (no source registry change)
- `.github/workflows/**` — untouched (no cron change)
- No prompt / classifier / extractor file edits
- No model selection edits
- No `python -m scripts.collector ...` invocation (no pipeline collection / classifier / extractor run)
- No new Python dependencies

### Both repos (process-level)
- No OpenAI API integration files
- No GitHub `@claude` integration / new workflows
- No daemon / watcher / scheduler / dashboard / TUI / web UI / autonomous loop
- No force-push, no history rewrite, no destructive reset
- No manual deploy (Vercel auto-trigger from web push is non-disruptive — served bundle byte-identical)

## Open risks

1. **Keep the helper scripts as file-template helpers only.** Their
   only job is to scaffold a single file from a single template per
   invocation. Anything that orchestrates *between* files (auto-running
   the next step, watching directories, etc.) is out of scope.
2. **Do not let `.agent/scripts/` grow into a general orchestrator
   without a separate design memo.** If a future task proposes shared
   state, scheduling, or "run the whole loop" semantics, it requires
   its own design memo and `risk_level: red` approval. The current
   helpers are deliberately three independent scripts that each do one
   small thing.
3. **`check_loop.py` audit helper deferred.** A small script that
   walks `tasks/`, `run_reports/`, `decisions/` and flags any
   `task_id` missing one of the three would be useful as a quick
   audit. Only add it *if* a missing file actually happens; do not
   pre-build it.
4. **Shared `_common.py` could later cover `new_task.py` too**, but
   the marginal value is tiny (the duplication is ~15 lines of date
   validation). Skip until a fourth helper appears.

## Recommended next-day tasks

> G2.1a memo, G2.1b spec, and G2.1c eval set are all shipped to both
> repos. AgentOps-2a policy/docs are now live on `origin/main` of the
> web repo. **Next safe step is QUEUE-0006 MANUAL_DRY_RUN — not full
> automation activation and not the AgentOps-2b runner.** Path A's
> next stop after that is **G2.1d** (red, classifier prompt rewrite
> + scoring dry-run), which remains blocked pending human + ChatGPT
> red-zone approval.

1. **QUEUE-0006 · MANUAL_DRY_RUN automation report** — risk `green`
   (web `.agent/automation_runs/` only). A single hand-filled example
   window report against a real recent day's work (e.g. today)
   following `.agent/templates/automation_window_report_template.md`.
   Validates the AgentOps-2a contract end-to-end **without any
   automation runner needing to exist**. Lifts no blocker. Must NOT
   activate any real Automation Window.
2. **P1.7b · sync hero mock numbers with `web_bundle.json`** — risk
   `yellow`. Read live values at build/render time instead of
   hardcoding `92` and `5`. Depends on: P1.7 (`2026-06-27_run_01`).
3. **P1.7c · model-string single source of truth** — risk `yellow`.
   Export a `MODELS_DISPLAY` const from a non-prompt module so the
   homepage caption stays in sync with `src/lib/anthropic.ts` model
   strings. Must NOT touch `src/lib/anthropic.ts`'s model-selection
   logic.
4. *(Path A later step)* **G2.1d · classifier prompt rewrite + scoring
   dry-run** — risk `red`, **blocked_pending_human** (QUEUE-0002 +
   BLK-0001). Schema not touched (sidecar-only per spec §5); but
   classifier prompt and a `CLASSIFIER_VERSION` bump are needed. Will
   read the G2.1c eval set as ground truth. Requires explicit human
   approval **before execution**, not just before merge.
5. *(Possibly later, NOT urgent)* **AgentOps-2b runner scoping** —
   smallest safe automation runner that obeys
   `.agent/policies/automation_policy.md`. **Out of order until
   MANUAL_DRY_RUN validates the contract first.** Not approved yet.
6. *(Possibly later, NOT urgent)* **`check_loop.py` audit helper**
   (QUEUE-0005, `deferred`). Walks the three `.agent/` subdirs to
   flag any incomplete TASK ↔ RUN_REPORT ↔ DECISION triple. Skip
   until missing files actually show up.

## Human decisions needed

- Pick the next task. Default is **QUEUE-0006 MANUAL_DRY_RUN report**
  to stress-test the AgentOps-2a contract; alternatives are P1.7b /
  P1.7c if you'd rather not touch the automation surface yet.
- (No red-zone approvals pending right now. G2.1d will need one when
  we reach it; full automation activation will need one before any
  real Automation Window opens; OpenAI API will need one before any
  OpenAI-API-shaped work starts. All three are tracked in
  `.agent/blockers.md`.)
