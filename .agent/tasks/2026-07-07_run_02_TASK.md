# TASK · P2.1a real generated report quality audit

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-07_run_02`
- **date**: `2026-07-07`
- **run_number**: `02` (P2.0c was `2026-07-07_run_01`)
- **title**: P2.1a real generated report quality audit
- **repo**:
  `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Audit — **without changing any code** — whether
the real generated-report experience is strong
enough to justify the product promise on the
homepage. Produce a single opinionated design
memo covering product-promise-vs-flow,
evidence quality, personalization quality,
actionability, UX risks, output-quality risks,
and 5 ranked next-task candidates with a single
default recommendation. This closes the freshness
credibility line (P2.0a/b/c) with an equally
honest read of the *report content* line.

## Background

- P2.0 line is fully shipped:
  P2.0a design memo (freshness) →
  P2.0b visible chip
  `Corpus snapshot: May 14, 2026` →
  P2.0c standalone `WEB_BUNDLE_STATS` drift
  check.
- Freshness disclosure is now honest. The next
  natural credibility question is the *report
  itself*: does the real generated output live
  up to the homepage's hero promise and the
  sample-report's polish? This memo answers
  that on-paper, before touching any code.
- Explicitly out-of-scope: prompt edits, model
  swaps, API-route edits, corpus refresh,
  bundle swap, sample-report/methodology copy
  edits, screenshot regeneration, and any
  further P2.0-track code loops (P2.0c
  DECISION §follow-up says "soak-first"
  before jumping to build-gate / codegen).

## Allowed files

> Paths the executor MAY edit. Glob patterns OK. If a path is not on this
> list AND not on `forbidden_files`, executor must ask before editing.

- `.agent/tasks/2026-07-07_run_02_TASK.md`
  (this file)
- `.agent/run_reports/2026-07-07_run_02_RUN_REPORT.md`
- **`.agent/design_memos/2026-07-07_P2.1a_real_report_quality_audit.md`**
  — new opinionated design memo.

## Read-only allowed (must NOT be modified)

- `src/app/page.tsx` — homepage promise, form,
  streaming reader, error UI, eval invocation.
- `src/app/sample-report/page.tsx` — fictional
  static preview.
- `src/app/methodology/page.tsx` — "What this
  corpus is / is NOT", "V1 ships snapshot-
  only".
- `src/app/api/generate-report/route.ts` —
  request shape, model choice, streaming
  behavior.
- `src/app/api/eval-report/route.ts` +
  `src/lib/eval-report.ts` — 3-judge eval
  flow.
- `src/app/api/classify/route.ts` — archetype
  classifier.
- `src/app/api/extract-pdf/route.ts` +
  `src/lib/extract-pdf.ts` — PDF-to-text.
- `src/lib/prompts.ts` — classifier +
  report-generator system prompts.
- `src/lib/corpus.ts` — archetype profile,
  evidence-JD picking, deep-dive company
  filter.
- `src/lib/web-bundle-stats.ts` — helper
  constants (6 fields).
- `src/data/web_bundle.json` — corpus (443
  records / 35 companies / 47 applied_ai).
- `package.json`, README/docs if any.
- Pipeline repo `/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar`
  — read-only sanity (must remain untouched).

## Forbidden files

> Paths the executor MUST NOT edit, even if it seems useful. Glob patterns OK.

- **`src/**` any file** — this task must not
  change product code.
- `src/data/**` — including
  `web_bundle.json`,
  `web_bundle_pipeline.json`,
  `web_bundle_staging.json`.
- `src/lib/prompts.ts`
- `src/app/api/**`
- **Pipeline repo any file**
  (`/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar/**`)
  — `sources.yaml`, `corpus/**`,
  `scripts/collector/**`,
  `.github/workflows/**`.
- `.agent/scripts/**` — hard rule per
  AgentOps-2c Q3-Q8.
- `.agent/policies/**`
- `.agent/templates/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.github/workflows/**` (web repo)
- `package.json`
- `package-lock.json`
- `.env*`
- `vercel.json` / `.vercel/**`
- Codex CLI / Claude Code config
- Any OpenAI API SDK / import / key / HTTP
  call

## Risk level

`yellow`

> See `.agent/policies/agent_policy.md` §2 for definitions. Red tasks require
> a prior approval reference (see `requires_human_approval`).

Yellow-adjacent (design-memo-only). Same class
as P2.0a. No code, no prompt, no data, no
config edit. Blast radius: 3 `.agent/` files
(TASK + memo + RUN_REPORT). No user-visible
change.

## Constraints

> Hard rules for this specific task. Anything beyond the global policy.

- **No product code edits.** No `src/**` diff.
- **No prompt edits** (`src/lib/prompts.ts`).
- **No API-route edits** (`src/app/api/**`).
- **No model selection edits.**
- **No collector run.**
- **No corpus refresh.**
- **No pipeline repo edits.**
- **No `sources.yaml` edits.**
- **No GitHub Actions edits.**
- **No `.agent/scripts/**` edits.**
- **No manual `vercel deploy`.**
- **No push.**
- Memo must be **opinionated** and pick
  **exactly one** default next task.
- Memo must rank **5 candidate next tasks**
  with per-candidate expected value / risk /
  files / green-yellow-red / human-approval /
  why-now-or-not.
- Memo must cite files by `path:line` where
  making a specific claim about behavior.
- Memo must not invent behavior — every
  behavioral claim must be grounded in the
  actual files inspected.

## Acceptance criteria

> Checklist that the RUN_REPORT must demonstrate as satisfied.

- [ ] `.agent/design_memos/2026-07-07_P2.1a_real_report_quality_audit.md`
      exists and contains all 10 required
      sections (Executive verdict, Product
      promise vs actual flow, Report
      structure, Evidence quality,
      Personalization quality, Actionability,
      UX risks, Output-quality risks,
      Recommended next task candidates,
      Default recommendation).
- [ ] Memo cites at least 15 specific
      `path:line` references across
      `src/app/page.tsx`,
      `src/lib/prompts.ts`,
      `src/lib/corpus.ts`,
      `src/app/api/generate-report/route.ts`,
      `src/app/api/eval-report/route.ts`,
      `src/lib/eval-report.ts`,
      `src/app/sample-report/page.tsx`,
      `src/app/methodology/page.tsx`,
      `src/lib/extract-pdf.ts`.
- [ ] Executive verdict picks exactly one of
      three states: strong-enough-to-keep /
      promising-but-needs-targeted-improvement
      / not-yet-credible-enough. Not "it
      depends".
- [ ] §9 (next-task candidates) contains
      exactly **5** ranked candidates with
      per-candidate expected value, risk,
      files, green/yellow/red, human-approval,
      why-now-or-not.
- [ ] §10 (default recommendation) picks
      exactly **one** next task, not two.
- [ ] Memo respects P2.0c DECISION
      §follow-ups: does NOT recommend
      build-gate integration or codegen as the
      default.
- [ ] Memo respects standing blockers: does
      NOT recommend G2.1d
      (BLK-0001), automation-infra resumption
      (Q10), OpenAI API usage (BLK-0003), or
      full automation activation (BLK-0002).
- [ ] No `src/**` files modified.
- [ ] No `src/data/**` files modified.
- [ ] No prompt files modified.
- [ ] No API-route files modified.
- [ ] No pipeline repo modification (pipeline
      HEAD = `b019786` at run start AND end).
- [ ] No collector invocation.
- [ ] No LLM call by this task.
- [ ] No `.agent/scripts/**` diff.
- [ ] No push, no manual deploy.
- [ ] RUN_REPORT lists all files inspected +
      audit verdict + top 5 risks + the
      recommended next task.

## Validation commands

> Exact commands the executor should run to validate. Copy-paste-able.

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# --- structural check ---
wc -l .agent/design_memos/2026-07-07_P2.1a_real_report_quality_audit.md
grep -c "^## " .agent/design_memos/2026-07-07_P2.1a_real_report_quality_audit.md   # expect ≥10

# --- diff audit ---
git status
git diff --stat
git diff --name-only

# --- forbidden audit ---
git diff --name-only origin/main..HEAD | grep -E "src/|.agent/scripts/|.github/workflows/|package|vercel|.env" || echo "  ✓ CLEAN (forbidden empty)"

# --- pipeline read-only sanity ---
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git log --oneline -3
```

## Expected output

RUN_REPORT must include:

- Files inspected (with paths).
- Audit verdict verbatim (one line).
- Top 5 risks (short list).
- The single recommended default next task.
- Forbidden-file audit (each bucket ✓ CLEAN).
- Confirmation: no `src/**` diff, no
  `src/data/**` diff, no prompt / API-route /
  model-selection / pipeline / collector /
  corpus-refresh / OpenAI-API /
  `.agent/scripts/**` diff, no push, no
  manual deploy.
- Recommendation: Human + ChatGPT review the
  RUN_REPORT + audit memo, then decide next
  task.

## Rollback plan

> How to undo this task if the DECISION is `request_changes` or `stop`.

Both commits are local-only until push.

- If DECISION is `request_changes` with
  memo-content edits: amend the memo and add a
  follow-up "Post-review correction" commit
  (same pattern as P2.0b `e3fbbab`).
- If DECISION is full reject: `git reset
  --soft HEAD~2` (drop impl + RUN_REPORT
  commits, keep changes staged for
  redrafting).
- If already pushed after DECISION and needs
  full revert: `git revert <impl_commit>
  <run_report_commit>` and push.

## Requires human approval

`no` for the audit-memo landing itself.

> Approved by:
> Bohao's message 2026-07-07 (start P2.1a).
> No prior DECISION file names P2.1a; this
> is the first entry point of the report-
> quality line. The message explicitly says
> "no-code audit". No red-zone content.
>
> Push and deploy remain separate
> human-approval gates.

## Max runtime minutes

45

## Max token budget hint

`unset`

## Stop conditions

> Beyond the global policy (`.agent/policies/agent_policy.md` §3), task-specific
> stop conditions.

- Stop if a `src/**` file appears in `git
  status` (this task must not modify product
  code).
- Stop if `src/data/**` appears in `git
  status`.
- Stop if `.agent/scripts/**` appears in `git
  status` (hard rule).
- Stop if any pipeline-repo file appears in
  `git status`.
- Stop if `package.json` /
  `package-lock.json` appears in `git
  status`.
- Stop if the memo cannot honestly reach an
  opinionated single-default recommendation
  (surface the ambiguity in RUN_REPORT
  rather than fabricating one).
- Stop if the memo would need to recommend
  G2.1d / automation-infra resumption /
  OpenAI API / BLK-0002 / BLK-0003 as the
  default.
