# TASK · AgentOps 5e complete Phase 3 Fixture B after classify hardening

## Metadata

- **task_id**: `2026-07-25_run_02`
- **date**: `2026-07-25`
- **run_number**: `02`
- **title**: AgentOps 5e complete Phase 3 Fixture B after classify hardening
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-5e-followup-phase3-fixture-b-completion-execute`
- **parent_loop**: `AgentOps-5e-followup-phase3-classify-json-hardening-implement` (`2026-07-25_run_01`)
- **authorizing_decision**: `.agent/decisions/2026-07-25_run_01_DECISION.md`
- **human_cost_approval**: Bohao msg 2026-07-25 (this turn) — Fixture B exactly once, zero retries, fresh $0.075 per-run cap, $0.15 total cap, newly authorized completion run (NOT retry under run_08 approval).

## Objective

Execute Fixture B exactly once against the classify JSON hardening (now on main HEAD `a565100`) to complete the missing second end-to-end Phase 3 report sample. Verify: (a) `/api/classify` returns 200 via `generateObject`-based hardening; (b) `/api/generate-report` is reached and a complete report is captured; (c) QI + structural + combined telemetry all emit correctly; (d) legacy `checks[]` / `classify(checks)` / `process.exit(classification.exit)` remain unchanged; (e) `affected_legacy_verdict = false` throughout; (f) cost stays within $0.075 per-run + $0.15 total caps; (g) exactly one Fixture B invocation, zero retries.

## Background

Phase 3 execution (`run_09`) partially completed: Fixture A end-to-end PASS · Fixture B pre-generation `/api/classify` 502 (Anthropic malformed JSON). Diagnostics (`run_10`) confirmed root cause. Hardening design (`run_11`) selected Option A: `generateObject` + strict Zod schema + `maxRetries: 0` + no `experimental_repairText` + 1 provider call. Hardening implementation (`2026-07-25_run_01`) shipped Option A · 117/117 tests PASS · tsc exit 0 · pushed `a565100`. Fixture B has NOT been rerun since hardening. This TASK executes the newly authorized completion run.

## Allowed files

- `.agent/tasks/2026-07-25_run_02_TASK.md`
- `.agent/regression_runs/<new-run-id>/**` (governance-safe run artifacts written by the harness)
- `.agent/run_reports/2026-07-25_run_02_RUN_REPORT.md`

## Forbidden files

- **Any implementation code** (`src/**`, `scripts/**` including tests, `package.json`, `package-lock.json`)
- Prompts (`src/lib/prompts.ts`) · QI checker · structural checker · harness · fixtures · baselines · previous run artifacts · `.agent/scripts/**`
- Workflows / env / `vercel.json` / pipeline

## Risk level

`yellow`

## Constraints

- **Exactly one Fixture B invocation.**
- **No Fixture A invocation.**
- **Zero retries** (automatic OR manual).
- **Hard per-run cap $0.075** · **hard total cap $0.15**.
- **No modification** of implementation / tests / prompts / checkers / harness / fixtures / baselines / package files / telemetry / legacy verdict / process exit.
- **No push · no deploy · no DECISION in this turn.**
- **No Phase 4/5/6 · no `AgentOps-5f-promote` · no blocking promotion.**

## Acceptance criteria

- [x] TASK for run 02 created.
- [ ] Preflight tests 117/117 PASS + `tsc --noEmit` exit 0.
- [ ] Dev server started (separate terminal) + Ready confirmed.
- [ ] Fixture B invoked exactly once via `node scripts/report-regression-local.mjs --fixture B`.
- [ ] Run ID recorded + governance-safe artifacts inspected.
- [ ] Result classified (Case A full success / Case B classify typed failure / Case C other failure).
- [ ] No `report.md` / screenshot / raw log / secret committed.
- [ ] No source / test / package / baseline / fixture / telemetry / .agent/scripts / workflow / env / vercel diff.
- [ ] Cost ≤ $0.075 per-run, ≤ $0.15 total (estimated; harness `cost_measured=false`).
- [ ] Zero retries · zero Fixture A invocations · exactly one Fixture B invocation.
- [ ] Pipeline `b019786` untouched.
- [ ] RUN_REPORT authored covering all 25 assessment questions (Q1-Q25).

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web
# Preflight (before paid call):
npx tsc --noEmit
node scripts/test-classify-schema.mjs
node scripts/test-classify-route.mjs
node scripts/test-structural-evidence-check.mjs
node scripts/test-structural-evidence-integration.mjs
# Paid Fixture B execution (separate terminal for dev server):
npm run dev   # separate terminal · wait for Ready
node scripts/report-regression-local.mjs --fixture B
# Governance:
git status
git diff --stat -- src scripts package.json package-lock.json .agent/scripts .agent/regression_baselines .agent/regression_fixtures
```

## Expected output

- 2 commits (run artifacts + RUN_REPORT).
- Web ahead by 2 vs origin/main.
- Pipeline `b019786` untouched.
- Cost: **estimated ≤ $0.075** (harness `cost_measured=false`).

## Rollback plan

- Revert 2 commits — reverts new run directory + RUN_REPORT.
- No baseline / code / prompt change to reverse.

## Requires human approval

`yes`

> Approved by: Bohao 2026-07-25 explicit cost-approved GO message.
> Implementation reference: `.agent/decisions/2026-07-25_run_01_DECISION.md`.

## Max runtime minutes

15

## Max token budget hint

`unset`

## Stop conditions

- Stop if classify HTTP status is not 200.
- Stop if `/api/generate-report` fails (non-200).
- Stop if app-error state detected.
- Stop if provider error / timeout / rate limit.
- Stop if captured report is missing.
- Stop if unexpected second provider invocation.
- Stop if unexpected retry (automatic or manual).
- Stop if unexpected code / baseline / fixture / package mutation.
- Stop if per-run cost cap risk is unclear.
- Stop after B — do NOT run any additional fixture or retry attempt.
