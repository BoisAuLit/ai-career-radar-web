# TASK · AgentOps-3e minimal local Playwright regression prototype

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-12_run_03`
- **date**: `2026-07-12`
- **run_number**: `03`
- **title**: AgentOps-3e minimal local
  Playwright regression prototype
- **repo**:
  `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Implement the **first minimal local
Playwright report regression prototype**
against **Fixture A only**. This is the
first design → code jump in the
AgentOps-3 track and the first artifact
to actually generate a real report
through the regression harness. Scope
must stay strictly narrow per
AgentOps-3d DECISION `9f3cdfa`:
Playwright CLI, localhost target,
Fixture A, no baseline promotion, no
production, no auto-push, no
`.agent/scripts/**` edit (harness
script lives under repo-root
`scripts/`).

## Background

- **AgentOps-3d DECISION**
  (`.agent/decisions/2026-07-12_run_02_DECISION.md`,
  commit `9f3cdfa`) approved the
  harness design + 14 initial
  answers to open decisions and
  named AgentOps-3e as default
  next code loop.
- **AgentOps-3c fixtures**
  (`b284831`) provide the
  synthetic input set. Fixture A
  = senior backend SWE → Applied
  AI Engineer.
- **Existing script pattern**:
  `scripts/screenshot.mjs`
  (Playwright CLI · Node ESM ·
  zero new deps) +
  `scripts/check-web-bundle-stats.mjs`
  (P2.0c). Harness follows the
  same convention.

## Allowed files

- `.agent/tasks/2026-07-12_run_03_TASK.md`
  (this file)
- **`scripts/report-regression-local.mjs`**
  (new harness script)
- `.agent/regression_runs/<run-id>/metadata.json`
- `.agent/regression_runs/<run-id>/structural_checks.json`
- `.agent/regression_runs/<run-id>/verdict.md`
- `.agent/run_reports/2026-07-12_run_03_RUN_REPORT.md`

## Conditionally allowed

- `.gitignore` only if needed to
  prevent large local artifacts from
  being committed. Prefer not to
  modify if avoidable.
- `package.json` only if absolutely
  necessary. Prefer not to modify;
  harness invokes via `node
  scripts/report-regression-local.mjs`.

## Forbidden files

- `src/**` any file
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `.agent/scripts/**` — hard rule.
- `.agent/policies/**`
- `.agent/templates/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/regression_fixtures/**`
- `.agent/planner_reports/**`
- Pipeline repo any file
- `sources.yaml` (pipeline)
- `corpus/**` (pipeline)
- `scripts/collector/**` (pipeline)
- `.github/workflows/**`
- `package-lock.json`
- `.env*`
- `vercel.json`
- Codex CLI / Claude Code config
- Any production deployment config
- Any baseline promotion file

## Risk level

`yellow`

Yellow because this is the first
design → code jump in AgentOps-3
and drives a real LLM call
(cost ~$0.05). Same class as
Candidate 1 / Candidate 4 / P2.0c.
Blast radius: 1 new runtime file
(`scripts/*.mjs`) + 3 small in-repo
artifacts.

## Constraints

- **Playwright CLI only** (not
  MCP; per AgentOps-3d §5.3 +
  DECISION answer #1).
- **Localhost target only** (per
  DECISION answer + §6 Stage 1).
  Script must hard-reject
  non-localhost URLs.
- **Fixture A only** (per
  DECISION answer #5).
- **No new npm dependency**.
- **No `package-lock.json`
  change**.
- **No `.env*` read** by the
  script.
- **No OpenAI / Anthropic API
  HTTP call** by the script.
  (The local app calls Anthropic
  through the normal Next.js
  runtime; the harness only
  drives the browser.)
- **No production target**.
- **No baseline promotion.**
- **No Eval click in v1** (per
  DECISION answer #8).
- **Chromium headless**.
- **Max 1 real generation per
  run** (per DECISION answer +
  §13 v1 thresholds).
- **Cost cap**: $0.25 hard,
  enforced by single-generation
  policy since exact cost is
  not measurable from the
  harness.
- **Large artifacts**
  (`report.md`, `screenshot.png`)
  live in **local scratchpad
  outside repo**; only
  lightweight
  (`metadata.json`,
  `structural_checks.json`,
  `verdict.md`) commit.
- **No push, no manual
  deploy**.
- **No `.agent/scripts/**`
  edit** (hard rule).
- **No `src/**` edit**.
- **No pipeline edit**.
- **No collector run**.
- **No corpus refresh**.
- **No GH Actions edit**.

## Acceptance criteria

- [ ] `scripts/report-regression-local.mjs`
      exists (Node ESM, uses
      Playwright).
- [ ] Script hard-rejects
      non-localhost target hosts
      (allowed: `localhost`,
      `127.0.0.1`).
- [ ] Script parses Fixture A
      by section headers.
- [ ] Script does NOT modify
      the fixture file.
- [ ] Script fills `#resume`
      + target textarea.
- [ ] Script clicks Generate.
- [ ] Script waits for done
      state (Copy report
      button appears).
- [ ] Script detects
      incomplete banner.
- [ ] Script captures report
      text.
- [ ] Script does NOT click
      Eval.
- [ ] Script screenshot →
      local scratchpad only.
- [ ] Script writes 3
      committed artifacts:
      `metadata.json`,
      `structural_checks.json`,
      `verdict.md` under
      `.agent/regression_runs/<run-id>/`.
- [ ] Script does NOT commit
      full `report.md` or
      screenshot.
- [ ] Run-id = UTC ISO
      timestamp
      (`YYYYMMDDTHHMMSSZ_fixture-A`).
- [ ] Verdict is one of
      green / amber / red.
- [ ] Exit code: green 0,
      amber 2, red 1.
- [ ] Metadata includes:
      run_id, fixture_id,
      fixture_version,
      base_url, started_at,
      finished_at,
      duration_ms,
      git_commit_sha,
      corpus_snapshot_date,
      model display,
      report_char_count,
      verdict, artifact
      policy, local scratch
      paths.
- [ ] Structural checks
      cover: page loaded,
      resume filled, target
      filled, generate
      clicked, done reached,
      incomplete banner
      absent, report
      non-empty, 5 section
      headers, length band,
      action bar buttons.
- [ ] Fixture-specific
      checks cover: ≥2
      expected strengths,
      ≥2 expected gaps,
      zero must-not-happen,
      recommendation match.
- [ ] Operational checks
      cover: duration under
      soft/hard, no fatal
      Playwright error, no
      production target.
- [ ] Local app is
      running or explicitly
      started; script
      documents which.
- [ ] Cost measurement
      documented
      (`cost_measured:
      false` +
      `cost_cap_enforced_by:
      "single_generation_limit"`).
- [ ] No new npm
      dependency.
- [ ] No `package-lock.json`
      change.
- [ ] No `src/**` change.
- [ ] No `.agent/scripts/**`
      change.
- [ ] No pipeline change
      (HEAD `b019786` at
      start AND end).
- [ ] No push, no manual
      deploy.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# ensure dev is running (Bohao's session or start it)
# curl -s -o /dev/null -w "%{http_code}" http://localhost:3000

node scripts/report-regression-local.mjs
echo "exit=$?"

# diff audit
git status
git diff --stat
git diff --name-only

# forbidden audit
git diff --name-only origin/main..HEAD | grep -E "src/|.agent/scripts/|.agent/policies/|.agent/templates/|.agent/blockers.md|.agent/automation_queue.md|.agent/regression_fixtures/|.agent/planner_reports/|.github/workflows/|package-lock|vercel|.env" || echo "  ✓ CLEAN (forbidden empty)"

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git log --oneline -3
```

## Expected output

RUN_REPORT must include:

- Script path.
- Fixture used (A).
- Run artifact path.
- Files changed.
- How local app was run.
- Whether one real report
  generation happened.
- Whether API cost was
  measured or bounded.
- Exact verdict + exit code.
- Duration + report char
  count.
- Structural / fixture /
  operational check
  summaries.
- Local scratch paths.
- Artifact storage policy
  justification.
- Why report.md / screenshot
  not committed.
- Forbidden-file audit.
- Confirmation: no prod
  target, no baseline
  promotion, no Eval click,
  no OpenAI API, no
  `.agent/scripts` change,
  no `src/**` change, no
  pipeline change, no
  collector, no corpus
  refresh, no push/deploy.

## Rollback plan

- Small correction →
  follow-up commit.
- Full reject → `git reset
  --soft HEAD~2`; delete
  `.agent/regression_runs/`
  contents.
- After push, needs revert
  → `git revert
  <impl_commit>
  <run_report_commit>`.

## Requires human approval

`no` for landing.

> Approved by:
> `.agent/decisions/2026-07-12_run_02_DECISION.md`
> (AgentOps-3d DECISION named
> 3e as default next code
> loop). Bohao's message
> 2026-07-12
> "Start AgentOps-3e"
> reconfirmed.
>
> Push and deploy remain
> separate human-approval
> gates.

## Max runtime minutes

45

## Max token budget hint

`unset`

## Stop conditions

- Stop if any file in `src/**`
  or `.agent/scripts/**`
  appears in `git status`.
- Stop if `.agent/regression_fixtures/**`
  appears (frozen).
- Stop if `package-lock.json`
  appears.
- Stop if any pipeline-repo
  file appears.
- Stop if the harness cannot
  find the local app running
  and cannot start it
  cleanly.
- Stop if the harness would
  target a non-localhost
  URL.
- Stop if the harness would
  click Eval.
- Stop if the harness would
  make an OpenAI / Anthropic
  API HTTP call directly.
- Stop if more than one real
  generation would be
  triggered.
