# TASK · AgentOps-3c benchmark resume fixtures

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-12_run_01`
- **date**: `2026-07-12`
- **run_number**: `01`
- **title**: AgentOps-3c benchmark resume
  fixtures
- **repo**:
  `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Create a **small, stable synthetic
benchmark resume fixture suite** for
future real-report regression testing.
5 personas A-E under
`.agent/regression_fixtures/` plus a
`README.md`. Green Markdown-only work.
No LLM call, no report generation, no
Playwright, no runner, no planner
implementation.

The fixtures will later be consumed
by the (still-unbuilt) report
regression harness (AgentOps-3d/3e)
to upload stable resume inputs,
generate real reports, compare
outputs over time, and detect
report-quality regressions.

## Background

- **AgentOps-3a DECISION**
  (`.agent/decisions/2026-07-08_run_01_DECISION.md`)
  named AgentOps-3c benchmark resume
  fixtures as a parallel green
  candidate loop.
- **AgentOps-3b DECISION**
  (`.agent/decisions/2026-07-11_run_01_DECISION.md`)
  reaffirmed AgentOps-3c as the
  default next code loop after 3b;
  explicitly pinned "**AgentOps-3c
  should create synthetic benchmark
  resumes only, not run report
  generation**" and "**3c is
  strictly separate from 3d, not
  bundled**".
- 5 fixture identities pinned in
  AgentOps-3a §7.2: A senior
  backend → Applied AI · B
  full-stack → AI Product Engineer
  · C data eng → LLM Infra · D
  ML-adjacent → Agent Engineer ·
  E enterprise SWE → AI
  transition.
- **Product state**: Candidate 1
  stream sentinel + Candidate 4
  empty-PDF gate live on
  `origin/main` at `63e8979`.
  Real-report entrance
  reliability is materially
  stronger. AgentOps-3a/3b memos
  pushed. Codex planner
  specified but not
  implemented.

## Allowed files

- `.agent/tasks/2026-07-12_run_01_TASK.md`
  (this file)
- `.agent/regression_fixtures/README.md`
- `.agent/regression_fixtures/benchmark_A_backend_to_applied_ai.md`
- `.agent/regression_fixtures/benchmark_B_fullstack_to_ai_product.md`
- `.agent/regression_fixtures/benchmark_C_data_to_llm_infra.md`
- `.agent/regression_fixtures/benchmark_D_ml_adjacent_to_agent_engineer.md`
- `.agent/regression_fixtures/benchmark_E_enterprise_to_ai_transition.md`
- `.agent/run_reports/2026-07-12_run_01_RUN_REPORT.md`

## Forbidden files

- **`src/**` any file**
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `package.json`
- `package-lock.json`
- `.github/workflows/**`
- `.agent/scripts/**` — hard rule.
- `.agent/policies/**`
- `.agent/templates/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/planner_reports/**`
- **Pipeline repo any file**.
- `sources.yaml` (pipeline)
- `corpus/**` (pipeline)
- `scripts/collector/**` (pipeline)
- `.env*`
- `vercel.json` / `.vercel/**`
- Codex CLI / Claude Code config
- Any planner / runner / daemon /
  scheduler / Playwright / OpenAI
  API code.

## Risk level

`green`

Markdown-only fixture files.
Zero runtime impact. No LLM call.
No push. No deploy. Same class as
docs / daily summaries.

## Constraints

- **No LLM / API call.**
- **No report generation.**
- **No Playwright harness
  invocation.**
- **No planner implementation.**
- No `.agent/scripts/**` edit.
- No `src/**` edit.
- No pipeline edit.
- No `.env*` access.
- No new npm dependency.
- No push, no manual deploy.
- Each fixture file **must be
  fully synthetic** — no real
  names / addresses / phone
  numbers / emails / private
  personal information.
- Fixtures must not reference
  Bohao or any real user.
- Companies must be synthetic
  (e.g. "Northstar Systems",
  "Atlas Retail Cloud",
  "Meridian DataWorks", etc.).
- Each fixture resume text
  should be **700-1100 words**
  when the "Resume input"
  section is measured.
- All 5 fixture files must
  have all required headings
  from the user's spec (§7 of
  the user message).
- Each fixture must contain
  `synthetic: true` in its
  metadata block.

## Acceptance criteria

- [ ] `.agent/regression_fixtures/`
      directory exists.
- [ ] `.agent/regression_fixtures/README.md`
      exists with all 7 required
      sections (Purpose, Privacy
      and safety, Fixture list,
      How future harness should
      use these fixtures, What
      these fixtures are not,
      Versioning policy, Future
      expansion).
- [ ] All 5 fixture files A-E
      exist at the pinned paths.
- [ ] Each fixture has 8-field
      Metadata (`fixture_id`,
      `version`, `synthetic:
      true`, `target_transition`,
      `target_role`,
      `intended_difficulty`,
      `primary_gap_theme`,
      `last_updated`).
- [ ] Each fixture contains
      `## Target role input`,
      `## Resume input`,
      `## Expected strengths`,
      `## Expected gaps`,
      `## Expected high-leverage
      next action`,
      `## Regression notes`,
      `## Must not happen`.
- [ ] Each fixture's Resume
      input body is between 700
      and 1100 words.
- [ ] Each fixture's Expected
      strengths has 4-6 items.
- [ ] Each fixture's Expected
      gaps has 4-6 items.
- [ ] All 5 fixtures use
      synthetic company names
      only.
- [ ] No real emails, phone
      numbers, addresses, or
      personal identifiers in
      any fixture.
- [ ] No fixture mentions
      Bohao or any real user.
- [ ] No fixture contains
      generated report content,
      screenshots, or LLM
      output.
- [ ] `grep -R "synthetic: true"
      .agent/regression_fixtures`
      returns 5 hits.
- [ ] `grep -R "## Resume input"
      .agent/regression_fixtures`
      returns 5 hits.
- [ ] `grep -R "## Target role
      input"
      .agent/regression_fixtures`
      returns 5 hits.
- [ ] `grep -R "## Must not
      happen"
      .agent/regression_fixtures`
      returns 5 hits.
- [ ] No `src/**` files
      modified.
- [ ] No `src/data/**` files
      modified.
- [ ] No prompt / model / API
      files modified.
- [ ] No pipeline repo
      modification (HEAD
      `b019786` at start AND
      end).
- [ ] No `.agent/scripts/**`
      diff.
- [ ] No `.agent/planner_reports/**`
      diff.
- [ ] No collector
      invocation.
- [ ] No LLM call.
- [ ] No new npm dependency.
- [ ] No push, no manual
      deploy.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# structural check
ls .agent/regression_fixtures/
wc -w .agent/regression_fixtures/*.md   # per-file word count

# grep checks
grep -R "synthetic: true" .agent/regression_fixtures | wc -l  # expect 5
grep -R "## Resume input" .agent/regression_fixtures | wc -l  # expect 5
grep -R "## Target role input" .agent/regression_fixtures | wc -l  # expect 5
grep -R "## Must not happen" .agent/regression_fixtures | wc -l  # expect 5

# diff audit
git status
git diff --stat
git diff --name-only

# forbidden audit
git diff --name-only origin/main..HEAD | grep -E "src/|.agent/scripts/|.agent/policies/|.agent/templates/|.agent/blockers.md|.agent/automation_queue.md|.agent/planner_reports/|.github/workflows/|package|vercel|.env" || echo "  ✓ CLEAN (forbidden empty)"

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git log --oneline -3
```

## Expected output

RUN_REPORT must include:

- Fixture directory path.
- Files created (list).
- Summary of each benchmark A-E
  (persona + target + primary gap
  theme).
- Why each fixture is useful for
  future report regression.
- Privacy/safety statement.
- Validation results (word counts
  + grep hit counts).
- Forbidden-file audit.
- Confirmation: no generated
  reports, no screenshots, no
  harness, no LLM / API call, no
  planner implementation, no
  `src/**`, no `.agent/scripts/**`,
  no pipeline, no push/deploy.
- Recommendation: Human +
  ChatGPT review, then DECISION.

## Rollback plan

Both commits are local-only until
push.

- Small correction → follow-up
  commit.
- Full reject → `git reset --soft
  HEAD~2`; remove fixture
  directory if desired.
- After push, needs revert →
  `git revert <impl_commit>
  <run_report_commit>`.

## Requires human approval

`no` for the fixture landing.

> Approved by:
> `.agent/decisions/2026-07-11_run_01_DECISION.md`
> (AgentOps-3b DECISION named 3c
> as default next code loop).
> Bohao's message 2026-07-12
> "Start AgentOps-3c" reconfirmed.
>
> Push and deploy remain separate
> human-approval gates.

## Max runtime minutes

60

## Max token budget hint

`unset`

## Stop conditions

- Stop if any file in `src/**`
  or `.agent/scripts/**`
  appears in `git status`.
- Stop if any pipeline-repo file
  appears.
- Stop if `.agent/planner_reports/**`
  appears.
- Stop if any real personal data
  slips into a fixture.
- Stop if `npm run *` is
  invoked (task requires no
  runtime).
- Stop if `package.json` /
  lockfile appears.
- Stop if any LLM / API call
  is attempted.
