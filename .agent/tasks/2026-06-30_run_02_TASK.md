# TASK · P1.7b sync hero mock numbers with web bundle

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-06-30_run_02`
- **date**: `2026-06-30`
- **run_number**: `02` (zero-padded sequence within the day)
- **title**: P1.7b sync hero mock numbers with web bundle
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only;
  pipeline repo is read-only sanity)

## Objective

Replace the hardcoded / mock hero numbers currently displayed on
`src/app/page.tsx` with values derived from `src/data/web_bundle.json`
(via a tiny display-only helper) so the landing page's grounding
claims match the actual on-disk corpus. Preserve the polished
"live report" feel; do NOT change model selection, prompts, or
any runtime AI logic.

**Observed facts** (from a preflight `python3 json.load` of
`src/data/web_bundle.json`, read-only):

- `n_records`: **443** (matches the `443` already on line 849)
- `unique_companies`: **35** (new, credible)
- `applied_ai` archetype count: **47** — the hero's current
  `92 applied_ai JDs` claim is **stale** (was possibly true in
  an earlier corpus snapshot, but the current bundle only has
  47 applied_ai records)
- `generated_at`: `2026-05-14T02:17:04+00:00` (~6 weeks old;
  present but not useful as a "last updated" claim)

Primary fix: `92 → 47` for the `applied_ai JDs` count in the two
places it appears (hero fit-signal strip + attribution strip).
Optional secondary polish: wire the already-correct `443` and
`5 evidence quotes` through the helper for grep-friendly future
refreshes, and optionally surface the `35 tracked companies` count
in one non-intrusive spot.

## Background

- P1 (`2026-06-27_run_01`) introduced the hero and its mock
  numbers with the intent to sync them later.
- QUEUE-0003 P1.7b (in `.agent/automation_queue.md`) named this
  task as a candidate; the AgentOps-2c DECISION Q10 recommended
  returning to product work with P1.7b as the default next
  task.
- Constraint: `src/app/page.tsx` is a client component
  (`"use client"` on line 1). Direct `import bundleJson from
  "@/data/web_bundle.json"` from a client component would ship
  the 1.4 MB JSON to the browser bundle. Existing
  `src/lib/corpus.ts` does exactly that but is NOT imported by
  the client page — server-only consumers use it. Therefore
  the display helper this TASK creates MUST NOT import the
  bundle at all — it exports **primitive constants**
  pre-computed at authoring time from the bundle, with a
  clear source-attribution comment so future refreshes are
  a small grep-and-edit.
- Precedent: `src/lib/corpus.ts` shows the same numbers exist
  in structured form; the new helper's constants trace back
  to that same source.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-06-30_run_02_TASK.md` (this file)
- `.agent/run_reports/2026-06-30_run_02_RUN_REPORT.md` (NEW;
  matching RUN_REPORT)
- **New helper**: `src/lib/web-bundle-stats.ts` (NEW,
  display-only primitive constants; ≤50 lines; no bundle
  import; no runtime logic; no API call).
- `src/app/page.tsx` (edit ONLY the specific lines that use
  `92`, `443`, and `5 evidence quotes` in the hero mock preview
  block. Do NOT rewrite unrelated sections. Do NOT touch the
  streaming/report body markdown components or any AI-invocation
  code paths.)
- `src/data/web_bundle.json` — **read-only reference**. Do NOT
  edit. This TASK reads it only for stat computation; the on-disk
  JSON is left untouched.

## Forbidden files

### Web repo

- `src/lib/anthropic.ts` — does not exist in this repo (per
  `ls src/lib/`), but the constraint is stated for clarity in
  case it's referenced by convention.
- `src/lib/prompts.ts` — untouched. Prompts are frozen.
- `src/lib/corpus.ts` — untouched. Server-only corpus logic is
  out of scope for this UI fix.
- `src/lib/types.ts` — untouched (no type additions needed).
- `src/lib/eval-report.ts`, `src/lib/extract-pdf.ts` —
  untouched. Not related to hero display.
- `src/data/web_bundle.json` — read-only.
- `src/data/web_bundle_pipeline.json` — untouched.
- `src/app/api/**` — untouched. No API changes.
- `src/app/lab/**`, `src/app/methodology/**`,
  `src/app/sample-report/**`, `src/app/snapshot-pipeline/**` —
  untouched. This TASK is landing-page only.
- `src/components/**` — untouched (no component changes
  needed; the hero block is inline in `page.tsx`).
- `package.json` / `package-lock.json` — untouched (no new
  dependencies).
- `.env*` — untouched.
- `.github/workflows/**` — untouched.
- `vercel.json` / `.vercel/**` — untouched.
- `.agent/policies/**` — untouched (policy at v1.1 stays).
- `.agent/templates/**` — untouched.
- `.agent/scripts/**` — untouched (**hard rule** per Q3-Q8 of
  the AgentOps-2c DECISION).
- `.agent/automation_queue.md` — untouched (queue transitions
  are B-time; if a status update is useful, do it in a
  separate cleanup commit).
- `.agent/blockers.md` — untouched.

### Pipeline repo

- **All files**. Pipeline read-only. At most one read-only
  `git status` / `git log` / `git rev-parse HEAD` check.

### Process-level

- No new GitHub Actions / workflow / cron job.
- No new deployment hooks.
- No new npm / Python dependencies.
- No OpenAI API SDK / key / `.env` entry / HTTP call
  (per Q7 blocked sense).
- No Codex CLI or Claude Code config mutation.
- No `python -m scripts.collector …` invocation (no pipeline
  collector run).
- No `npm run collect` or similar corpus regeneration.
- No LLM call by this task.
- No force push / history rewrite.
- No `git push`.
- No manual `vercel deploy`.
- No automation runner / daemon / scheduler / cron file
  creation.

## Risk level

`yellow`

> User-visible UI change. Scope narrow (2 lines of numbers in
> the hero + possibly 1 optional "tracked companies" mention).
> No prompt / model / API / pipeline touched. Same risk class
> as any small landing-page copy fix.

## Constraints

- No changes to `src/lib/anthropic.ts` (if it existed; not
  present in this repo).
- No changes to `src/lib/prompts.ts` (model prompts frozen).
- No changes to `src/lib/corpus.ts` (server-only corpus logic
  frozen).
- No changes to `src/data/web_bundle.json` or any pipeline
  file.
- No changes to `.agent/scripts/**` (hard rule per Q3-Q8).
- No new npm / Python dependency.
- No `python -m scripts.collector …` / `npm run collect` /
  LLM call.
- No `git push`. No `vercel deploy`.
- The new helper `src/lib/web-bundle-stats.ts` MUST NOT
  import `web_bundle.json` (client-bundle bloat risk). It
  exports pre-computed primitive constants with a JSDoc
  comment referencing the on-disk source and the
  authoring-time computation.
- Edits to `page.tsx` MUST be surgical: touch only lines
  currently displaying the hardcoded numbers being replaced.
  No unrelated refactor. No component extraction. No copy
  rewrite beyond what's needed to reflect the corrected
  numbers.
- Optional "tracked companies" mention MAY be added if it
  fits the existing hero attribution strip without disrupting
  the layout; otherwise skip.
- Copy must remain factual / conservative. Do NOT invent
  metrics. Do NOT use marketing-inflated language.
- If a number is not safely derivable from the bundle, use a
  conservative label like "tracked companies" / "current
  snapshot" instead.

## Acceptance criteria

- [ ] `src/lib/web-bundle-stats.ts` exists, ≤50 lines, exports
      primitive constants (`totalJds`, `appliedAiJds`,
      `trackedCompanies`, plus `evidenceQuotesPerReport` if
      wired), with a JSDoc comment naming
      `src/data/web_bundle.json` as the source + authoring
      timestamp.
- [ ] Helper does NOT `import` from `@/data/web_bundle.json` or
      transitively from `@/lib/corpus`.
- [ ] `page.tsx` line 764 `92` → uses `appliedAiJds` (renders
      as `47`).
- [ ] `page.tsx` line 822 `92` → uses `appliedAiJds` (renders
      as `47`).
- [ ] `page.tsx` line 824 `5` (evidence quotes) → uses
      `evidenceQuotesPerReport` if the helper exports it;
      otherwise stays literal (fixed policy, not a bundle
      stat).
- [ ] `page.tsx` line 849 `443` → uses `totalJds` (renders as
      `443`; grep-friendly future refresh).
- [ ] Optional: `trackedCompanies` (35) surfaced in exactly
      ONE non-intrusive spot, or skipped if it doesn't fit.
- [ ] `src/lib/prompts.ts`, `src/lib/corpus.ts`,
      `src/lib/types.ts` untouched.
- [ ] `src/data/web_bundle.json` untouched.
- [ ] `package.json` / `package-lock.json` untouched.
- [ ] `npm run lint` passes (or reports only pre-existing
      warnings, unchanged from baseline).
- [ ] `npm run build` passes (Next.js production build
      succeeds; TypeScript clean; no new bundle-size
      regression from client bundle importing the JSON —
      helper is primitive-constant only).
- [ ] `git diff --stat` shows changes only under
      `.agent/tasks/`, `.agent/run_reports/`, `src/lib/`
      (new helper file only), and `src/app/page.tsx`.
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created.
- [ ] Pipeline HEAD = `b019786` unchanged.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check
git status --short
git diff --stat

# helper structural check
wc -l src/lib/web-bundle-stats.ts
grep -c 'import.*web_bundle.json' src/lib/web-bundle-stats.ts   # must be 0
grep -c 'import.*corpus' src/lib/web-bundle-stats.ts            # must be 0

# hero change spot-check
grep -n '>92<\|"92"\| 92 applied_ai' src/app/page.tsx           # must be 0
grep -n 'appliedAiJds\|totalJds\|trackedCompanies' src/app/page.tsx

# forbidden audit
git diff --stat HEAD -- \
    src/lib/prompts.ts src/lib/corpus.ts src/lib/types.ts \
    src/data/ package.json package-lock.json \
    .agent/policies/ .agent/templates/ .agent/scripts/ \
    .agent/blockers.md .agent/automation_queue.md \
    .github/workflows/ vercel.json .vercel/ 2>&1 || true

# lint + build
npm run lint 2>&1 | tail -20
npm run build 2>&1 | tail -30

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

- Exact path of `src/lib/web-bundle-stats.ts` (NEW, ≤50 lines).
- Exact commit hashes: TASK+impl commit + RUN_REPORT commit.
- `npm run lint` result (pass / warnings-only-unchanged-from-baseline).
- `npm run build` result (pass, with route count matching prior
  builds; no new "External" warning about the JSON in the
  client bundle).
- Confirmation that pipeline HEAD = `b019786` at start and end.

## Rollback plan

- Yellow product-code task. Same direct-on-`main` pattern the
  prior product loops used (P1.7 at `2026-06-27_run_01`).
- If DECISION requests changes: edit in a follow-up commit;
  do not amend.
- If full revert: `git revert <run_report_commit> <impl_commit>`.
  Never `git reset --hard` on pushed commits.
- The pre-push state means a pre-push revert is just
  `git reset --keep HEAD~2`, but use `git revert` for the
  audit-trail consistency the AgentOps loops established.

## Requires human approval

`no`

> Yellow small UI-copy change, no red-zone file. Push (after
> the matching DECISION) is a separate human approval per
> global policy.

## Max runtime minutes

`45` — one hero fix + one small helper + build.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

- Stop if any path under `src/lib/prompts.ts`,
  `src/lib/corpus.ts`, `src/lib/anthropic.ts` (if it existed),
  `src/data/web_bundle.json`, `src/data/web_bundle_pipeline.json`,
  `package*.json`, `.env*`, `vercel.json`, `.vercel/`,
  `.github/workflows/`, `.agent/scripts/`, `.agent/policies/`,
  `.agent/templates/`, `.agent/blockers.md`, or any
  pipeline-repo file appears in `git status`.
- Stop if `npm run build` fails.
- Stop if `npm run lint` introduces NEW errors (baseline
  warnings are OK).
- Stop if the client bundle size grows meaningfully (would
  indicate the JSON leaked in).
- Stop if the new helper imports `web_bundle.json` or any
  server-only module.
- Stop if more than 4 files would be committed in the impl
  commit (allowed: 1 TASK + 1 helper + 1 page.tsx + 1
  RUN_REPORT max).
- Stop if a DECISION file appears in `git status`.
