# TASK · Add Executive Digest to automation report template

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-06-29_run_02`
- **date**: `2026-06-29`
- **run_number**: `02` (zero-padded sequence within the day)
- **title**: Add Executive Digest to automation report template
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only;
  pipeline repo is read-only sanity)

## Objective

Amend `.agent/templates/automation_window_report_template.md` to add
a compact **Executive Digest** section near the top so every future
automation window report opens with a ≤10-line digest designed for
fast Human + ChatGPT Non-Automation Time review. Also amend
`.agent/policies/automation_policy.md` §7 with a one-line note
making the digest a required field. The longer Executive summary and
all 19 existing template sections stay intact.

This is purely a template + policy contract update. No runner /
daemon / scheduler / cron is created. No real automation runs. The
2026-06-29 MANUAL_DRY_RUN report does not get retroactively
updated — the new digest section applies to future reports only.

## Background

- The MANUAL_DRY_RUN report at
  `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md`
  shipped on `origin/main` at `475b116` and was reviewed in DECISION
  `2026-06-29_run_01_DECISION.md` (verdict `approve`, commit
  `475b116`). The DECISION enumerated **7 risks**; risk #1 was that
  the 448-line report is useful as an audit artifact but slow for a
  30-second Non-Automation Time skim.
- Non-blocking follow-up #3 in that DECISION was: *"Add a note to
  future automation reports requiring a 10-line Executive Digest at
  the top."* This task is the implementation of that follow-up.
- Today's daily summary
  (`.agent/daily_summaries/2026-06-29_SUMMARY.md`, in `95e8f0f`)
  explicitly lists the Executive Digest amendment as recommended
  next-day task #1, ahead of the AgentOps-2b design memo.
- `.agent/policies/automation_policy.md` §7 currently lists the
  required fields every per-window report must include; extending
  that list to include the Executive Digest keeps the policy and
  template in lockstep. Policy is otherwise frozen — this is the
  smallest valid mutation: a single sentence.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK. If a path is not on this
> list AND not on `forbidden_files`, executor must ask before editing.

- `.agent/tasks/2026-06-29_run_02_TASK.md` (this file)
- `.agent/templates/automation_window_report_template.md` (add
  Executive Digest section; do NOT remove or reorder existing
  sections)
- `.agent/policies/automation_policy.md` (one-line addition in §7
  ONLY; do NOT touch any other section)
- `.agent/run_reports/2026-06-29_run_02_RUN_REPORT.md` (NEW; matching
  RUN_REPORT for this TASK)
- `.agent/automation_queue.md` — only if a new queue item is
  appropriate to record the follow-up (e.g. "after digest is live,
  consider MANUAL_DRY_RUN-2 to validate the new digest section");
  optional, not required.

## Forbidden files

> Paths the executor MUST NOT edit, even if it seems useful. Glob patterns OK.

### Web repo

- `src/**` (all app code)
- `src/lib/prompts.ts`
- `src/lib/anthropic.ts`
- `src/data/web_bundle.json`
- `package.json`
- `package-lock.json`
- `.env*`
- `.github/workflows/**`
- `vercel.json`
- `.vercel/**`
- `.agent/policies/agent_policy.md` (red-zone policy; not in scope)
- `.agent/policies/automation_policy.md` sections other than §7
  (everything else in this file is frozen for this task)
- `.agent/templates/**` other than
  `automation_window_report_template.md` (TASK / RUN_REPORT /
  DECISION / daily-summary templates are frozen here)
- `.agent/README.md` (no link edits needed for a sub-section
  addition)
- `.agent/decisions/**` (DECISION is a downstream step; not
  authored in this TASK)
- `.agent/automation_runs/**` (existing MANUAL_DRY_RUN report is
  frozen by design — future reports use the new template)
- `.agent/daily_summaries/**` (today's already shipped; future
  daily summaries are unrelated)
- `.agent/scripts/**` (helper scripts unchanged; only used to
  scaffold)
- `.claude/**`, `~/.claude/**`, `~/.codex/**` (Codex/Claude config)

### Pipeline repo

- **All files**. Pipeline is read-only for this task. At most one
  read-only `git status` / `git log` / `git rev-parse HEAD` check
  is allowed. No `cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar`
  for write purposes.

### Process-level

- No new GitHub Actions / workflow / cron job
- No new deployment hooks
- No new npm dependencies
- No new Python dependencies
- No OpenAI API SDK / key / `.env` entry / HTTP call
- No `python -m scripts.collector …` invocation
- No `npm run …` invocation (no build needed)
- No LLM call by this task
- No Codex CLI config or Claude Code config mutation
- No automation runner / daemon / scheduler / cron file creation
- No force push / history rewrite / destructive reset
- No `git push`
- No manual `vercel deploy`

## Risk level

`yellow`

> Per `.agent/policies/automation_policy.md` §6, policy edits
> (`.agent/policies/**`) escalate to yellow even if green-shaped
> elsewhere. Because §7 gets a one-line addition, this task is
> yellow rather than green. Template-only would have been green;
> the policy touch bumps it.

## Constraints

> Hard rules for this specific task. Anything beyond the global policy.

- No automation runner / daemon / scheduler / cron is created.
- No GitHub Actions edit.
- No Codex CLI or Claude Code config edit.
- No OpenAI API SDK / key / `.env` entry / HTTP call introduced.
- No new dependency.
- No `python -m scripts.collector …` invocation.
- No `npm run …` invocation.
- No LLM call performed by this task.
- No prompt / classifier / extractor / schema / model selection
  edits.
- No deploy.
- No `git push`.
- No pipeline-repo file edited.
- The existing MANUAL_DRY_RUN report at
  `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md` is
  NOT modified retroactively. The new Executive Digest section
  applies to future automation reports only.
- The 19 existing H2 sections of
  `automation_window_report_template.md` MUST all remain present.
  Section order MUST be preserved. Only addition allowed: insert
  the new Executive Digest section between "Metadata" and
  "Executive summary".
- The policy `automation_policy.md` edit MUST be confined to §7
  (Automation window report policy). No other §, no Change Control
  block edit beyond a single-line dated note ("2026-06-29: §7
  extended to require Executive Digest as a top section").
- No queue red item or blocker is touched. QUEUE-0001 still `done`,
  QUEUE-0006 still `done`, QUEUE-0002 still `blocked_pending_human`.
  BLK-0001 / BLK-0002 / BLK-0003 stay `open`.
- This task does NOT start AgentOps-2b runner work in any form,
  not even a design memo. AgentOps-2b remains a separate downstream
  TASK.

## Acceptance criteria

> Checklist that the RUN_REPORT must demonstrate as satisfied.

- [ ] `automation_window_report_template.md` includes a new H2
      section exactly named `## Executive Digest`.
- [ ] The Executive Digest is positioned **between Metadata and
      Executive summary** (i.e. immediately after the Metadata
      block and before the existing "Executive summary" section).
- [ ] The Executive Digest is designed to fit in ≤10 numbered
      bullet lines (one bullet per field). 10 specific fields:
      window verdict, main outcome, tasks completed, commits
      created, repos touched, validation status, red-zone /
      forbidden audit, open blockers, human decisions needed,
      safest next action.
- [ ] The Executive Digest section explicitly states it is intended
      for fast Human + ChatGPT review (Non-Automation Time skim).
- [ ] The longer Executive summary section is preserved verbatim
      (no rewording).
- [ ] All other 18 existing H2 sections (Metadata, Goals selected
      by Codex, Tasks attempted, Tasks completed, Commits created,
      Files changed, Validation results, Claude Code usage summary,
      Codex review summary, Red-zone encounters, Blockers created
      or updated, Failed validations, Merge / push / deploy
      status, Human decisions requested, Suggested ChatGPT review
      questions, Next recommended automation tasks, Safety audit,
      Final status) are preserved verbatim, in the same order.
- [ ] `automation_policy.md` §7 (Automation window report policy)
      includes a one-line addition naming Executive Digest as a
      required top section. No other policy section edited.
- [ ] `git diff --stat` shows changes only under `.agent/templates/`
      and `.agent/policies/` and `.agent/tasks/` (TASK file) and
      `.agent/run_reports/` (RUN_REPORT file). No other path.
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created in this task (DECISION is a separate
      downstream step).
- [ ] No retroactive edit to
      `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md`.

## Validation commands

> Exact commands the executor should run to validate. Copy-paste-able.

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check — must show only allowed paths
git status --short
git diff --stat

# template structural check — confirm 20 H2 sections, Executive Digest present
grep -c '^## ' .agent/templates/automation_window_report_template.md
grep -n '^## ' .agent/templates/automation_window_report_template.md

# policy structural check — confirm §7 mentions Executive Digest
grep -n 'Executive [Dd]igest' .agent/policies/automation_policy.md

# forbidden audit — all of these must show no diff
git diff --stat HEAD -- src/ src/lib/prompts.ts src/lib/anthropic.ts \
    src/data/web_bundle.json package.json package-lock.json \
    .github/workflows/ vercel.json .vercel/ \
    .agent/templates/task_template.md \
    .agent/templates/run_report_template.md \
    .agent/templates/decision_template.md \
    .agent/templates/daily_summary_template.md \
    .agent/policies/agent_policy.md \
    .agent/README.md \
    .agent/automation_runs/ \
    .agent/decisions/ \
    .agent/daily_summaries/ \
    .agent/scripts/ 2>&1 || true

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

Beyond the standard RUN_REPORT template:

- Exact paths of all files touched (TASK + template + policy +
  optionally queue + RUN_REPORT).
- Exact commit hashes for the combined "TASK + impl" commit and the
  RUN_REPORT commit.
- `grep -c '^## '` on the template = **20** post-edit (was 19).
- `grep -n '^## '` on the template confirms `## Executive Digest`
  appears immediately after `## Metadata` and before
  `## Executive summary`.
- `grep -n 'Executive [Dd]igest'` on the policy returns ≥1 match
  inside §7.
- Confirmation that pipeline `git rev-parse HEAD` is unchanged at
  `b019786` between TASK start and RUN_REPORT commit.

## Rollback plan

> How to undo this task if the DECISION is `request_changes` or `stop`.

- Yellow `.agent/` doc task → direct on `main` (same pattern as
  AgentOps-2a's policy commits and QUEUE-0006's MANUAL_DRY_RUN
  commit). No branch cut.
- If the DECISION requests changes: edit the digest in a follow-up
  commit; do not amend the original commits.
- If a full revert is requested: `git revert <run_report_commit>
  <impl_commit>` — keeps the audit trail; never `git reset --hard`
  on pushed commits. Note: at the point of any revert, the
  template+policy commits will NOT have been pushed yet (push is a
  separate downstream human approval), so a pre-push revert is just
  `git reset --keep HEAD~2` on the local branch — but use revert
  even pre-push for consistency.
- The existing MANUAL_DRY_RUN report at
  `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md` is
  not affected by any rollback.

## Requires human approval

`no`

> Yellow `.agent/`-only doc work, but no red-zone file. Push to
> `main` (after the matching DECISION) is still a separate human
> approval per global policy. The TASK itself does not require
> pre-approval.

## Max runtime minutes

`30` — small template + policy edit + RUN_REPORT authoring.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

> Beyond the global policy (`.agent/policies/agent_policy.md` §3 and
> `.agent/policies/automation_policy.md` §11), task-specific stop
> conditions.

- Stop immediately if any path under `src/`, `package*.json`,
  `.env*`, `vercel.json`, `.vercel/`, `.github/workflows/`, or any
  pipeline-repo file appears in `git status`.
- Stop if `grep -c '^## '` on the template is anything other than
  20 after the edit (means a section was accidentally dropped or
  the digest was added twice).
- Stop if the existing Executive summary section content has been
  reworded.
- Stop if the policy edit lands outside §7.
- Stop if `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md`
  appears in `git status` (no retroactive edit allowed).
- Stop if more than 4 files would be committed across this task
  (allowed: 1 TASK + 1 template + 1 policy + 1 RUN_REPORT, plus
  optionally 1 queue update = max 5 if queue added; if scope grows
  past that, escalate).
- Stop if a DECISION file appears in `git status` — DECISION is a
  separate downstream task.
