# TASK · P1.7c model-string single source of truth

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-05_run_01`
- **date**: `2026-07-05`
- **run_number**: `01` (zero-padded sequence within the day)
- **title**: P1.7c model-string single source of truth
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only;
  pipeline repo is read-only sanity)

## Objective

Centralize the **user-visible display-only** model / provider
strings currently hardcoded across the web UI into a small new
helper `src/lib/models-display.ts`, so future model-name updates
happen in one place. Same shape as P1.7b's
`src/lib/web-bundle-stats.ts`: primitive constants only, no
imports of API clients / prompt files / runtime-selection code,
no API call. This is a **display/copy** consolidation, NOT a
runtime-behavior change.

**Observed hardcoded model-version strings**
(preflight `grep` on `src/`):

- `src/app/page.tsx:1067-1068` — "Anthropic's API (Claude
  **Sonnet 4.6** for generation, **Haiku 4.5** if you run the
  eval). Nothing else."
- `src/app/page.tsx:1122` — "Two LLM calls (**Sonnet 4.6** +
  **Haiku 4.5**)"
- `src/app/page.tsx:1639` — "Claude **Sonnet 4.6** (generation)
  · **Haiku 4.5** (eval) · Single-shot, evidence-grounded."
- `src/app/lab/page.tsx:245` — "3-metric LLM-as-judge (Claude
  **Haiku 4.5**) over each"

**Explicitly out of scope** (all remain untouched):

- SEO / metadata keywords in `src/app/layout.tsx` (lines
  30-35: array of terms "LLM", "Claude", "Anthropic",
  "OpenAI"). These are search-keyword strings, not
  user-visible model-version labels.
- Rate-limit error copy in `src/app/page.tsx:371`
  ("... set by Anthropic, not the app.") — provider-level
  reference, not a versioned model claim.
- Mock hero preview artifact chip `Anthropic SDK` at
  `src/app/page.tsx:815` — example content, not a real
  model-string.
- All other mentions of "Anthropic" / "Claude" / "OpenAI" in
  archetype blurbs, JD content, methodology description,
  lab-page corpus commentary — those are content/domain
  material, not runtime-model claims.
- Runtime model-selection code lives conceptually in
  `src/app/api/**` (server) and possibly wherever the
  Anthropic SDK is instantiated; this TASK **does not** read
  or modify any of that.
- `src/lib/prompts.ts` — untouched (prompts frozen).
- `src/lib/anthropic.ts` — does not exist in this repo
  (verified in P1.7b); constraint trivially satisfied.

## Background

- P1.7 (`2026-06-27_run_01`) introduced the hero and the
  "how it works" panel with hardcoded `Sonnet 4.6` /
  `Haiku 4.5` labels.
- QUEUE-0004 P1.7c (in `.agent/automation_queue.md`) has
  named this task since the AgentOps-2c cleanup; the P1.7b
  DECISION recommended it as the next product task per Q10.
- Precedent: P1.7b (`1c912f8`) created
  `src/lib/web-bundle-stats.ts` — 29-line primitive-constants
  helper with a JSDoc source-attribution comment. This TASK
  follows the same pattern for model-display strings.
- Rule (from Q7 + P1.7b): the helper does NOT import runtime
  code and does NOT change runtime behavior. It is a
  display-only central point that lets the UI say the same
  words consistently.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-07-05_run_01_TASK.md` (this file)
- `.agent/run_reports/2026-07-05_run_01_RUN_REPORT.md` (NEW;
  matching RUN_REPORT)
- **New helper**: `src/lib/models-display.ts` (NEW,
  display-only primitive constants; ≤50 lines; no API
  client / prompts / runtime-selection imports; no API call).
- `src/app/page.tsx` (edit ONLY the specific lines with
  "Sonnet 4.6" / "Haiku 4.5" model-version claims. Do NOT
  touch archetype blurbs, JD example content, rate-limit
  error copy, or the mock hero preview chip.)
- `src/app/lab/page.tsx` (edit ONLY line 245's model-judge
  string. Do NOT touch corpus commentary or example JD
  content.)

## Forbidden files

### Web repo

- **`src/lib/prompts.ts`** — untouched. Prompts frozen.
- **`src/lib/anthropic.ts`** — does not exist in this repo;
  constraint trivially satisfied. If it were to appear, do
  NOT create or edit it in this task.
- **`src/app/api/**`** — untouched. API routes (
  `classify`, `companies`, `eval-report`, `extract-pdf`,
  `generate-report`) contain runtime model selection and
  invocation logic; those stay frozen.
- **`src/lib/corpus.ts`** — untouched. Server-only corpus
  logic frozen.
- **`src/lib/types.ts`** — untouched (no type additions
  needed; helper uses inline `as const`).
- **`src/lib/eval-report.ts`, `src/lib/extract-pdf.ts`,
  `src/lib/web-bundle-stats.ts`** — untouched (unrelated to
  model strings; frozen for this task).
- **`src/data/**`** — untouched. `web_bundle.json` and
  `web_bundle_pipeline.json` are frozen; corpus content
  stays.
- **`src/app/layout.tsx`** — untouched. The SEO-keyword
  array on lines 30-35 is out of scope; changing keywords
  is not a model-display SSOT concern.
- **`src/app/opengraph-image.tsx`** — untouched. OG image
  reflects marketing copy; separate concern.
- **`src/app/methodology/page.tsx`** — untouched. Contains
  corpus-methodology commentary, no runtime-model claims.
- **`src/app/sample-report/page.tsx`** — untouched. Contains
  example report content (no versioned model claims;
  mentions "Anthropic" only in JD example blurbs).
- **`src/app/snapshot-pipeline/page.tsx`** — untouched.
- **`src/components/**`** — untouched (no component
  changes needed).
- **`package.json` / `package-lock.json`** — untouched (no
  new deps).
- **`.env*`** — untouched.
- **`.github/workflows/**`** — untouched.
- **`vercel.json` / `.vercel/**`** — untouched.
- **`.agent/policies/**`** — untouched (policy at v1.1 stays).
- **`.agent/templates/**`** — untouched.
- **`.agent/scripts/**`** — untouched. **Hard rule** per
  Q3-Q8 of AgentOps-2c DECISION.
- **`.agent/automation_queue.md`** — untouched (queue
  transitions are B-time).
- **`.agent/blockers.md`** — untouched.

### Pipeline repo

- **All files**. Pipeline read-only. At most one read-only
  `git status` / `git log` / `git rev-parse HEAD` check.

### Process-level

- No new GitHub Actions / workflow / cron job.
- No new deployment hooks.
- No new npm / Python dependencies.
- No OpenAI API SDK / key / `.env` entry / HTTP call
  (per Q7 blocked sense).
- No Codex CLI or Claude Code config mutation.
- No `python -m scripts.collector …` invocation.
- No `npm run collect` or similar corpus regeneration.
- No LLM call by this task.
- No force push / history rewrite.
- No `git push`.
- No manual `vercel deploy`.
- No automation runner / daemon / scheduler / cron file
  creation.

## Risk level

`yellow`

> User-visible product copy change. Scope narrow (4 lines of
> UI copy across 2 files + 1 new small helper). Same risk
> class as P1.7b.

## Constraints

- No model / prompt / runtime-selection edits.
- No `src/lib/prompts.ts` / `src/lib/corpus.ts` /
  `src/lib/types.ts` / `src/app/api/**` changes.
- No `src/data/**` changes.
- No `.agent/scripts/**` changes (**hard rule**).
- No new dependency (npm or Python).
- No `python -m scripts.collector …` / `npm run collect` /
  LLM call.
- No `git push`. No `vercel deploy`.
- The new helper `src/lib/models-display.ts` MUST export
  primitive constants only — no import from
  `@anthropic-ai/sdk` (or any AI SDK), no import from
  `@/lib/prompts`, no import from anything under
  `@/app/api/**`, no fetch call, no side effect at
  module scope beyond the `as const` object.
- Copy MUST remain factually correct at authoring time.
  The current runtime models really are Claude Sonnet 4.6
  (generation) and Claude Haiku 4.5 (eval); the helper
  encodes those verbatim. If the actual runtime model
  changes later, a separate task refreshes this helper.
- Edits to `page.tsx` and `lab/page.tsx` MUST be surgical:
  touch only the specific occurrences of "Sonnet 4.6" /
  "Haiku 4.5" / "Claude Sonnet 4.6" / "Claude Haiku 4.5".
  No unrelated refactor. No component extraction. No copy
  rewrite beyond substituting the version string.
- Do NOT invent unsupported model claims (e.g. "frontier
  model", benchmark numbers). Preserve the existing
  conservative "single-shot, evidence-grounded" framing.
- Do NOT mention OpenAI as a runtime provider (Q7 blocked
  sense; OpenAI is not used at runtime for this project's
  own AI calls).

## Acceptance criteria

- [ ] `src/lib/models-display.ts` exists, ≤50 lines,
      exports primitive constants (`generationModel`,
      `generationModelShort`, `evalModel`, `evalModelShort`,
      `provider`), with a JSDoc comment naming the source
      (runtime API routes) and refresh instructions if
      runtime models change.
- [ ] Helper does NOT import `@anthropic-ai/sdk`, `@/lib/prompts`,
      `@/lib/corpus`, or anything under `@/app/api/**`.
- [ ] Helper does NOT call any API or perform any side
      effect at module scope.
- [ ] `page.tsx:1067-1068` "Claude Sonnet 4.6 for
      generation, Haiku 4.5 if you run the eval" → uses
      `MODELS_DISPLAY.generationModel` +
      `MODELS_DISPLAY.evalModelShort`.
- [ ] `page.tsx:1122` "Sonnet 4.6 + Haiku 4.5" → uses
      `MODELS_DISPLAY.generationModelShort` +
      `MODELS_DISPLAY.evalModelShort`.
- [ ] `page.tsx:1639` "Claude Sonnet 4.6 (generation) ·
      Haiku 4.5 (eval)" → uses
      `MODELS_DISPLAY.generationModel` +
      `MODELS_DISPLAY.evalModelShort`.
- [ ] `lab/page.tsx:245` "Claude Haiku 4.5" → uses
      `MODELS_DISPLAY.evalModel`.
- [ ] `src/lib/prompts.ts`, `src/lib/corpus.ts`,
      `src/lib/types.ts`, `src/lib/anthropic.ts` (not
      present), and all files under `src/app/api/**`
      untouched.
- [ ] `src/data/**` untouched.
- [ ] `src/app/layout.tsx`, `src/app/opengraph-image.tsx`,
      `src/app/methodology/page.tsx`,
      `src/app/sample-report/page.tsx`,
      `src/app/snapshot-pipeline/page.tsx`,
      `src/components/**` untouched.
- [ ] `package.json` / `package-lock.json` untouched.
- [ ] `.agent/scripts/**` untouched (hard rule).
- [ ] `.agent/blockers.md` /
      `.agent/automation_queue.md` untouched.
- [ ] `npm run lint` passes with only pre-existing baseline
      errors unchanged.
- [ ] `npm run build` passes (Next.js production build
      succeeds; TypeScript clean).
- [ ] `git diff --stat` shows changes only under
      `.agent/tasks/`, `.agent/run_reports/`, `src/lib/`
      (new helper file only), `src/app/page.tsx`, and
      `src/app/lab/page.tsx`.
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created.
- [ ] Pipeline HEAD = `b019786` unchanged.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check
git status --short
git diff --stat

# helper structural check
wc -l src/lib/models-display.ts
grep -c 'from "@anthropic-ai\|from "@/lib/prompts\|from "@/lib/corpus\|from "@/app/api' src/lib/models-display.ts  # must be 0
grep -c 'fetch(\|await ' src/lib/models-display.ts  # must be 0 (no side effect)

# UI change spot-check
grep -n 'Sonnet 4\.6\|Haiku 4\.5' src/app/page.tsx src/app/lab/page.tsx  # remaining hardcoded occurrences?
grep -n 'MODELS_DISPLAY' src/app/page.tsx src/app/lab/page.tsx

# forbidden audit
git diff --stat HEAD -- \
    src/lib/prompts.ts src/lib/corpus.ts src/lib/types.ts \
    src/lib/eval-report.ts src/lib/extract-pdf.ts \
    src/lib/web-bundle-stats.ts \
    src/data/ package.json package-lock.json \
    src/app/api/ src/app/layout.tsx src/app/opengraph-image.tsx \
    src/app/methodology/ src/app/sample-report/ src/app/snapshot-pipeline/ \
    src/components/ \
    .agent/policies/ .agent/templates/ .agent/scripts/ \
    .agent/blockers.md .agent/automation_queue.md \
    .github/workflows/ vercel.json .vercel/ 2>&1 || true

# lint + build
npm run lint 2>&1 | tail -20
npm run build 2>&1 | tail -30

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

- Exact path of `src/lib/models-display.ts` (NEW, ≤50 lines).
- Exact commit hashes: TASK+impl commit + RUN_REPORT commit.
- `npm run lint` result (pass / warnings-only-unchanged-from-baseline).
- `npm run build` result (pass, TypeScript clean, 14/14
  static routes).
- Confirmation that pipeline HEAD = `b019786` at start and
  end.

## Rollback plan

- Yellow product-code task. Same direct-on-`main` pattern
  P1.7b (`1c912f8`) used.
- If DECISION requests changes: edit in a follow-up commit;
  do not amend.
- If full revert: `git revert <run_report_commit>
  <impl_commit>`. Never `git reset --hard` on pushed
  commits.

## Requires human approval

`no`

> Yellow small display-copy change, no red-zone file. Push
> (after the matching DECISION) is a separate human
> approval per global policy; the push will trigger Vercel
> auto-deploy (same as P1.7b).

## Max runtime minutes

`45` — small helper + 4 line-edits + build.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

- Stop if any path under `src/lib/prompts.ts`,
  `src/lib/corpus.ts`, `src/lib/anthropic.ts` (if
  created), `src/app/api/**`, `src/data/**`,
  `package*.json`, `.env*`, `vercel.json`, `.vercel/`,
  `.github/workflows/`, `.agent/scripts/`,
  `.agent/policies/`, `.agent/templates/`,
  `.agent/blockers.md`, `.agent/automation_queue.md`, or
  any pipeline-repo file appears in `git status`.
- Stop if `npm run build` fails.
- Stop if `npm run lint` introduces NEW errors (baseline
  warnings are OK).
- Stop if the helper imports `@anthropic-ai/sdk`,
  `@/lib/prompts`, `@/lib/corpus`, or anything under
  `@/app/api/**`.
- Stop if the helper contains any `fetch(` /
  `await …` at module scope.
- Stop if more than 4 files would be committed in the
  impl commit (allowed: 1 TASK + 1 helper + 1 page.tsx +
  1 lab/page.tsx + 1 RUN_REPORT max).
- Stop if a DECISION file appears in `git status`.
- Stop if `layout.tsx`'s SEO-keywords array is edited.
- Stop if any archetype blurb / JD content / methodology
  commentary is edited (out of scope; content, not
  model-string SSOT).
