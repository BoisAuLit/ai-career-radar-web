# TASK · AgentOps 4a stability Fixture B rerun

## Metadata

- **task_id**: `2026-07-19_run_02`
- **date**: `2026-07-19`
- **run_number**: `02`
- **title**: AgentOps-4a-stability · one Fixture B stability re-run before B baseline promotion
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-4a-stability`
- **parent_loop**: `AgentOps-4a` (`2026-07-19_run_01`)
- **grandparent_loop**: `AgentOps-3g-2` (`2026-07-12_run_09`)

## Objective

Run **Fixture B** once more through the existing local report regression
harness (`scripts/report-regression-local.mjs` unchanged since AgentOps-4a
· `0341461` is `origin/main` HEAD) against `localhost:3000` to test
**stability** before any Fixture B baseline promotion. Primary goal is a
**second Fixture B data point** to confirm the AgentOps-4a first-try GREEN
was reproducible, not a lucky run. This mirrors AgentOps-3g-stability's
role before AgentOps-3g-2 for Fixture A.

**No code changes.** No harness edits. Fixture A default behavior remains
preserved. Fixture B baseline is NOT promoted in this loop.

## Background

AgentOps-4a (`2026-07-19_run_01`) landed the first real Fixture B run:
`20260719T045622Z_fixture-B` · verdict green · exit 0 ·
`capture_scope="main section"` · `fallback_used=false` ·
`report_char_count=10407` · `duration_ms=67719` · 25/25 checks passed ·
recommendation-match 5/5 (agent · tool call · tool-call · eval ·
telemetry). AgentOps-4a DECISION explicitly recommended a stability
re-run before baseline promotion, following the same pattern as
AgentOps-3g-stability which validated Fixture A's first GREEN before
AgentOps-3g-2 promoted A's baseline.

Reference values (from `20260719T045622Z_fixture-B`):

| field | value |
|---|---|
| verdict | green |
| exit_code | 0 |
| capture_scope | main section |
| fallback_used | false |
| report_char_count | 10407 |
| page_body_char_count | 16983 |
| report_length_soft_min / max | 1500 / 14000 |
| duration_ms | 67719 |
| markers | 5 / 5 + Evidence Appendix |
| checks total / passed | 25 / 25 |
| recommendation-match hits | 5 / 5 |
| must_not_happen matches | 0 |
| corpus_snapshot_date | May 14, 2026 |
| model_display | Claude Sonnet 4.6 |
| harness commit at run time | `d393db9` (harness logic pre-4a) |
| harness commit today | `0b99358` (AgentOps-4a added `--fixture` only) |

## Allowed files

- `.agent/tasks/2026-07-19_run_02_TASK.md` (this file)
- `.agent/regression_runs/<new-run-id>_fixture-B/metadata.json`
- `.agent/regression_runs/<new-run-id>_fixture-B/structural_checks.json`
- `.agent/regression_runs/<new-run-id>_fixture-B/verdict.md`
- `.agent/run_reports/2026-07-19_run_02_RUN_REPORT.md`

## Forbidden files

- **`scripts/report-regression-local.mjs`** (harness stable · no edit in
  a stability loop)
- `src/**` (any file)
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `.agent/scripts/**` (hard rule per AgentOps-2c Q3-Q8)
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/regression_fixtures/**` (frozen · B fixture file read-only)
- **`.agent/regression_baselines/**`** (A baseline read-only · no B
  baseline created)
- **`.agent/baselines/**`** · **`baselines/**`** (wrong paths)
- `.agent/planner_reports/**`
- uploaded resume PDFs (external material · separate intake loop)
- pipeline repo (any file)
- `sources.yaml`, `corpus/**`, `scripts/collector/**`
- `.github/workflows/**`
- `package.json`, `package-lock.json` (no new dep)
- `.env*`, `vercel.json`
- Codex config files, Claude config files
- production deployment config

## Risk level

`yellow` (one real localhost generation · ≈ $0.05 · no code change)

## Constraints

- **Fixture B only.**
- **Localhost only.** Base URL must resolve to `localhost` / `127.0.0.1`.
- **Playwright CLI only.**
- **No harness code change.**
- **No new npm dependencies.**
- **No `.env*` read.**
- **No Anthropic/OpenAI HTTP call** from harness (browser driver only).
- **One real report generation** exactly. Max 2 total if first fails on
  env glitch.
- **No `src/**` edits.**
- **No `.agent/scripts/**` edits.**
- **No `.agent/regression_baselines/**` edits.** No Fixture B baseline
  creation.
- **No `report.md` / `screenshot.png` committed.**
- **No production target.**
- **No push. No deploy.**
- **No Eval button click.**
- **No corpus refresh / collector run.**
- **No GitHub Actions changes.**
- **No uploaded PDF ingestion.**

## Stability comparison targets (manual · in RUN_REPORT)

Compare new run against previous Fixture B GREEN run
`20260719T045622Z_fixture-B`:

- **report_char_count delta**: new − 10407
- **report_char_count percent change**: `(new − 10407) / 10407 × 100`
- **duration_ms delta**: new − 67719
- **duration_ms percent change**: `(new − 67719) / 67719 × 100`
- **within ±30%** for `report_char_count`? (yes/no)
- **within 2×** for `duration_ms`? (yes/no)
- **capture_scope stayed non-body**? (yes/no)
- **fallback_used stayed false**? (yes/no)
- **all 25 checks stayed green**? (yes/no)
- **recommendation-match hits**: compare 5-of-5 keyword hit rate
- **must_not_happen matches**: both should be 0

**Do NOT modify harness** to add comparison — do it manually in RUN_REPORT
prose.

## Acceptance criteria

- [ ] One real Fixture B generation via localhost happened.
- [ ] `done` state reached; incomplete banner absent (Candidate 1
      sentinel intact).
- [ ] `capture_scope != body_fallback`; `fallback_used == false`.
- [ ] All 5 required section headers + Evidence Appendix present.
- [ ] Zero must-not-happen matches on Fixture B's literal list.
- [ ] No fatal Playwright error.
- [ ] `metadata.json`, `structural_checks.json`, `verdict.md` committed
      under `.agent/regression_runs/<new-run-id>_fixture-B/`.
- [ ] `report.md` and `screenshot.png` written to `os.tmpdir()` only,
      **not** committed.
- [ ] **No `scripts/report-regression-local.mjs` change.**
- [ ] No `src/**` change · no `.agent/scripts/**` change · no
      `.agent/regression_fixtures/**` change · no
      `.agent/regression_baselines/**` change · no pipeline change · no
      `package.json` / lockfile change · no `.env*` read.
- [ ] No wrong-path baseline dirs created.
- [ ] No uploaded resume PDFs committed.
- [ ] Comparison against previous Fixture B GREEN recorded in RUN_REPORT.
- [ ] RUN_REPORT includes the 3f `## Regression verdict` section with
      `regression_required=yes`, verdict reflecting result,
      `baseline_promoted=no`, `production_target_used=no`.
- [ ] BLK-0001 / BLK-0002 / BLK-0003 still `open`.
- [ ] QUEUE-0002 G2.1d still `blocked_pending_human`.

## Expected outcomes

**Ideal**: verdict = **GREEN** · `exit_code=0` · `capture_scope != body_fallback`
· `fallback_used=false` · `report_char_count` within ±30% of 10407 ·
`duration_ms` within 2× of 67719 · recommendation-match hits ≥ 3/5 · all
25 checks pass. → Clears the way for AgentOps-4b baseline promotion.

**Acceptable**: verdict remains GREEN with modest metadata drift within
tolerance bounds — normal LLM variance.

**Interesting** (still `approve`-eligible but worth calling out): AMBER
for a known-flaky check (recommendation match heuristic, action-bar
visibility jitter, or length-band edge case). RUN_REPORT names the check
and explains.

**Blocking**: verdict = RED · OR `fallback_used=true` · OR
`capture_scope=body_fallback` · OR any must-not-happen match · OR
incomplete banner visible · OR any red check failing. → Defers
AgentOps-4b · opens a fix loop.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# preflight
git status
git log --oneline -6
[ ! -d .agent/regression_baselines/fixture-B ] && echo "OK: no fixture-B baseline yet"
[ ! -d .agent/baselines ] && [ ! -d baselines ] && echo "OK: no wrong-path baseline dirs"

# dev server if not up
curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:3000 || \
  npm run dev > /tmp/dev-4a-stability-fixture-b.log 2>&1 &

# run harness (no code change · same command as 4a)
node scripts/report-regression-local.mjs --fixture B

# validate
git diff --name-only
git diff --stat
cat .agent/regression_runs/<new-run-id>_fixture-B/verdict.md

# forbidden sweep
git diff --name-only | grep -E '^(src/|scripts/report-regression-local\.mjs|\.agent/scripts/|\.agent/regression_fixtures/|\.agent/regression_baselines/|\.agent/planner_reports/|\.agent/baselines/|baselines/|package(-lock)?\.json|\.github/workflows/)' || echo "OK"
```

## Expected output

RUN_REPORT must include:
- task path
- previous Fixture B GREEN run id: `20260719T045622Z_fixture-B`
- new run id (fresh UTC timestamp with `_fixture-B` suffix)
- exact harness command: `node scripts/report-regression-local.mjs --fixture B`
- 3f Regression verdict section (all 21 fields)
- structural + fixture + operational check summaries
- **stability comparison table** vs `20260719T045622Z_fixture-B`
- fixture-specific keyword hit comparison
- comparison to Fixture A baseline **as context only** (not pass/fail)
- whether Fixture B stability is confirmed
- whether Fixture B is eligible for future baseline consideration
- recommendation logic:
  - GREEN → approve + push · next loop = AgentOps-4b B baseline promotion
  - AMBER → explain calibration vs product-risk root cause
  - RED → recommend fix/revert/analyze before push
- why uploaded 20 PDFs were not ingested this loop
- forbidden-file audit + scratchpad paths

## Rollback plan

- If harness / env misbehaves: rename or delete
  `.agent/regression_runs/<new-run-id>_fixture-B/` (uncommitted); keep
  TASK for the log; mark next DECISION as `request_changes`.
- No harness code touched → nothing to revert on the script side.
- No push in this loop → rollback is local-only.

## Requires human approval

`yes` (yellow · one real localhost generation · ≈ $0.05 · no code change)

> Approved by: `Bohao msg 2026-07-19 · "Start AgentOps-4a-stability:
> Fixture B stability re-run"` and
> `.agent/decisions/2026-07-19_run_01_DECISION.md` (explicit endorsement
> of 4a-stability as default next loop).

## Max runtime minutes

10 (harness ~2 min; dev server ~15 s; overhead)

## Max token budget hint

`unset`

## Stop conditions

- Stop if any forbidden file appears in `git status`.
- Stop if any baseline directory gets created.
- Stop if more than 2 real report generations happen.
- Stop if the harness attempts a non-localhost target.
- Stop if `done_state_reached` is false and cannot be recovered.
- Stop if `.agent/scripts/**`, `src/**`, `.agent/regression_baselines/**`,
  `.agent/regression_fixtures/**`, or `scripts/report-regression-local.mjs`
  appears in `git diff`.
- Stop if any uploaded PDF appears staged.

## Do NOT

- Do NOT modify `scripts/report-regression-local.mjs`.
- Do NOT modify any code.
- Do NOT modify `src/**`.
- Do NOT modify `.agent/scripts/**`.
- Do NOT modify `.agent/regression_fixtures/**`.
- Do NOT modify `.agent/regression_baselines/**`.
- Do NOT create `.agent/regression_baselines/fixture-B/`.
- Do NOT promote Fixture B baseline.
- Do NOT ingest the 20 uploaded PDFs.
- Do NOT commit uploaded PDFs.
- Do NOT run Fixture A.
- Do NOT run C/D/E.
- Do NOT run A-E full suite.
- Do NOT test production.
- Do NOT click Eval.
- Do NOT implement Codex planner.
- Do NOT create `.agent/planner_reports/`.
- Do NOT modify pipeline.
- Do NOT run collector.
- Do NOT refresh corpus.
- Do NOT call Anthropic/OpenAI outside the local app.
- Do NOT introduce OpenAI API.
- Do NOT modify GitHub Actions.
- Do NOT modify `package.json` or `package-lock.json`.
- Do NOT push.
- Do NOT deploy.
- Do NOT resolve BLK-0001 / BLK-0002 / BLK-0003.
- Do NOT start G2.1d.
- Do NOT resume full automation.
