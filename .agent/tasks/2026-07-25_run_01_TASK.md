# TASK · AgentOps 5e implement classify JSON hardening

## Metadata

- **task_id**: `2026-07-25_run_01`
- **date**: `2026-07-25`
- **run_number**: `01`
- **title**: AgentOps 5e implement classify JSON hardening
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-5e-followup-phase3-classify-json-hardening-implement`
- **parent_loop**: `AgentOps-5e-followup-phase3-classify-json-hardening-design` (`2026-07-24_run_11`)
- **authorizing_decision**: `.agent/decisions/2026-07-24_run_11_DECISION.md`

## Objective

Implement deterministic schema-constrained `/api/classify` output per the approved Phase 3 hardening design: replace the current `generateText` + manual `JSON.parse(raw)` architecture with `generateObject` + strict Zod validation, `maxRetries: 0`, no `experimental_repairText`, sanitized client errors, redacted server-side observability, correlation ID, and 37+ fully mocked deterministic tests. Add `zod` as a direct project dependency (`^4.4.3`). Preserve success response contract at the field/type level. **No paid provider call. No fixture rerun. No baseline/telemetry/legacy/process-exit change. $0 provider cost.**

## Background

Approved by `2026-07-24_run_11_DECISION.md` (Option A · generateObject + direct Zod strict schema · one provider call · no retry · no repair · sanitized errors · redacted observability · field/type-compatible success). Installed versions: `ai` 6.0.182 · `@ai-sdk/anthropic` 3.0.77 · `zod` 4.4.3 (transitive today, becoming DIRECT via this implementation). Node v22.21.0 · npm 10.9.4 · tsconfig `moduleResolution: bundler` + `allowJs: true` supports importing `.mjs` from route.ts.

## Allowed files

- `package.json` (add `zod` to `dependencies`)
- `package-lock.json` (auto-updated)
- `src/app/api/classify/route.ts` (rewrite as thin wrapper)
- `src/lib/classify-schema.mjs` (**new** · Zod schema · pure ESM)
- `src/lib/classify-handler.mjs` (**new** · pure ESM · dependency-injected handler)
- `scripts/test-classify-schema.mjs` (**new** · schema tests)
- `scripts/test-classify-route.mjs` (**new** · route tests + privacy + compat + static assertions)
- `.agent/tasks/2026-07-25_run_01_TASK.md`
- `.agent/design_memos/2026-07-25_AgentOps-5e-followup-phase3-classify-json-hardening-implement.md`
- `.agent/run_reports/2026-07-25_run_01_RUN_REPORT.md`

## Forbidden files

- `src/lib/prompts.ts` (evaluate carefully; keep semantically equivalent if touched at all — prefer NOT to touch)
- `scripts/quote-integrity-check.mjs` · `scripts/structural-evidence-check.mjs` · `scripts/lib/structural-evidence-integration.mjs`
- `scripts/report-regression-local.mjs`
- `.agent/regression_baselines/**` · `.agent/regression_runs/**` · `.agent/regression_fixtures/**`
- `.agent/scripts/**` · workflows / env / `vercel.json`
- Pipeline
- Any dependency other than `zod`

## Risk level

`yellow`

## Constraints

- **No paid provider call** · no Anthropic · no OpenAI · no `/api/classify` real invocation · no `/api/generate-report` invocation
- **No rerun** (Fixture A or B)
- **No dev server** started this loop
- **No new dependency** other than `zod ^4.4.3`
- **No retry** · no `experimental_repairText` · no repair · no fallback provider · exactly 1 provider call per request
- **No silent transforms** (no trim / no lowercase / no dedup / no defaults / no coercion / no enum normalization / no repair)
- **No baseline mutation** · no telemetry semantic change · no legacy verdict / process-exit change
- **No promotion** · no blocking · no `AgentOps-5f-promote` · no Phase 4/5/6
- **No push · no deploy · no DECISION in this turn**

## Acceptance criteria

- [x] TASK created for run 01.
- [ ] `zod ^4.4.3` added to `package.json` `dependencies` · `package-lock.json` reconciled.
- [ ] `src/lib/classify-schema.mjs` — strict Zod schema (validation only · duplicate refinement · whitespace-only refinement · `.strict()`).
- [ ] `src/lib/classify-handler.mjs` — pure ESM · dependency-injected · one provider call · sanitized errors.
- [ ] `src/app/api/classify/route.ts` — thin wrapper wiring real deps (`generateObject`, `anthropic`, `classifySystemPrompt`, correlation ID, logger).
- [ ] `scripts/test-classify-schema.mjs` — at least 16 tests (T1-T16).
- [ ] `scripts/test-classify-route.mjs` — at least 31 tests (T17-T47); static assertions include `JSON.parse(raw)` removed / no retry loop / exactly one gen call / no `experimental_repairText` / Zod in direct deps.
- [ ] Total ≥ **37 tests** · all mocked · zero real provider · zero network.
- [ ] `npx tsc --noEmit` exit 0.
- [ ] `node --check` on all new/modified .mjs files pass.
- [ ] `node scripts/test-structural-evidence-check.mjs` still 40/40 PASS.
- [ ] `node scripts/test-structural-evidence-integration.mjs` still 26/26 PASS.
- [ ] Forbidden zones 0-diff: `src/lib/prompts.ts` · QI checker · structural checker · harness · fixtures · baselines · regression runs · `.agent/scripts/**` · workflows / env / vercel.
- [ ] Cost this turn: **$0 provider cost**.
- [ ] No push · no deploy · no DECISION.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web
npm install zod@^4.4.3 --save --package-lock-only --no-audit --no-fund
node --check scripts/test-classify-schema.mjs
node --check scripts/test-classify-route.mjs
node --check src/lib/classify-schema.mjs
node --check src/lib/classify-handler.mjs
npx tsc --noEmit
node scripts/test-classify-schema.mjs
node scripts/test-classify-route.mjs
node scripts/test-structural-evidence-check.mjs
node scripts/test-structural-evidence-integration.mjs
git diff --stat
git diff --stat -- src/lib/prompts.ts scripts/quote-integrity-check.mjs scripts/structural-evidence-check.mjs scripts/report-regression-local.mjs .agent/regression_baselines/ .agent/regression_fixtures/ .agent/regression_runs/ .agent/scripts/
```

## Expected output

- 3 commits (impl + tests + governance).
- Web ahead by 3.
- Pipeline `b019786` untouched.
- Cost: **$0 provider cost**.

## Rollback plan

- Revert 3 commits — reverts package.json/lockfile Zod addition, route rewrite, and new files.
- No baseline / prompt / harness / telemetry change to reverse.

## Requires human approval

`yes`

> Approved by: `.agent/decisions/2026-07-24_run_11_DECISION.md`.

## Max runtime minutes

90

## Max token budget hint

`unset`

## Stop conditions

- Stop if a forbidden file appears in `git status`.
- Stop if `generateObject` cannot be made compatible with `@ai-sdk/anthropic` for the specific schema — create a **revise** RUN_REPORT rather than silently switching to Option B.
- Stop if `npx tsc --noEmit` reports errors that cannot be resolved without touching forbidden scope.
- Stop if `scripts/test-structural-evidence-*.mjs` regress.
- Stop if any test requires a real provider call.
