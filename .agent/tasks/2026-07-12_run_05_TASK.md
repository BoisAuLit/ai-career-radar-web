# TASK · AgentOps 3e tune length band

## Metadata

- **task_id**: `2026-07-12_run_05`
- **date**: `2026-07-12`
- **run_number**: `05`
- **title**: AgentOps 3e-tune-2 · length-band calibration
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-3e-tune-2`
- **parent_loop**: `AgentOps-3e-tune` (`2026-07-12_run_04`)
- **grandparent_loop**: `AgentOps-3e` (`2026-07-12_run_03`)

## Objective

Calibrate `scripts/report-regression-local.mjs` by widening the report length
soft max from **6000 → 14000**, then re-run **Fixture A** once locally against
`localhost:3000` and write a new small verdict. Primary goal is to remove the
false-AMBER `report_length_in_soft_band` failure caused by the outdated
`6000` upper bound that was set for the whole-body-capture era. Do NOT force
green — let the harness decide honestly. If AMBER persists for a new real
reason (must-not-happen match, missing section, capture regression, etc.),
record that reason exactly.

## Background

AgentOps-3e (`36cd001`) shipped the minimal Playwright regression prototype and
produced verdict AMBER only because `report_length_in_soft_band` failed with
`chars=17181`. AgentOps-3e-tune (`a240565`) implemented `extractReportText`
marker-scored candidate selection; `report_char_count` dropped 17181 → 11115
(−35%), `capture_scope="main section"`, `fallback_used=false`. Verdict remained
AMBER because the 1500-6000 band was still calibrated for whole-body capture.
This loop bumps `REPORT_LEN_SOFT_MAX` to 14000 to match the real
report-specific selected content, and records the thresholds explicitly in
metadata + check details so future readers can see what band was used.

Relevant files:
- `.agent/decisions/2026-07-12_run_04_DECISION.md` (endorses 3e-tune-2 as
  default next loop with `REPORT_LEN_SOFT_MAX = 14000` recommended)
- `.agent/run_reports/2026-07-12_run_04_RUN_REPORT.md`
- `.agent/regression_runs/20260713T011451Z_fixture-A/{metadata,structural_checks,verdict}.*`

## Allowed files

- `.agent/tasks/2026-07-12_run_05_TASK.md`
- `scripts/report-regression-local.mjs`
- `.agent/regression_runs/<new-run-id>/metadata.json`
- `.agent/regression_runs/<new-run-id>/structural_checks.json`
- `.agent/regression_runs/<new-run-id>/verdict.md`
- `.agent/run_reports/2026-07-12_run_05_RUN_REPORT.md`

## Forbidden files

- `src/**` (any file)
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `.agent/scripts/**` (hard rule per AgentOps-2c Q3-Q8)
- `.agent/policies/**`
- `.agent/templates/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/regression_fixtures/**` (frozen)
- `.agent/planner_reports/**` (do not create in this loop)
- pipeline repo (any file)
- `sources.yaml`, `corpus/**`, `scripts/collector/**`
- `.github/workflows/**`
- `package.json`, `package-lock.json`
- `.env*`, `vercel.json`
- Codex config files, Claude config files
- any production deployment config
- any baseline promotion file

## Risk level

`yellow`

## Constraints

- **Fixture A only.**
- **Localhost only.** Base URL must resolve to `localhost` / `127.0.0.1`.
- **Playwright CLI only.** No Playwright MCP.
- **One real report generation** after tuning. Max 2 total if the first fails.
- **No new npm dependencies.**
- **No `.env*` read** by harness.
- **No Anthropic/OpenAI HTTP call** from harness.
- **No `src/**` edits** (no `data-testid`, no DOM markers).
- **No baseline promotion.**
- **No `report.md` / `screenshot.png` committed.**
- **No production target.**
- **No push.** No `git push`. No `vercel deploy`.
- **No Eval button click.**
- **No corpus refresh / collector run.**
- **No GitHub Actions changes.**

## Length band change

- `REPORT_LEN_SOFT_MIN`: stays `1500`.
- `REPORT_LEN_SOFT_MAX`: **6000 → 14000**.
- Both constants live at the top of `scripts/report-regression-local.mjs`
  already (lines 53-54); no magic numbers to hoist.

## Metadata additions

- `report_length_soft_min` (from constant)
- `report_length_soft_max` (from constant · will read `14000` post-change)

## Structural checks change

- `report_length_in_soft_band` detail already carries
  `chars=<n> band=<min>-<max>`; the new max flows through automatically.
  No new key needed.

## Acceptance criteria

- [ ] `REPORT_LEN_SOFT_MAX = 14000` in `scripts/report-regression-local.mjs`.
- [ ] `REPORT_LEN_SOFT_MIN = 1500` unchanged.
- [ ] Metadata `report_length_soft_min = 1500` and
      `report_length_soft_max = 14000` recorded.
- [ ] `extractReportText` marker-scored candidate strategy unchanged.
- [ ] Body-fallback path unchanged (still forces AMBER via
      `report_capture_scope_not_body`).
- [ ] One real report generation happened via localhost.
- [ ] `done` state reached; incomplete banner absent.
- [ ] All 5 required section headers + Evidence Appendix present in selected
      report text.
- [ ] Zero must-not-happen matches.
- [ ] No fatal Playwright error.
- [ ] `metadata.json`, `structural_checks.json`, `verdict.md` committed under
      `.agent/regression_runs/<new-run-id>/`.
- [ ] `report.md` and `screenshot.png` written to `os.tmpdir()` only,
      **not** committed.
- [ ] No `src/**` change. No `.agent/scripts/**` change. No
      `.agent/regression_fixtures/**` change. No pipeline change. No
      `package.json` / lockfile change. No `.env*` read.
- [ ] BLK-0001 / BLK-0002 / BLK-0003 still `open`.
- [ ] QUEUE-0002 G2.1d still `blocked_pending_human`.

## Expected result

**Ideal**: verdict = **GREEN** · `capture_scope` remains a report-specific
candidate (not `body_fallback`) · `fallback_used=false` ·
`report_length_in_soft_band` **passes** at 11115 within 1500-14000 · all red
+ amber checks pass.

**Acceptable**: verdict remains AMBER **only for a new real reason** that
RUN_REPORT explains explicitly (e.g. duration jitter over soft 120s threshold,
recommendation match heuristic miss for this generation, action-bar visibility
flake). Not acceptable if `report_length_in_soft_band` still fails.

**Not acceptable**: body_fallback used without warning · production target
attempted · generation fails · incomplete banner present · missing Evidence
Appendix · must-not-happen matched · `src/**` modified · `.agent/scripts/**`
modified · pipeline modified · baseline promoted · more than one real
generation after a successful one.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# preflight
git status
git log --oneline -6

# start local dev server if not up
curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:3000 || \
  npm run dev > /tmp/dev-3e-tune-2.log 2>&1 &

# run tuned harness
node scripts/report-regression-local.mjs

# validate
git diff --name-only
git diff --stat
cat .agent/regression_runs/<new-run-id>/verdict.md
```

## Expected output

RUN_REPORT must include:
- previous run id: `20260713T011451Z_fixture-A`
- new run id (fresh UTC timestamp)
- exact length-band change (`6000 → 14000`)
- `capture_scope` value
- `fallback_used` value
- `page_body_char_count`
- `report_char_count`
- `report_length_soft_min` / `report_length_soft_max`
- verdict + exit code + duration
- side-by-side vs 3e-tune run
- whether AMBER was removed
- if AMBER remains, exact new reason
- forbidden-file audit (empty-diff sweep)
- scratchpad paths
- recommendation: human + ChatGPT review, then DECISION

## Rollback plan

- If the harness misbehaves: revert `scripts/report-regression-local.mjs`
  to `a240565`, delete the new `.agent/regression_runs/<new-run-id>/`
  directory, mark next DECISION as `request_changes`.
- No push happens in this loop, so rollback is local-only.

## Requires human approval

`yes` (yellow risk + script change + real generation)

> Approved by: `Bohao msg 2026-07-12 · "Start AgentOps-3e-tune-2: length-band calibration"`
> and `.agent/decisions/2026-07-12_run_04_DECISION.md` (explicit endorsement of
> 3e-tune-2 as default next loop with `REPORT_LEN_SOFT_MAX = 14000` recommended).

## Max runtime minutes

10 (harness ~2 min; dev server ~15 s; overhead)

## Max token budget hint

`unset`

## Stop conditions

- Stop if any forbidden file appears in `git status`.
- Stop if more than 2 real report generations happen.
- Stop if the harness attempts a non-localhost target.
- Stop if `done_state_reached` is false.
- Stop if any `src/**` file appears in `git diff`.
- Stop if any red check fails.

## Do NOT

- Do NOT start AgentOps-3f.
- Do NOT integrate verdict into RUN_REPORT template.
- Do NOT modify `.agent/templates/**`.
- Do NOT implement Codex planner.
- Do NOT create `.agent/planner_reports/`.
- Do NOT run A-E full suite.
- Do NOT run production.
- Do NOT promote baseline.
- Do NOT click Eval.
- Do NOT add OpenAI API.
- Do NOT call Anthropic/OpenAI directly from harness.
- Do NOT read `.env` files from harness.
- Do NOT modify `.agent/scripts/**`.
- Do NOT modify `src/**`.
- Do NOT add DOM markers or `data-testid`.
- Do NOT modify prompts.
- Do NOT modify model selection.
- Do NOT modify generate-report route.
- Do NOT modify eval route.
- Do NOT modify pipeline.
- Do NOT run collector.
- Do NOT refresh corpus.
- Do NOT modify GitHub Actions.
- Do NOT add dependencies.
- Do NOT modify `package.json` or `package-lock.json`.
- Do NOT push.
- Do NOT deploy.
