# TASK · AgentOps-3a Automation Advancement design memo

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-08_run_01`
- **date**: `2026-07-08` (task filename convention;
  memo authored during 2026-07-11 strategic-pause
  exit)
- **run_number**: `01`
- **title**: AgentOps-3a Automation Advancement
  System design memo
- **repo**:
  `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Design — **without implementing anything** — the
**next-stage automation advancement system** that
succeeds AgentOps-2b (runner options memo) and
AgentOps-2c (supervised dry-run shapes memo). The
memo pins the target role for each actor (Human,
ChatGPT, Codex, Claude Code, Playwright report-
regression harness) and stages a **phased rollout
(3a → 3f)** in which the earliest automation
touches remain read-only and every product-quality
change is gated behind a **real-report regression
harness**. Nothing in this loop starts an actual
runner, harness, planner script, or scheduler.

## Background

- **Strategic pause resumed today**: 2026-07-08
  daily summary explicitly called out "any
  AgentOps-2* work" as NOT recommended in the
  cleanup turn after Candidate 1 + Candidate 4 +
  E2E. Bohao's 2026-07-11 message opens a **new
  track (AgentOps-3)** distinct from the paused
  AgentOps-2 track — the pause was for the
  runner-implementation track pinned in
  AgentOps-2c Q10, not for further strategic
  scoping.
- **AgentOps-2b DECISION** (`.agent/decisions/2026-06-29_run_03_DECISION.md`)
  pinned **Q1 = Option B (Local supervised runner,
  dry-run only)** as the future direction (NOT
  implementation approval); **Q7 = Codex CLI via
  ChatGPT sign-in does NOT trigger BLK-0003**;
  OpenAI API SDK / key / HTTP / import / CI secret
  / background token remain blocked.
- **AgentOps-2c DECISION** (`.agent/decisions/2026-06-30_run_01_DECISION.md`)
  pinned **Shape B (Non-executing local planner)**
  as the future prototype shape; **Q3-Q8** = the
  first Shape B prototype is extremely narrow
  (report-only, non-executing, no auto Codex /
  Claude / commit / push / product touch, no
  `.agent/scripts/**` mod); **Q10 = pause
  automation-infra expansion and return to product
  work** (which produced P2.0a/b/c and P2.1a +
  Candidate 1 + Candidate 4).
- **Blockers** all remain `open`: BLK-0001
  (G2.1d red-zone), BLK-0002 (full automation
  activation), BLK-0003 (OpenAI API standing
  Q7-scoped).
- **Product state**: Candidate 1 stream sentinel
  + Candidate 4 empty-PDF gate are on
  `origin/main` at `4d97d12`; E2E smoke test
  PASS. Real report entrance reliability is
  materially stronger. Now the natural next
  automation question is: **how do we
  automate more of the daily loop without
  losing the report-quality bar we just
  raised?**

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-07-08_run_01_TASK.md`
  (this file)
- `.agent/design_memos/2026-07-08_AgentOps-3a_automation_advancement.md`
  (new memo)
- `.agent/run_reports/2026-07-08_run_01_RUN_REPORT.md`

## Read-only allowed (must NOT be modified)

- `.agent/**` (existing content, non-scope files)
- `package.json`
- `src/app/page.tsx`
- `src/app/api/generate-report/route.ts`
- `src/app/api/extract-pdf/route.ts`
- `src/lib/prompts.ts`
- `src/lib/eval-report.ts`
- Any script under `scripts/` if useful for
  context
- Pipeline repo files for context only

## Forbidden files

> Paths the executor MUST NOT edit, even if it seems useful.

- **`src/**` any file** — this task must not
  change product code.
- `src/data/**`
- `src/lib/prompts.ts`
- `src/app/api/**`
- `package.json`
- `package-lock.json`
- `.github/workflows/**`
- `.agent/scripts/**` — hard rule per
  AgentOps-2c Q3-Q8.
- `.agent/policies/**`
- `.agent/templates/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- **Pipeline repo any file**.
- `sources.yaml` (pipeline)
- `corpus/**` (pipeline)
- `scripts/collector/**` (pipeline)
- `.env*`
- `vercel.json` / `.vercel/**`
- Codex CLI / Claude Code config
- Any OpenAI API SDK / import / key / HTTP call
- **Any daemon / runner / scheduler / cron /
  GitHub-Actions / Codex-automation / Claude-
  automation / Playwright-harness implementation**
  — memo only.
- **Any benchmark-resume fixture file** — memo
  names them but does not create them yet.

## Risk level

`yellow`

Yellow-adjacent. Design-memo-only. Same class as
AgentOps-2b / 2c / P2.0a / P2.1a. **The subject
of the memo IS red/yellow-adjacent** (future
automation control + quality gates), but this
specific TASK adds only `.agent/` design
documentation. Blast radius: 3 `.agent/` files.

## Constraints

- **No product code edits.** No `src/**` diff.
- **No prompt edits** (`src/lib/prompts.ts`).
- **No API-route edits** (`src/app/api/**`).
- **No model selection edits.**
- **No collector run.** No corpus refresh.
- **No pipeline repo edits.**
- **No `sources.yaml` edits.**
- **No GitHub Actions edits.**
- **No `.agent/scripts/**` edits.**
- **No new implementation** of any kind —
  planner, runner, harness, benchmark fixture,
  or config file.
- **No manual `vercel deploy`.**
- **No push.**
- Memo must contain **all 16 required
  sections** from the user's spec.
- Memo must be **opinionated** with a single
  concrete top-line recommendation in §16.
- Memo must respect standing guardrails: no
  recommendation lifts BLK-0001 / BLK-0002 /
  BLK-0003; no recommendation introduces OpenAI
  API in the Q7 blocked sense; Codex CLI via
  ChatGPT sign-in remains the sanctioned Codex
  invocation channel; `.agent/scripts/**`
  remains under hard rule (Shape B implementation
  needs its own separate TASK + DECISION when
  the day comes).
- Memo must reference existing artifacts by
  `path` — AgentOps-2b memo, AgentOps-2c memo,
  policies, blockers, queue — so a future reader
  can trace decisions.

## Acceptance criteria

- [ ] Memo file exists at
      `.agent/design_memos/2026-07-08_AgentOps-3a_automation_advancement.md`
- [ ] Memo contains **all 16 required
      sections**: Goal · Current state · Target
      architecture · Automation phases · Codex
      planner responsibilities · Claude Code
      executor responsibilities · Real report
      regression harness · Quality gates · Risk
      classification · Human daily interaction
      model · Failure handling · Required
      artifacts · Permissions matrix ·
      Implementation roadmap · Open decisions ·
      Recommendation.
- [ ] §3 (Target architecture) includes a
      text-diagram of the daily flow with all
      6 actors named.
- [ ] §4 (Phases) enumerates 3a → 3f, each
      with a one-sentence outcome and an
      allowed/forbidden line.
- [ ] §7 (Regression harness) names the first
      **5 benchmark resumes** (A-E) with role
      + target pairing, and lists first-version
      minimum checks + future checks.
- [ ] §8 (Quality gates) defines when
      regression is required vs optional vs
      forbidden.
- [ ] §9 (Risk classification) gives concrete
      green / yellow / red examples matching
      `.agent/policies/agent_policy.md` §2 +
      `.agent/policies/automation_policy.md`
      §6 vocabulary.
- [ ] §13 (Permissions matrix) is a table
      with rows = {Human, ChatGPT, Codex,
      Claude Code, Playwright harness, GitHub
      Actions / Vercel} and columns covering
      Read repo / Write files / Push / Deploy
      / Approve red-zone / Run LLM calls /
      Run collector / Modify prompts / Modify
      `.agent/scripts` / Create TASK / Draft
      RUN_REPORT / Draft DECISION.
- [ ] §14 (Roadmap) proposes exactly the
      **5 next TASKs (3b → 3f)** with title,
      risk, and "not yet started" flag.
- [ ] §15 (Open decisions) lists at least
      **10 concrete yes/no questions** for
      Bohao + ChatGPT.
- [ ] §16 (Recommendation) picks **exactly
      one** top-line default next TASK and
      justifies it in ≤ 5 lines.
- [ ] Memo does NOT recommend lifting any of
      the 3 open blockers.
- [ ] Memo does NOT recommend OpenAI API in
      any Q7 blocked sense.
- [ ] Memo does NOT recommend `.agent/scripts/**`
      edits in this TASK's scope.
- [ ] Memo does NOT recommend G2.1d
      (BLK-0001).
- [ ] Memo does NOT recommend a red-zone
      task as default.
- [ ] No `src/**` files modified.
- [ ] No `src/data/**` files modified.
- [ ] No prompt files modified.
- [ ] No API-route files modified.
- [ ] No pipeline repo modification (pipeline
      HEAD = `b019786` at run start AND end).
- [ ] No collector invocation.
- [ ] No LLM call by this task.
- [ ] No `.agent/scripts/**` diff.
- [ ] No push, no manual deploy.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# structural check
wc -l .agent/design_memos/2026-07-08_AgentOps-3a_automation_advancement.md
grep -c "^## " .agent/design_memos/2026-07-08_AgentOps-3a_automation_advancement.md   # expect ≥16

# diff audit
git status
git diff --stat
git diff --name-only

# forbidden audit
git diff --name-only origin/main..HEAD | grep -E "src/|.agent/scripts/|.agent/policies/|.agent/templates/|.agent/blockers.md|.agent/automation_queue.md|.github/workflows/|package|vercel|.env" || echo "  ✓ CLEAN (forbidden empty)"

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git log --oneline -3
```

## Expected output

RUN_REPORT must include:

- Files changed (3 `.agent/` files).
- Summary of proposed automation architecture.
- Key recommendations.
- 3a → 3f phase table.
- Regression-harness concept summary.
- Quality-gate summary.
- Permissions-matrix summary.
- Open decisions list.
- Forbidden-file audit.
- Confirmation of every "no X" boundary.
- Recommendation: Human + ChatGPT review the
  memo + RUN_REPORT, then decide next task.

## Rollback plan

Both commits are local-only until push.

- Small correction → follow-up commit (P2.0b
  `e3fbbab` pattern).
- Full reject → `git reset --soft HEAD~2`.
- After push, needs revert → `git revert
  <impl_commit> <run_report_commit>`.

## Requires human approval

`no` for memo landing.

> Approved by:
> Bohao's message 2026-07-11 "Start AgentOps-3a".
> No prior DECISION file names AgentOps-3a; this
> is the first entry point of the AgentOps-3
> track. Q10 pause was scoped to AgentOps-2*;
> AgentOps-3 is a fresh strategic-scoping
> initiative and is not in violation.
>
> Push and deploy remain separate human-approval
> gates.

## Max runtime minutes

60

## Max token budget hint

`unset`

## Stop conditions

- Stop if a `src/**` file appears in `git
  status`.
- Stop if `src/data/**` appears.
- Stop if `.agent/scripts/**` appears.
- Stop if any pipeline-repo file appears.
- Stop if `package.json` / lockfile appears.
- Stop if `.agent/policies/**` /
  `.agent/templates/**` / `.agent/blockers.md`
  / `.agent/automation_queue.md` appears
  (all frozen this loop).
- Stop if the memo cannot honestly reach a
  single-default recommendation in §16.
- Stop if the memo would need to recommend
  G2.1d / OpenAI API / BLK-0002 activation /
  BLK-0003 lift / red-zone auto-execute as
  the default.
- Stop if the memo would need to introduce any
  actual runner / harness / planner / benchmark
  code.
