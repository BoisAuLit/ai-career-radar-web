# TASK · P1.7 premium detail pass

> First real TASK under the AgentOps file protocol. Replaces ad-hoc chat
> instructions for the next product-polish iteration.

## Metadata

- **task_id**: `2026-06-27_run_01`
- **date**: `2026-06-27`
- **run_number**: `01`
- **title**: P1.7 premium detail pass
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Make the AI Career Radar homepage and related product pages feel more premium,
professional, and trustworthy, **without changing product logic or pipeline
logic**. P1.6 already made the structure directionally right (Apple-style
hero, visual system consolidated). This is detail polish — not another full
redesign.

## Background

- **P1.6** consolidated the design tokens (`.eyebrow`, `.surface`, `.surface-card`,
  `.surface-hero`, `.section-y`, `.prose-body`, `.pull-quote`, `.metric`) in
  `src/app/globals.css` and made the homepage Apple-style.
- **P2.1 / P2.2** added the multi-viewport screenshot workflow, the methodology
  TOC, lab table polish, snapshot data polish, and the editorial
  markdownComponents for the generated report.
- The current bottleneck for "feels premium" is now the **mock report preview
  on the hero**, the **generator workbench layout**, the **shell around the
  streamed report**, and the **static sample-report page**. Secondary pages
  (snapshot / methodology / lab) are close to right — only light unification
  here, no rework.
- This is the first TASK going through `.agent/`. Treat the file protocol as
  load-bearing: write a real RUN_REPORT at the end, do not skip the
  template's checklists.

## Allowed files

- `src/app/page.tsx` (homepage including hero mock + generator workbench + result display)
- `src/app/sample-report/**`
- `src/app/snapshot-pipeline/**`
- `src/app/methodology/**`
- `src/app/lab/**`
- `src/app/globals.css` (token-level additions only — no removals; no token renames that would break other consumers)
- `src/components/**` (only if a component is already used by the pages above; do not add new top-level abstractions on speculation)

## Forbidden files

- `.github/workflows/**`
- `src/lib/prompts.ts`
- `src/data/web_bundle.json`
- `package.json`
- `package-lock.json`
- `.env*`
- `**/secrets*`
- Any pipeline repo files (anything under `/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar/`)
- Any classifier or extractor prompt files (in either repo)
- Any model selection files (e.g. references to `claude-sonnet-4-6`, `claude-haiku-4-5` literals — leave the existing strings as-is)

## Risk level

`yellow`

> Visual polish on production UI. Build must pass; screenshots must be reviewed;
> no red-zone file touches.

## Constraints

- Do not change API behavior (no edits to `src/app/api/**`).
- Do not change report generation logic.
- Do not change eval logic.
- Do not change model selection.
- Do not add npm dependencies.
- Do not touch the pipeline repo.
- Do not touch cron.
- Do not manually deploy.
- Do not push.

**Keep existing functionality working** (smoke-check every one of these still works after the change):

- PDF upload
- Resume textarea (paste flow)
- Target textarea (paste flow)
- Target preset buttons
- Fictional sample personas (load buttons)
- Company deep-dive `<select>`
- Privacy details `<details>`
- Generate button + busy state
- Loading state ("Classifying…" / "Streaming report…")
- Error state (raw error + categorized friendly title/hint)
- Result display (streamed markdown render via `markdownComponents`)
- Eval-report button (if present)
- Start Over button
- Nav links to `/sample-report`, `/snapshot-pipeline`, `/methodology`, `/lab`
- The three secondary pages render with their P2.1 features intact (TOC sidebar on methodology, bar fill on snapshot company breakdown, lab matrix scores, etc.)

## Acceptance criteria

- [ ] Hero mock report preview feels more like a premium product screenshot
      (better window-chrome detail, sharper hierarchy, no decorative noise that doesn't read).
- [ ] Generator section feels more like a workbench, not a raw form
      (clear primary CTA, calmer secondary controls, better grouping
      between resume / target / company-deepdive / privacy).
- [ ] Generated report presentation has a better premium shell
      (header strip, optional eval-score sash, consistent paddings; markdownComponents body unchanged).
- [ ] `/sample-report` page exists, is clearly labelled `sample · fictional`,
      and uses the same premium shell as the live report.
- [ ] `/snapshot-pipeline`, `/methodology`, `/lab` are lightly more visually consistent if safe
      (shared nav/footer chrome via `SiteChrome` if not already; otherwise leave as-is).
- [ ] `npm run build` passes.
- [ ] `npm run screenshot` produces 15/15 captures (5 pages × 3 viewports) with no obvious regressions vs the prior baseline.
- [ ] No red-zone files touched.
- [ ] No package / dependency changes (`package.json` and `package-lock.json` byte-identical to `HEAD`).
- [ ] Claude writes `.agent/run_reports/2026-06-27_run_01_RUN_REPORT.md` after execution
      following the run-report template.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web
git status
git diff --stat main..HEAD
npm run build
# Optional but recommended for visual sign-off:
npm run dev &   # in another shell, or skip if dev server already up
npm run screenshot
git status
```

## Expected output

- A new commit (or commits) on branch `agent/2026-06-27_run_01` — **not** on `main`.
- `git diff --stat main..HEAD` showing only files under `src/app/`, `src/components/`, and `src/app/globals.css`.
- `npm run build` ending with `✓ Compiled`.
- `temporary/screenshots/{desktop,tablet,mobile}/` with 15 fresh PNGs.
- `.agent/run_reports/2026-06-27_run_01_RUN_REPORT.md` filled out per template,
  including a `constraints_checked` walk-through that explicitly affirms each
  `forbidden_files` entry was untouched.

## Rollback plan

- Branch name: `agent/2026-06-27_run_01` (executor creates this; never works on `main`).
- Discard locally: `git checkout main && git branch -D agent/2026-06-27_run_01`.
- Nothing is pushed in this run, so no remote rollback needed.
- If a single commit causes a regression but others are fine, executor may
  `git revert <hash>` on the branch — do NOT amend or rebase committed history.

## Requires human approval

`no`

> Yellow task with no red-zone file touches. ChatGPT (or, in later AgentOps
> phases, the OpenAI API reviewer) signs off via the DECISION step on the
> RUN_REPORT. Bohao must explicitly approve any merge to `main` or push.

## Max runtime minutes

`90`

> Polish across 5 pages plus a new static page plus a screenshot pass.

## Max token budget hint

`unset`

## Stop conditions

Beyond the global policy (`.agent/policies/agent_policy.md` §3):

- Stop if a red-zone file appears in `git status`.
- Stop if `package.json` or `package-lock.json` would need to change.
- Stop if `src/lib/prompts.ts` would need to change.
- Stop if `src/data/web_bundle.json` would need to change.
- Stop if `npm run build` fails and the fix is not obvious (≤ 1 small targeted change).
- Stop before any push or deploy.
- Stop if the task scope grows beyond P1.7 visual/product presentation polish
  (e.g. starts becoming a refactor of API routes, a redesign of the design
  system, or a re-write of `markdownComponents` content rules).
- Stop after 3 commits on the branch and surface in RUN_REPORT before continuing.
