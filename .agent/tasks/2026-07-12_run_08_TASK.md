# TASK · AgentOps 3g stability Fixture A rerun

## Metadata

- **task_id**: `2026-07-12_run_08`
- **date**: `2026-07-12`
- **run_number**: `08`
- **title**: AgentOps-3g-stability · one Fixture A stability re-run before baseline promotion
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-3g-stability`
- **parent_loop**: `AgentOps-3g` (`2026-07-12_run_07`)
- **grandparent_loop**: `AgentOps-3e-tune-2` (`2026-07-12_run_05`)

## Objective

Run **Fixture A** once more with the **existing** local report regression
harness (`scripts/report-regression-local.mjs` @ `d393db9`) against
`localhost:3000` to test **stability** before first baseline promotion.
Primary goal is a **second data point** to confirm the AgentOps-3e-tune-2
GREEN was reproducible, not a lucky run. Do NOT modify the harness. Do NOT
promote a baseline. Do NOT create baseline files. If verdict is GREEN with
metadata comparable to the previous GREEN run, this loop clears the way for
AgentOps-3g-2 to promote Fixture A as the first official baseline.

## Background

AgentOps-3g DECISION (`.agent/decisions/2026-07-12_run_07_DECISION.md`)
explicitly recommended a stability re-run before baseline promotion (memo §17
+ open decisions 1-2). The AgentOps-3g memo §4 lists lucky-green risk as one
of six reasons not to auto-promote. This loop resolves that risk with a
second data point at ≈ $0.05.

Reference values (from `20260713T014957Z_fixture-A`):

| field | value |
|---|---|
| verdict | green |
| exit_code | 0 |
| capture_scope | main section |
| fallback_used | false |
| report_char_count | 11773 |
| page_body_char_count | 18422 |
| report_length_soft_min / max | 1500 / 14000 |
| duration_ms | 75804 |
| checks total / passed | 25 / 25 |
| corpus_snapshot_date | May 14, 2026 |
| model_display | Claude Sonnet 4.6 |

## Allowed files

- `.agent/tasks/2026-07-12_run_08_TASK.md` (this file)
- `.agent/regression_runs/<new-run-id>/metadata.json`
- `.agent/regression_runs/<new-run-id>/structural_checks.json`
- `.agent/regression_runs/<new-run-id>/verdict.md`
- `.agent/run_reports/2026-07-12_run_08_RUN_REPORT.md`

## Forbidden files

- **`scripts/report-regression-local.mjs`** (harness must not change in a
  stability loop)
- `src/**` (any file)
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `.agent/scripts/**` (hard rule per AgentOps-2c Q3-Q8)
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/regression_fixtures/**` (frozen)
- `.agent/planner_reports/**` (Codex planner still spec-only)
- **`.agent/baselines/**`** (do NOT create in this loop)
- **`.agent/regression_baselines/**`** (do NOT create in this loop)
- **`baselines/**`** (do NOT create in this loop)
- pipeline repo (any file)
- `sources.yaml`, `corpus/**`, `scripts/collector/**`
- `.github/workflows/**`
- `package.json`, `package-lock.json`
- `.env*`, `vercel.json`
- Codex config files, Claude config files
- any production deployment config
- any baseline promotion file

## Risk level

`yellow` (one real localhost generation)

## Constraints

- **Fixture A only.**
- **Localhost only.** Base URL must resolve to `localhost` / `127.0.0.1`.
- **Playwright CLI only.** No Playwright MCP.
- **One real report generation** exactly. If the first fails due to a
  harness or env glitch, at most one retry (max 2 real generations
  total).
- **No new npm dependencies.**
- **No `.env*` read** by harness.
- **No Anthropic/OpenAI HTTP call** from harness (browser driver only).
- **No `src/**` edits.**
- **No `scripts/report-regression-local.mjs` edit.**
- **No baseline promotion.** No baseline files. No baseline dirs
  created.
- **No `report.md` / `screenshot.png` committed.**
- **No production target.**
- **No push. No deploy.**
- **No Eval button click.**
- **No corpus refresh / collector run.**
- **No GitHub Actions changes.**

## Stability comparison targets (recorded manually in RUN_REPORT)

Compare new run against previous GREEN run `20260713T014957Z_fixture-A`:

- **report_char_count delta**: new − 11773
- **report_char_count percent change**: `(new−11773)/11773 * 100`
- **duration_ms delta**: new − 75804
- **duration_ms percent change**: `(new−75804)/75804 * 100`
- **within ±30%** for `report_char_count`? (yes/no)
- **within 2×** for `duration_ms`? (yes/no)
- **capture_scope stayed non-body**? (yes/no)
- **fallback_used stayed false**? (yes/no)
- **all checks stayed green**? (yes/no)

**Do NOT modify the harness to add this comparison.** Do it manually in
the RUN_REPORT prose.

## Acceptance criteria

- [ ] One real Fixture A generation via localhost happened.
- [ ] `done` state reached; incomplete banner absent (Candidate 1
      sentinel intact).
- [ ] `capture_scope != body_fallback`; `fallback_used == false`.
- [ ] All 5 required section headers + Evidence Appendix present in
      selected report text.
- [ ] Zero must-not-happen matches.
- [ ] No fatal Playwright error.
- [ ] `metadata.json`, `structural_checks.json`, `verdict.md` committed
      under `.agent/regression_runs/<new-run-id>/`.
- [ ] `report.md` and `screenshot.png` written to `os.tmpdir()` only,
      **not** committed.
- [ ] No `scripts/report-regression-local.mjs` change.
- [ ] No `src/**` change. No `.agent/scripts/**` change. No
      `.agent/regression_fixtures/**` change. No pipeline change. No
      `package.json` / lockfile change. No `.env*` read.
- [ ] No `.agent/baselines/**` / `.agent/regression_baselines/**` /
      `baselines/**` created.
- [ ] Comparison against previous GREEN recorded in RUN_REPORT.
- [ ] RUN_REPORT includes the 3f `## Regression verdict` section with
      `regression_required=yes`, verdict reflecting harness result,
      `baseline_promoted=no`, `production_target_used=no`.
- [ ] BLK-0001 / BLK-0002 / BLK-0003 still `open`.
- [ ] QUEUE-0002 G2.1d still `blocked_pending_human`.

## Expected outcomes

**Ideal**: verdict = **GREEN** · `exit_code=0` · `capture_scope=main
section` (or another non-body report-specific candidate) ·
`fallback_used=false` · `report_char_count` within ±30% of 11773 ·
`duration_ms` within 2× of 75804 · all 25 checks pass.

**Acceptable**: verdict remains GREEN with modest metadata drift within
tolerance bounds — normal LLM variance.

**Interesting** (still `approve`-eligible but worth calling out): verdict
= AMBER for a check that's known-flaky (recommendation match heuristic,
action-bar visibility jitter). RUN_REPORT must name the check and
explain.

**Blocking** (would NOT clear the way for 3g-2): verdict = RED, OR
`fallback_used=true`, OR `capture_scope=body_fallback`, OR any
must-not-happen match, OR incomplete banner visible, OR any red check
failing. In that case AgentOps-3g-2 is deferred and a fix loop opens.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# preflight
git status
git log --oneline -6
[ ! -d .agent/baselines ] && [ ! -d .agent/regression_baselines ] && [ ! -d baselines ] && echo "OK: no baseline dirs"

# dev server if not up
curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:3000 || \
  npm run dev > /tmp/dev-3g-stability.log 2>&1 &

# run harness
node scripts/report-regression-local.mjs

# validate
git diff --name-only
git diff --stat
cat .agent/regression_runs/<new-run-id>/verdict.md

# forbidden sweep
git diff --name-only | grep -E '^(src/|scripts/report-regression-local\.mjs|\.agent/scripts/|\.agent/regression_fixtures/|\.agent/planner_reports/|\.agent/baselines/|\.agent/regression_baselines/|baselines/|package(-lock)?\.json|\.github/workflows/)' || echo "OK"
```

## Expected output

RUN_REPORT must include:
- task path
- previous green run id: `20260713T014957Z_fixture-A`
- new run id (fresh UTC timestamp)
- exact harness command
- side-by-side previous vs new comparison table (chars / duration /
  scope / fallback / verdict / marker count)
- 3f Regression verdict section (all 21 fields)
- structural + fixture + operational check summaries
- stability analysis + recommendation logic:
  - clean GREEN → recommend approve + proceed to AgentOps-3g-2
  - AMBER → recommend human + ChatGPT review
  - RED → recommend do not promote; fix/revert/analyze first
- forbidden-file audit
- scratchpad paths
- confirmation of no baseline promotion / no baseline files / no
  production / no A-E

## Rollback plan

- If the harness misbehaves: rename or delete the new
  `.agent/regression_runs/<new-run-id>/` directory (uncommitted state),
  keep the TASK for the log, mark next DECISION as `request_changes`.
- No push happens in this loop, so rollback is local-only.

## Requires human approval

`yes` (yellow · one real localhost generation · $0.05)

> Approved by: `Bohao msg 2026-07-13 · "Start AgentOps-3g-stability:
> one Fixture A stability re-run before baseline promotion"` and
> `.agent/decisions/2026-07-12_run_07_DECISION.md` (explicit
> endorsement of 3g-stability as default next loop).

## Max runtime minutes

10 (harness ~2 min; dev server startup ~15 s; overhead)

## Max token budget hint

`unset`

## Stop conditions

- Stop if any forbidden file appears in `git status`.
- Stop if any baseline directory gets created.
- Stop if more than 2 real report generations happen.
- Stop if the harness attempts a non-localhost target.
- Stop if `done_state_reached` is false.
- Stop if `.agent/scripts/**` or `src/**` or
  `scripts/report-regression-local.mjs` appears in `git diff`.

## Do NOT

- Do NOT promote baseline.
- Do NOT create baseline files.
- Do NOT create `.agent/regression_baselines/` /
  `.agent/baselines/` / `baselines/`.
- Do NOT run A-E full suite.
- Do NOT run fixtures B-E.
- Do NOT test production.
- Do NOT modify `scripts/report-regression-local.mjs`.
- Do NOT modify `.agent/scripts/**`.
- Do NOT modify `src/**`.
- Do NOT modify pipeline.
- Do NOT modify prompts.
- Do NOT modify model selection.
- Do NOT modify report generation route.
- Do NOT click Eval.
- Do NOT implement Codex planner.
- Do NOT create `.agent/planner_reports/`.
- Do NOT call Anthropic/OpenAI outside the local app.
- Do NOT introduce OpenAI API.
- Do NOT run collector.
- Do NOT refresh corpus.
- Do NOT modify GitHub Actions.
- Do NOT modify `package.json` or `package-lock.json`.
- Do NOT push.
- Do NOT deploy.
- Do NOT resolve BLK-0001 / BLK-0002 / BLK-0003.
- Do NOT start G2.1d.
- Do NOT resume full automation.
