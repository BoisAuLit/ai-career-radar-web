# TASK · P2.0a data freshness corpus promotion design memo

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-05_run_04`
- **date**: `2026-07-05`
- **run_number**: `04` (zero-padded sequence within the day)
- **title**: P2.0a data freshness corpus promotion design memo
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only for
  writes; pipeline repo is read-only inspection)

## Objective

Author a design memo at
`.agent/design_memos/2026-07-05_P2.0a_data_freshness_corpus_promotion_design.md`
that scopes a safe, staged path for improving data freshness and
promoting a refreshed corpus/bundle into the web product,
**without executing the refresh**. Purpose: gate the next 3-6
loops with a written plan so a naive "swap the bundle" doesn't
regress the credibility work P1.7 + P1.8 just shipped.

**Preflight read-only inspection** already surfaced the
critical finding: the served bundle and the pipeline bundle
are **not compatible for naive swap**. Naive swap would
regress company count 35 → 8 (−77%) — a credibility
disaster. Memo must scope this.

## Background

- P1.7 + P1.8 credibility copy cluster complete at
  `origin/main = e6c72ba`. Hero/sample-report/methodology all
  now trace displayed counts to `WEB_BUNDLE_STATS`
  (443/47/35/5) via `src/lib/web-bundle-stats.ts` and
  `src/data/web_bundle.json`.
- Strategic review (2026-07-05) identified data freshness as
  the largest remaining trust risk. Bundle `generated_at =
  2026-05-14`, current 2026-07-05 → **~7.5 weeks old**.
- Automation pipeline IS running (per
  pipeline`.github/workflows/weekly-collect.yml` header —
  file is legacy-named `weekly-collect.yml` but workflow name
  is "Daily automated collection", cron `0 6 * * *`,
  concurrency group `weekly-collect` kept for stable ID). The
  cron writes only to
  `corpus/web_bundle_pipeline.json`; live
  `corpus/web_bundle.json` is untouched by design until an
  explicit F3 swap.
- Pipeline bundle produces smaller diversity than served
  bundle (see Preflight facts below) because the automated
  `sources.yaml` registry has only 8 companies while the
  older manual curation covered ~35.

## Preflight facts (read-only, cited in memo)

**Three bundles exist in the pipeline repo:**

| file | size | `generated_at` | `n_records` | unique companies | `applied_ai` |
|---|---|---|---|---|---|
| `corpus/web_bundle.json` (served copy in web repo `src/data/`) | 1.4 MB | **2026-05-14T02:17:04Z** | **443** | **35** | **47** |
| `corpus/web_bundle_pipeline.json` (automation output) | 2.1 MB | **2026-06-28T07:17:39Z** | **519** | **8** | **32** |
| `corpus/web_bundle_staging.json` (older intermediate) | 1.7 MB | 2026-05-19T03:16:12Z | 404 | 8 | 15 |

**Cron schedule**: daily 06:00 UTC per workflow file.
**Last dated automated collection commit on `main` of pipeline
repo**: `65c3f9b Daily automated collection · 2026-06-28`
(7 days ago). Cron either paused or last 7 runs did not
produce a commitable delta — open question, not blocking
the memo.

**Freshness surface today**: `/snapshot-pipeline` route on
the live site DOES show `bundle.generated_at`
(`src/app/snapshot-pipeline/page.tsx:289` via
`formatGeneratedAt`). Homepage / methodology / sample-report
do NOT surface any last-updated date.

**API routes** (frozen for this task): `src/app/api/**` — 5
routes, all runtime model / bundle consumers. Not read for
this memo; runtime behavior stays unchanged.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-07-05_run_04_TASK.md` (this file)
- `.agent/design_memos/2026-07-05_P2.0a_data_freshness_corpus_promotion_design.md`
  (NEW; the design memo deliverable)
- `.agent/run_reports/2026-07-05_run_04_RUN_REPORT.md` (NEW;
  matching RUN_REPORT)

## Read-only allowed

**Web repo (read-only, do NOT modify)**:
- `src/data/web_bundle.json`
- `src/data/web_bundle_pipeline.json` (if present in web repo;
  the main copy lives in pipeline repo)
- `src/lib/web-bundle-stats.ts`
- `src/lib/corpus.ts`, `src/lib/types.ts`
- `src/app/methodology/page.tsx`,
  `src/app/sample-report/page.tsx`, `src/app/page.tsx`,
  `src/app/snapshot-pipeline/page.tsx`

**Pipeline repo (read-only only — no `git add`, no editor,
no `>` redirect)**:
- `sources.yaml`
- `scripts/collector/**` — collector code + entry points
- `corpus/**` — bundles, snapshots, state, runs, evals
- `.github/workflows/weekly-collect.yml`
- Any docs (`README.md`, `AUTOMATED_COLLECTION_DESIGN.md`,
  `BUILD_LOG.md`, etc.) if needed to understand promotion
  path semantics

## Forbidden modifications

### Both repos

- **Pipeline repo — no file writes.** `sources.yaml`,
  `corpus/**`, `scripts/collector/**`,
  `.github/workflows/**`, `state/**`, `runs/**`,
  `web_bundle*.json` in pipeline repo — all untouched.
- **`src/data/**`** — untouched (no bundle swap, no manual
  edit).
- **`src/lib/web-bundle-stats.ts`** — untouched (constants
  frozen; refresh only via matched TASK + DECISION).
- **`src/lib/models-display.ts`** — untouched.
- **`src/lib/prompts.ts`** — untouched (prompts frozen).
- **`src/lib/anthropic.ts`** — does not exist; trivially
  satisfied.
- **`src/lib/corpus.ts`** — untouched.
- **`src/app/api/**`** — untouched. Runtime model selection
  / API-route bundle consumer logic frozen.
- **`src/app/**/page.tsx`** — untouched by this memo. Any
  UI change is a future TASK.
- **`src/components/**`** — untouched.
- **`package.json` / `package-lock.json`** — untouched.
- **`.env*`** — untouched.
- **`.github/workflows/**`** (both repos) — untouched.
- **`vercel.json` / `.vercel/**`** — untouched.
- **`.agent/policies/**`** — untouched.
- **`.agent/templates/**`** — untouched.
- **`.agent/scripts/**`** — untouched. **Hard rule** per
  AgentOps-2c DECISION Q3-Q8.
- **`.agent/automation_queue.md`** — untouched (memo does
  not auto-transition queue items).
- **`.agent/blockers.md`** — untouched.

### Process-level

- **No `python -m scripts.collector …` invocation.** No
  collector run, no `dry-run`, no `clean-preview`, no `run`,
  no `list`, no `review`.
- **No `npm run collect`** or any collector wrapper.
- **No LLM call by this task** (no `anthropic` / `openai`
  SDK invocation).
- **No new GitHub Actions / workflow / cron / hook file.**
- **No new npm / Python dependency.**
- **No OpenAI API SDK / key / `.env` entry / HTTP call**
  (per Q7 blocked sense).
- **No Codex CLI or Claude Code config mutation.**
- **No force push / history rewrite.**
- **No `git push`.**
- **No manual `vercel deploy`.**
- **No automation runner / daemon / scheduler / cron file
  creation.**

## Risk level

`yellow` (design memo).

> Future execution stages may be Yellow (visible freshness
> disclosure, build-time codegen) or Red (bundle swap,
> classifier / prompt / schema edit, sources.yaml
> expansion, deploy). Memo scopes gates; does not execute.

## Constraints

- No pipeline-file writes. All pipeline inspection is
  read-only via `head`, `grep`, `wc`, `ls`, `python3 -c
  "import json; …"`. No `>` redirect, no `git add`, no
  editor, no `sed -i`.
- No `src/data/**` writes.
- No `.agent/scripts/**` writes (hard rule).
- No collector invocation.
- No LLM call.
- Memo MUST NOT propose a "swap web_bundle.json now" plan.
  Naive swap regresses company count 35 → 8, contradicting
  the entire P1.7 + P1.8 credibility cluster.
- Memo MUST distinguish between:
  (a) visible freshness disclosure (green-adjacent, could
      ship immediately),
  (b) build-time drift check (green-adjacent, no
      pipeline touch),
  (c) preview-only pipeline bundle promotion (yellow),
  (d) production bundle swap (yellow/red, gated by
      registry expansion or hybrid strategy),
  (e) full automated production promotion (red).
- Memo MUST recommend exactly ONE next task, with a clear
  reason.
- Memo MUST include a rollback plan for any future
  production bundle swap.
- Memo MUST include concrete acceptance-gate criteria (not
  hand-wavy) for any future promotion.

## Acceptance criteria

- [ ] `.agent/tasks/2026-07-05_run_04_TASK.md` exists (this
      file).
- [ ] `.agent/design_memos/2026-07-05_P2.0a_data_freshness_corpus_promotion_design.md`
      exists with the 10 required sections (Problem, Current
      state, Source-of-truth map, Risks, Promotion path
      options, Recommended staged plan, Acceptance criteria
      for future promotion, Rollback plan, Explicit
      non-goals, Recommended next task).
- [ ] Memo cites the preflight facts above (three bundle
      files, sizes, generated_at, records, companies,
      applied_ai counts).
- [ ] Memo compares ≥5 promotion options (A no-op / B stats
      codegen / C preview-only / D production swap / E full
      automation).
- [ ] Memo defines ≥8 concrete acceptance gates for any
      future promotion (record thresholds, company
      thresholds, applied_ai sanity, duplicate rate, empty
      fields, attribution/compliance, archetype
      distribution, sample-page render, build passes,
      rollback path).
- [ ] Memo names one recommended next task (not multiple).
- [ ] Memo explicit-non-goals section includes: no G2.1d, no
      classifier prompt changes, no OpenAI API, no
      autonomous promotion, no automatic production swap, no
      pipeline file edits without separate approval, no
      `.agent/scripts` changes.
- [ ] `.agent/run_reports/2026-07-05_run_04_RUN_REPORT.md`
      exists with the full RUN_REPORT for this task.
- [ ] `git diff --stat` shows changes only under
      `.agent/tasks/`, `.agent/design_memos/`, and
      `.agent/run_reports/`.
- [ ] `src/data/**` untouched.
- [ ] `src/lib/web-bundle-stats.ts` untouched.
- [ ] `src/lib/prompts.ts`, `src/lib/corpus.ts`,
      `src/app/api/**` untouched.
- [ ] Pipeline repo untouched (verified `git status` clean +
      HEAD unchanged at `b019786`).
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created.
- [ ] No collector invocation.
- [ ] No LLM call by this task.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check
git status --short
git diff --stat

# memo structural check
wc -l .agent/design_memos/2026-07-05_P2.0a_data_freshness_corpus_promotion_design.md
grep -c '^## ' .agent/design_memos/2026-07-05_P2.0a_data_freshness_corpus_promotion_design.md

# forbidden audit
git diff --stat HEAD -- \
    src/data/ src/lib/web-bundle-stats.ts \
    src/lib/models-display.ts src/lib/prompts.ts \
    src/lib/corpus.ts src/app/api/ src/app/**/page.tsx \
    src/components/ .agent/policies/ .agent/templates/ \
    .agent/scripts/ .agent/blockers.md \
    .agent/automation_queue.md .github/workflows/ \
    package.json package-lock.json 2>&1 || true

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

- Exact paths of TASK, memo, RUN_REPORT.
- Exact commit hashes for `(TASK + memo)` commit and RUN_REPORT
  commit.
- Memo line count (target 400-800; hard cap 1200).
- Memo H2 section count = **10** (matching required sections).
- Confirmation that pipeline HEAD = `b019786` unchanged.

## Rollback plan

- Yellow `.agent/`-only doc work; direct on `main` follows the
  established pattern.
- If DECISION requests changes: edit memo in a follow-up
  commit; do not amend.
- If full revert: `git revert <run_report_commit> <memo_commit>`.

## Requires human approval

`no`

> Yellow memo-only work. Push (after matching DECISION) is a
> separate human gate.

## Max runtime minutes

`90` — memo authoring bulk.

## Max token budget hint

`unset` — advisory.

## Stop conditions

- Stop if any pipeline-repo path appears in `git status` of
  the pipeline repo (must remain clean throughout).
- Stop if `src/data/**` appears in web repo `git status`.
- Stop if `.agent/scripts/**` appears in `git status` (hard
  rule).
- Stop if `src/app/api/**`, `src/lib/prompts.ts`, or
  `src/lib/web-bundle-stats.ts` appears in `git status`.
- Stop if collector is invoked.
- Stop if memo `wc -l` exceeds 1200.
- Stop if memo section count is less than 10.
- Stop if memo does NOT recommend exactly one next task.
- Stop if a DECISION file appears in `git status`.
- Stop if the memo proposes a naive
  `web_bundle.json ← web_bundle_pipeline.json` swap as the
  recommended next step (regresses company count 35 → 8).
