# TASK · AgentOps-2c supervised runner dry-run design memo

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-06-30_run_01`
- **date**: `2026-06-30`
- **run_number**: `01` (zero-padded sequence within the day)
- **title**: AgentOps-2c supervised runner dry-run design memo
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only;
  pipeline repo is read-only sanity)

## Objective

Author a design memo at
`.agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md`
that scopes a **local supervised runner dry-run** under Option B
(chosen in AgentOps-2b DECISION Q1). The memo describes a
non-executing or minimally-supervised dry-run that can read
policy / queue / blockers and produce an automation window
report with Executive Digest — **without** executing code,
pushing, deploying, calling OpenAI API, starting Claude Code
automatically, or modifying repos outside the allowed `.agent/`
doc surface.

**This is a memo only.** No runner / daemon / scheduler / cron /
GitHub Actions / executable script / Codex CLI config / Claude
Code config / OpenAI API / app code / pipeline-repo file is
created or modified. The memo describes future dry-run behavior;
it does not enable it.

## Background

- AgentOps-2b (`baf2781`) shipped the parent design memo that
  scopes the future Codex CLI + Claude Code automation runner
  and recorded two load-bearing decisions:
  - **Q1 = Option B** (Local supervised runner, dry-run only)
    as the next automation-infra direction — NOT
    implementation approval.
  - **Q7 = Codex CLI via ChatGPT sign-in** does NOT trigger
    BLK-0003; OpenAI API usage in key / SDK / HTTP /
    automation-token / CI-secret / import / background-token
    senses remains blocked.
- QUEUE-0008 (added in `f985752`) named this AgentOps-2c
  design memo as the natural follow-up, with status
  `blocked_pending_human`. Bohao has now explicitly approved
  starting this TASK; the queue can transition
  `blocked_pending_human` → `in_review` in this task's queue
  edit.
- BLK-0002 (full automation activation) explicitly remains
  `open` and is gated on this memo PLUS a separate future
  implementation TASK + DECISION PLUS at least one supervised
  real Automation Window. The memo MUST NOT lift BLK-0002.
- The MANUAL_DRY_RUN (`475b116`) proved the *reporting*
  contract; AgentOps-2b (`baf2781`) scoped the *execution*
  contract at a high level; AgentOps-2c narrows the
  execution contract to Option B's specific dry-run shape.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-06-30_run_01_TASK.md` (this file)
- `.agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md`
  (NEW; the deliverable)
- `.agent/run_reports/2026-06-30_run_01_RUN_REPORT.md` (NEW;
  matching RUN_REPORT)
- `.agent/automation_queue.md` — only to transition QUEUE-0008
  `blocked_pending_human` → `in_review`; do NOT lift, mark
  `done`, or transition any other queue item.
- `.agent/blockers.md` — only if a genuinely NEW blocker is
  discovered by writing the memo. Do NOT resolve / lift any
  existing blocker.

## Forbidden files

### Web repo

- `.agent/scripts/**` — **NO edits to any executable helper.**
  This is a hard rule for AgentOps-2c because Option B
  discussions might tempt someone to prototype early. Memo
  only.
- `src/**` (all app code)
- `src/lib/prompts.ts`, `src/lib/anthropic.ts`
- `src/data/web_bundle.json`
- `package.json` / `package-lock.json`
- `.env*`
- `.github/workflows/**`
- `vercel.json` / `.vercel/**`
- `.agent/policies/**` (policy stays at v1.1; memo cites but
  does NOT amend)
- `.agent/templates/**` (templates frozen; memo cites the
  Executive Digest contract but does NOT amend any template)
- `.agent/README.md` (no link edit)
- `.agent/decisions/**` (DECISION is downstream)
- `.agent/automation_runs/**` (existing MANUAL_DRY_RUN report
  stays as historical record; no NEW automation run report
  authored by AgentOps-2c — that would be MANUAL_DRY_RUN-2 or
  the first real dry-run, both separate tasks)
- `.agent/daily_summaries/**` (today's summary either not yet
  written or was written for the AgentOps-2b promotion; either
  way not in this TASK's scope)
- `.agent/design_memos/**` OTHER than the AgentOps-2c memo
  path listed in Allowed files (existing G2.1a and AgentOps-2b
  memos are frozen historical records)
- **Any actual runner / daemon / scheduler / cron / orchestrator
  / executable file**: no `runner.py`, `dry_run.py`,
  `planner.sh`, `runner.ts`, `daemon.*`, `orchestrate.*`,
  `automation_loop.*`, etc. — neither under
  `.agent/scripts/` nor anywhere else. Memo describes;
  nothing executes.
- `.claude/**`, `~/.claude/**`, `~/.codex/**` (Codex/Claude
  config)

### Pipeline repo

- **All files**. Pipeline read-only. At most one read-only
  `git status` / `git log` / `git rev-parse HEAD` check.

### Process-level

- No new GitHub Actions / workflow / cron job
- No new deployment hooks
- No new npm / Python dependencies
- No OpenAI API SDK / key / `.env` entry / HTTP call
  (per Q7 blocked sense)
- No `python -m scripts.collector …` invocation
- No `npm run …` invocation
- No LLM call by this task
- No Codex CLI config or Claude Code config mutation
- No force push / history rewrite
- No `git push`
- No manual `vercel deploy`
- No executable file / shell script / config that enables
  automatic execution

## Risk level

`yellow`

> Same reasoning as AgentOps-2b: the memo may shape future
> automation behavior. Implementation is documentation-only;
> no executable code created. Yellow matches the AgentOps-2a /
> AgentOps-2b / Executive Digest precedent for
> `.agent/design_memos/` and `.agent/policies/` edits.

## Constraints

- No automation runner / daemon / scheduler / cron / GitHub
  Actions edit / Codex CLI config edit / Claude Code config
  edit / OpenAI API integration / new dependency.
- No `.agent/scripts/**` edits — **hard rule**.
- No `python -m scripts.collector …` / `npm run …` invocation.
- No LLM call by this task.
- No prompt / classifier / extractor / schema / model
  selection edits.
- No deploy. No `git push`. No pipeline-repo file edited.
- The memo is `status: draft_for_human_chatgpt_review` —
  authored, not adopted. Any *implementation* of the
  dry-run scoped by this memo is a separate downstream
  TASK + DECISION.
- The memo MUST scope only Option B (Local supervised runner,
  dry-run only). Options A / C / D are out of scope for
  AgentOps-2c; they were compared in AgentOps-2b.
- The memo MUST recommend that the next step is a
  **non-executing report-only dry-run** as the safest first
  experiment. NOT a script that invokes Codex / Claude
  automatically.
- The memo MUST cite Q1 (Option B) and Q7 (Codex CLI vs
  BLK-0003) from the AgentOps-2b DECISION and honor them.
- The memo MUST NOT lift or resolve BLK-0001 / BLK-0002 /
  BLK-0003. QUEUE-0002 G2.1d must stay
  `blocked_pending_human`.
- The memo MUST state that full automation remains inactive,
  OpenAI API remains blocked (in Q7 blocked sense), and
  G2.1d remains blocked.
- The memo MUST reference the Executive Digest contract from
  `automation_window_report_template.md` and require any
  future dry-run to emit a report starting with that digest.
- If `.agent/automation_queue.md` is updated, ONLY QUEUE-0008
  status may transition `blocked_pending_human` → `in_review`.
  Do NOT mark `done` (that's for the DECISION cleanup step).
  Do NOT add a queue item for the AgentOps-2c-derived runner
  implementation — that only exists after AgentOps-2c is
  approved and its own follow-up TASK is drafted.
- If `.agent/blockers.md` is touched, it can ONLY be to ADD
  a new blocker if a genuine one surfaces; do NOT resolve /
  close any existing blocker.

## Acceptance criteria

- [ ] `.agent/tasks/2026-06-30_run_01_TASK.md` exists (this file).
- [ ] `.agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md`
      exists with the 17 listed sections.
- [ ] Memo `Status` field reads `draft_for_human_chatgpt_review`.
- [ ] Memo `Scope` field reads `design only`.
- [ ] Memo `Non-goal` field explicitly says no runner
      implementation.
- [ ] Memo scopes ONLY Option B (Local supervised runner,
      dry-run only). Not a re-hash of A / C / D.
- [ ] Memo defines the non-executing dry-run flow (steps
      1-10 per the objective).
- [ ] Memo defines what the dry-run is allowed to read.
- [ ] Memo defines what the dry-run is allowed to write.
- [ ] Memo defines what the dry-run must never execute.
- [ ] Memo defines expected input files.
- [ ] Memo defines expected output files (report path
      convention).
- [ ] Memo defines how Executive Digest must be used.
- [ ] Memo defines queue selection logic without executing
      items.
- [ ] Memo defines blocker handling (read-only, may add,
      cannot resolve).
- [ ] Memo defines Human + ChatGPT review process after the
      dry-run.
- [ ] Memo defines exact stop conditions.
- [ ] Memo defines failure modes and mitigations.
- [ ] Memo compares ≥2 candidate implementation shapes
      (e.g. pure manual procedure / non-executing local
      planner) with the safest recommended first.
- [ ] Memo recommends a first experiment that is
      non-executing, report-only, manually invoked,
      commit-optional, no push, no schedule, requires
      another TASK + DECISION before any implementation.
- [ ] Memo lists ≥6-10 decision questions for Human +
      ChatGPT.
- [ ] `git diff --stat` shows changes only under
      `.agent/tasks/`, `.agent/design_memos/`,
      `.agent/run_reports/`, and optionally
      `.agent/automation_queue.md` / `.agent/blockers.md`.
      No other path.
- [ ] `.agent/scripts/**` NOT touched.
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created.
- [ ] No executable file created anywhere.
- [ ] QUEUE-0008 status transitioned to `in_review` (NOT
      `done`).
- [ ] BLK-0001 / BLK-0002 / BLK-0003 all still `open` at end
      of task; QUEUE-0002 G2.1d still `blocked_pending_human`.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check
git status --short
git diff --stat

# memo exists + reasonable size
ls -l .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md
wc -l .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md
grep -c '^## ' .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md

# explicit strings must appear
grep -c 'design only' .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md
grep -c 'draft_for_human_chatgpt_review' .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md
grep -c 'Option B' .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md
grep -c 'BLK-0001\|BLK-0002\|BLK-0003' .agent/design_memos/2026-06-30_AgentOps-2c_supervised_runner_dry_run_design.md

# hard rule: no .agent/scripts changes
git diff --stat HEAD -- .agent/scripts/

# forbidden audit
git diff --stat HEAD -- src/ src/lib/prompts.ts src/lib/anthropic.ts \
    src/data/web_bundle.json package.json package-lock.json \
    .github/workflows/ vercel.json .vercel/ \
    .agent/policies/ .agent/templates/ .agent/README.md \
    .agent/automation_runs/ .agent/decisions/ \
    .agent/daily_summaries/ 2>&1 || true

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

- Exact paths of files touched (TASK + memo + RUN_REPORT;
  optionally queue).
- Exact commit hashes for "TASK + memo (+ queue)" and
  RUN_REPORT commits.
- Memo line count (target 500–900; hard stop 1200).
- Memo H2 section count ≥ 17.
- Pipeline HEAD = `b019786` unchanged.

## Rollback plan

- Yellow `.agent/` doc work, direct on `main` (same pattern
  as AgentOps-2a / MANUAL_DRY_RUN / Executive Digest /
  AgentOps-2b).
- Follow-up commit if DECISION requests changes; `git revert`
  if full rollback requested; never `git reset --hard` on
  pushed commits.
- Memo is `draft_for_human_chatgpt_review` until DECISION
  approves it; even after approval, the memo does not cause
  anything to execute.

## Requires human approval

`no`

> Human already approved starting AgentOps-2c in this turn's
> instruction. Yellow `.agent/`-only memo work, no red-zone
> file. Push (after matching DECISION) is a separate human
> approval per global policy.

## Max runtime minutes

`90` — memo authoring bulk.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

- Stop if any path under `.agent/scripts/`, `src/`,
  `package*.json`, `.env*`, `vercel.json`, `.vercel/`,
  `.github/workflows/`, or any pipeline-repo file appears in
  `git status`.
- Stop if any executable file / shell script / config file
  appears in `git status`.
- Stop if memo line count exceeds 1200.
- Stop if memo `Status` is not `draft_for_human_chatgpt_review`
  at end of run.
- Stop if any of BLK-0001 / BLK-0002 / BLK-0003 marked
  resolved.
- Stop if a queue item for "AgentOps-2c runner" (not the
  memo) is added or activated.
- Stop if QUEUE-0008 marked `done` in this task (reserved
  for post-DECISION cleanup).
- Stop if more than 5 files would be committed across this
  task (allowed: 1 TASK + 1 memo + 1 RUN_REPORT + optional
  1 queue + optional 1 blocker = max 5).
- Stop if a DECISION file appears in `git status`.
