# TASK · AgentOps-3d report regression harness design memo

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-12_run_02`
- **date**: `2026-07-12`
- **run_number**: `02` (3c was `run_01`)
- **title**: AgentOps-3d report regression
  harness design memo
- **repo**:
  `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Design — without implementing — the **report
regression harness** that will eventually
automate real AI Career Radar report
generation + evaluation. The memo pins how
the AgentOps-3c fixtures A-E are consumed,
Playwright CLI vs MCP recommendation,
local-first target strategy, artifact
storage, baseline policy, structural + rubric
+ operational checks, verdict algorithm,
thresholds / budgets, nondeterminism
handling, and integration with the future
Codex planner + Claude Code executor. No
harness code, no Playwright run, no report
generation, no baseline artifact, no
screenshot creation, no `.agent/scripts/**`
edit, no `src/**` edit.

## Background

- **AgentOps-3a memo** pinned the phased
  3a→3f rollout and named the harness as
  a required quality gate.
- **AgentOps-3b DECISION** endorsed 3d as
  the natural next code loop after 3c and
  pinned "3d must remain a separate loop,
  not bundled with 3c push".
- **AgentOps-3c** shipped the 5 synthetic
  benchmark resume fixtures A-E under
  `.agent/regression_fixtures/` (2026-07-12
  DECISION `b284831`). 3d designs how the
  harness consumes them.
- **Product state**: Candidate 1 stream
  sentinel + Candidate 4 empty-PDF gate
  live on `origin/main` (`685769c`). E2E
  smoke test 2026-07-08 established the
  Playwright-driven baseline pattern the
  harness will follow.
- **Standing guardrails**: BLK-0001 /
  BLK-0002 / BLK-0003 all `open`;
  automation-infra paused (Q10);
  `.agent/scripts/**` hard rule per
  AgentOps-2c Q3-Q8; Codex CLI via
  ChatGPT sign-in remains sanctioned per
  AgentOps-2b Q7; OpenAI API blocked in
  Q7-blocked senses.

## Allowed files

- `.agent/tasks/2026-07-12_run_02_TASK.md`
  (this file)
- `.agent/design_memos/2026-07-12_AgentOps-3d_report_regression_harness_design.md`
  (new memo)
- `.agent/run_reports/2026-07-12_run_02_RUN_REPORT.md`

## Read-only allowed

- `.agent/design_memos/**`
- `.agent/daily_summaries/**`
- `.agent/regression_fixtures/**`
- `.agent/tasks/**`
- `.agent/run_reports/**`
- `.agent/decisions/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/README.md`
- `.agent/policies/**`
- `.agent/templates/**`
- `package.json`
- `src/app/page.tsx`
- `src/app/api/generate-report/route.ts`
- `src/app/api/eval-report/route.ts`
- `src/lib/eval-report.ts`
- Pipeline repo `git status` /
  `git log --oneline` for read-only
  sanity only.

## Forbidden files

- `src/**` any file
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `package.json` / `package-lock.json`
- `.github/workflows/**`
- `.agent/scripts/**` — hard rule.
- `.agent/policies/**`
- `.agent/templates/**`
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/regression_fixtures/**`
- `.agent/planner_reports/**`
- `.agent/regression_runs/**` (must NOT
  be created)
- Pipeline repo any file
- `sources.yaml` (pipeline)
- `corpus/**` (pipeline)
- `scripts/collector/**` (pipeline)
- `.env*`
- `vercel.json` / `.vercel/**`
- Codex CLI / Claude Code config
- Any planner / runner / daemon /
  scheduler / Playwright / harness code
- Any report generation
- Any baseline report artifact
- Any screenshot artifact
- Any OpenAI / Anthropic API call

## Risk level

`yellow`

Yellow because the memo scopes a
future quality gate that will
eventually gate red-zone product
changes. Subject is red-adjacent but
this TASK adds only `.agent/`-only
design documentation. Blast radius: 3
`.agent/` files.

## Constraints

- No product code edits.
- No prompt edits.
- No API-route edits.
- No model-selection edits.
- No collector run.
- No corpus refresh.
- No pipeline edits.
- No `.agent/scripts/**` edit.
- No new implementation of any kind.
- **No Playwright invocation** —
  neither `npm run screenshot` nor
  `npx playwright` in any form.
- **No report generation** — no
  `curl` / `fetch` against
  `/api/generate-report`.
- **No baseline artifact** — no
  `report.md`, `report.png`,
  `metadata.json`, `verdict.md`
  written anywhere.
- **No screenshot** created.
- **No `.agent/regression_runs/**`
  directory** created.
- No manual `vercel deploy`.
- No push.
- Memo must contain **all 22 required
  sections** from the user's spec.
- Memo must be opinionated with a
  single concrete top-line
  recommendation in §22.
- Memo must respect standing
  guardrails: no recommendation lifts
  BLK-0001 / 2 / 3; no OpenAI API in
  Q7-blocked senses; no
  `.agent/scripts/**` edits; no
  fixture edits; Codex remains
  read-only in v1.
- Memo must cite the AgentOps-3a
  DECISION 13 answers and
  AgentOps-3b DECISION 10 answers by
  number where they constrain 3d.

## Acceptance criteria

- [ ] Memo file exists at
      `.agent/design_memos/2026-07-12_AgentOps-3d_report_regression_harness_design.md`.
- [ ] Memo contains all 22 required
      sections in order: Purpose ·
      Current state · Design
      principles · Fixture usage ·
      Playwright CLI vs Playwright
      MCP · Target environment
      strategy · Future harness
      flow · Artifact storage design
      · Baseline policy · Checks v1
      · Checks deferred · Verdict
      algorithm · Thresholds and
      budgets · Nondeterminism
      handling · Integration with
      Codex planner · Integration
      with Claude Code · When
      regression is required ·
      Failure handling · Security
      and privacy · Implementation
      roadmap after 3d · Open
      decisions · Recommendation.
- [ ] §4 explains how each of
      fixtures A-E is used and pins
      the v1-prototype-one-fixture
      rule per AgentOps-3a DECISION
      answer #4.
- [ ] §5 recommends **Playwright
      CLI for v1**, defers MCP.
- [ ] §6 defines 3 target-
      environment stages (local
      dev → local preview build →
      production smoke).
- [ ] §7 lists the 18-step future
      harness flow (preflight →
      target-env confirm → app
      start → fixture load →
      resume fill → target fill →
      generate → wait for done →
      incomplete-banner detect →
      markdown capture →
      screenshot capture →
      metadata capture → optional
      eval click → structural
      checks → rubric checks →
      baseline compare → verdict
      → artifact summary + stop).
- [ ] §8 pins the
      `.agent/regression_runs/`
      layout + committed-vs-local
      artifact policy.
- [ ] §9 pins baseline promotion
      through TASK + RUN_REPORT +
      DECISION only.
- [ ] §10 lists v1 checks in 3
      buckets (structural /
      fixture-specific /
      operational).
- [ ] §11 lists deferred checks
      with reasons.
- [ ] §12 defines green /
      amber / red verdict
      criteria.
- [ ] §13 pins thresholds
      (start with 1 fixture,
      $25/mo budget, $0.25 per-
      run cap, 120s soft / 240s
      hard latency).
- [ ] §14 explains
      nondeterminism strategy.
- [ ] §15 integrates verdict
      into Codex planner (per
      AgentOps-3b §10 5 states).
- [ ] §16 integrates into
      Claude Code execution
      boundary.
- [ ] §17 pins when regression
      is required / optional /
      insufficient-alone.
- [ ] §18 defines failure
      handling with §11
      incident-note reference.
- [ ] §19 pins security +
      privacy (synthetic
      fixtures only, no secrets,
      no `.env` reading, no
      production automation
      without approval).
- [ ] §20 stages 3e / 3f /
      later.
- [ ] §21 lists ≥12 open
      decisions.
- [ ] §22 makes a single
      opinionated
      recommendation (approve
      + next loop = 3e).
- [ ] Memo does NOT recommend
      lifting any of the 3 open
      blockers.
- [ ] Memo does NOT recommend
      OpenAI API in Q7-blocked
      senses.
- [ ] Memo does NOT recommend
      `.agent/scripts/**`
      edits.
- [ ] Memo does NOT recommend
      G2.1d.
- [ ] Memo does NOT create a
      standalone regression run
      artifact.
- [ ] No `src/**` files
      modified.
- [ ] No `src/data/**` files
      modified.
- [ ] No prompt / API-route /
      model files modified.
- [ ] No pipeline repo
      modification (HEAD
      `b019786` at start AND
      end).
- [ ] No collector invocation.
- [ ] No Playwright run.
- [ ] No report generation.
- [ ] No LLM / API call.
- [ ] No baseline artifact
      created.
- [ ] No screenshot created.
- [ ] No `.agent/regression_runs/`
      created.
- [ ] No `.agent/scripts/**`
      diff.
- [ ] No push, no manual
      deploy.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# structural check
wc -l .agent/design_memos/2026-07-12_AgentOps-3d_report_regression_harness_design.md
grep -c "^## " .agent/design_memos/2026-07-12_AgentOps-3d_report_regression_harness_design.md
# expect ≥22 (22 required + Status)

# diff audit
git status
git diff --stat
git diff --name-only

# forbidden audit
git diff --name-only origin/main..HEAD | grep -E "src/|.agent/scripts/|.agent/policies/|.agent/templates/|.agent/blockers.md|.agent/automation_queue.md|.agent/regression_fixtures/|.agent/planner_reports/|.agent/regression_runs/|.github/workflows/|package|vercel|.env" || echo "  ✓ CLEAN (forbidden empty)"

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git log --oneline -3
```

## Expected output

RUN_REPORT must include:

- Files changed (3 `.agent/` files).
- Summary of regression harness
  design.
- Playwright CLI vs MCP
  recommendation.
- Local vs production strategy.
- Fixture usage A-E.
- Artifact storage proposal.
- Baseline policy.
- v1 checks list.
- Deferred checks list.
- Verdict algorithm.
- Thresholds / budgets.
- Nondeterminism handling.
- Codex planner integration.
- Claude Code integration.
- Open decisions.
- Validation results.
- Forbidden-file audit.
- Confirmation: no implementation,
  no Playwright, no report
  generation, no LLM/API call, no
  baseline artifact, no
  screenshot, no
  `.agent/regression_runs/`, no
  `.agent/scripts/**` change, no
  `src/**` change, no pipeline
  change, no push/deploy.

## Rollback plan

- Small correction → follow-up
  commit.
- Full reject → `git reset --soft
  HEAD~2`.
- After push, needs revert →
  `git revert <impl_commit>
  <run_report_commit>`.

## Requires human approval

`no` for memo landing.

> Approved by:
> `.agent/decisions/2026-07-12_run_01_DECISION.md`
> (AgentOps-3c DECISION named 3d as
> default next code loop).
> Bohao's message 2026-07-12
> "Start AgentOps-3d" reconfirmed.
>
> Push and deploy remain separate
> human-approval gates.

## Max runtime minutes

60

## Max token budget hint

`unset`

## Stop conditions

- Stop if any `src/**` file appears
  in `git status`.
- Stop if `.agent/scripts/**`
  appears.
- Stop if `.agent/regression_fixtures/**`
  appears (frozen this loop).
- Stop if `.agent/regression_runs/`
  is created (memo only).
- Stop if any pipeline-repo file
  appears.
- Stop if `package.json` /
  lockfile appears.
- Stop if the memo would need to
  implement Playwright / report-
  generation / baseline capture.
- Stop if the memo cannot honestly
  reach a single-default
  recommendation in §22.
