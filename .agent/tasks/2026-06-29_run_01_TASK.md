# TASK · QUEUE-0006 MANUAL_DRY_RUN automation window report

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-06-29_run_01`
- **date**: `2026-06-29`
- **run_number**: `01` (zero-padded sequence within the day)
- **title**: QUEUE-0006 MANUAL_DRY_RUN automation window report
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only; pipeline repo is read-only sanity)

## Objective

Hand-author a single example **automation window report** at
`.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md` that follows
`.agent/templates/automation_window_report_template.md` and simulates a
reviewable automation window using the most recent completed work
(G2.1c closure + AgentOps-2a policy promotion + cleanup commit `1fd3c8d`).
The report must be useful enough for Human + ChatGPT to review and answer
"is the AgentOps-2a contract usable in practice?" — **without** any
runner / daemon / scheduler / cron actually existing or running.

## Background

- AgentOps-2a (policy / docs only) was promoted to `origin/main` of the
  web repo at `f1bab0f` (impl `798bf69` + RUN_REPORT `de662a3` +
  DECISION `f1bab0f`). Cleanup commit `1fd3c8d` updated the daily
  summary + marked `QUEUE-0001` as `done` in
  `.agent/automation_queue.md`.
- `QUEUE-0006` was seeded in `.agent/automation_queue.md` as a green,
  web-only `candidate` whose entire job is to stress-test the contract
  end-to-end before any real Automation Window opens. See
  `.agent/policies/automation_policy.md` §6 (Green) and §7 (per-window
  report policy), and `.agent/blockers.md` BLK-0002 (full automation
  activation is explicitly blocked until at least one MANUAL_DRY_RUN
  has been reviewed).
- No runner / daemon / scheduler / cron exists in the repo. The
  automation runner is **AgentOps-2b**, which is unscoped, unapproved,
  and explicitly downstream of this MANUAL_DRY_RUN review per the
  AgentOps-2a DECISION (`f1bab0f`).
- The two pieces of "real work" the report should simulate already
  shipped end-to-end with human DECISION and human-approved push, so
  the report has real commit hashes / file paths / validation results
  to cite. No new work is performed by this task; the report
  *summarizes* what already shipped.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK. If a path is not on this
> list AND not on `forbidden_files`, executor must ask before editing.

- `.agent/tasks/2026-06-29_run_01_TASK.md` (this file)
- `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md` (NEW;
  the deliverable)
- `.agent/run_reports/2026-06-29_run_01_RUN_REPORT.md` (NEW; matching
  RUN_REPORT for this TASK)
- `.agent/automation_queue.md` — only if needed to record QUEUE-0006
  progress (e.g. `candidate` → `in_review`). Do NOT mark `done` from
  this task; the DECISION on this task is what closes QUEUE-0006.
- `.agent/blockers.md` — only if a genuine new blocker is discovered
  while writing the report. Do NOT invent unnecessary blockers.

## Forbidden files

> Paths the executor MUST NOT edit, even if it seems useful. Glob patterns OK.

### Web repo

- `src/**` (all app code)
- `src/lib/prompts.ts`
- `src/lib/anthropic.ts`
- `src/data/web_bundle.json`
- `package.json`
- `package-lock.json`
- `.env*`
- `.github/workflows/**`
- `vercel.json`
- `.vercel/**`
- `.agent/policies/**` (policy is frozen by AgentOps-2a; no edit here)
- `.agent/templates/**` (template is frozen by AgentOps-2a; no edit here)
- `.agent/README.md` (no link edits this task)
- `.agent/decisions/**` (DECISION is a separate, downstream step; not
  written in this TASK)
- `.agent/scripts/**` (helper scripts are frozen for this task; only
  used to scaffold)
- `.claude/**`, `~/.claude/**`, `~/.codex/**` (Codex/Claude config)

### Pipeline repo

- **All files**. Pipeline repo is read-only for this task. At most one
  read-only `git status` / `git log` check is allowed. No `cd
  /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar` for write purposes.

### Process-level

- No new GitHub Actions / workflow / cron job
- No new deployment hooks
- No new npm dependencies
- No new Python dependencies
- No OpenAI API SDK / key / `.env` entry / HTTP call
- No `python -m scripts.collector …` invocation
- No `npm run …` invocation (no build needed)
- No LLM call by this task (the report *describes* prior LLM-free
  work, does not perform any)
- No Codex CLI config or Claude Code config mutation
- No automation runner / daemon / scheduler / cron file creation
- No force push / history rewrite / destructive reset
- No `git push`
- No manual `vercel deploy`

## Risk level

`green`

> Per `.agent/policies/automation_policy.md` §6 (Green), this task only
> touches `.agent/automation_runs/`, `.agent/tasks/`,
> `.agent/run_reports/`, and (optionally) `.agent/automation_queue.md`.
> No app code, no prompt, no schema, no pipeline file, no
> deploy/cron/workflow. Green is appropriate. Push is still gated on
> human approval per global policy.

## Constraints

> Hard rules for this specific task. Anything beyond the global policy.

- No automation runner / daemon / scheduler / cron is created.
- No GitHub Actions edit.
- No Codex CLI or Claude Code config edit.
- No OpenAI API SDK / key / `.env` entry / HTTP call introduced.
- No new dependency (npm or Python).
- No `python -m scripts.collector …` invocation.
- No `npm run …` invocation.
- No LLM call performed by this task.
- No prompt / classifier / extractor / schema / model selection edits.
- No deploy.
- No `git push`.
- No pipeline repo file edited. At most one read-only sanity check.
- No invented blocker — only record a new blocker if a real one is
  discovered.
- QUEUE-0001 (already `done`) must not be reopened.
- QUEUE-0002 G2.1d must remain `blocked_pending_human` (red).
- BLK-0001 / BLK-0002 / BLK-0003 must remain `open`. This task lifts
  zero blockers.
- The MANUAL_DRY_RUN report's `status` field MUST be
  `ready_for_review` at end of this task — NOT `reviewed_approved`,
  because human + ChatGPT review has not happened yet.
- The MANUAL_DRY_RUN report MUST follow the section order of
  `.agent/templates/automation_window_report_template.md`. No
  section omitted; `none` / `n/a` is the correct fill for sections
  that don't apply.

## Acceptance criteria

> Checklist that the RUN_REPORT must demonstrate as satisfied.

- [ ] `.agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md` exists.
- [ ] Report's Metadata block matches: `report_id:
      2026-06-29_MANUAL_DRY_RUN`, `date: 2026-06-29`,
      `window_name: MANUAL_DRY_RUN`, `window_type: manual_dry_run`,
      `generated_by: human_manual` (Claude Code, following Human +
      ChatGPT instruction), `intended reviewer: Human + ChatGPT Chat`,
      `status: ready_for_review`.
- [ ] Every section from the template is present in the same order.
- [ ] Report cites the real recent commits (G2.1b `b0b12ff` web /
      `f833bfd` pipeline; G2.1c `1668807` web / `b019786` pipeline;
      AgentOps-2a `798bf69` / `de662a3` / `f1bab0f` web; cleanup
      `1fd3c8d` web). All hashes copy-pasteable.
- [ ] Report explicitly answers the 10 review questions in the TASK
      ("What changed recently?" through "What must NOT happen yet?").
- [ ] Report explicitly states: full automation NOT active, AgentOps-2b
      runner NOT approved, G2.1d still `blocked_pending_human`,
      OpenAI API remains blocked, this report does not execute code or
      automation, QUEUE-0006 is a report dry-run only.
- [ ] Safety audit checklist: 10/10 boxes checked (no red-zone /
      forbidden / runner / OpenAI / GH Actions / new deps / force push /
      deploy / daemon).
- [ ] `git diff --stat` shows changes only under `.agent/` —
      specifically the 3 allowed files (TASK + report + RUN_REPORT) and
      optionally `automation_queue.md`. No other path.
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created in this task. DECISION is a separate
      downstream step gated on human + ChatGPT review.

## Validation commands

> Exact commands the executor should run to validate. Copy-paste-able.

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check — must show only allowed paths
git status --short
git diff --stat

# report exists + is non-empty + section count sane
ls -l .agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md
wc -l .agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md
grep -c '^## ' .agent/automation_runs/2026-06-29_MANUAL_DRY_RUN_REPORT.md

# forbidden audit — all of these must show no diff
git diff --stat HEAD -- src/ src/lib/prompts.ts src/lib/anthropic.ts \
    src/data/web_bundle.json package.json package-lock.json \
    .github/workflows/ vercel.json .vercel/ .env || true

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

Beyond the standard RUN_REPORT template:

- Exact paths of the 3 created files (TASK already created by helper;
  report; RUN_REPORT).
- Exact commit hash for the "TASK + report" commit (created in this
  task) and the RUN_REPORT commit (created in this task).
- Line count of the MANUAL_DRY_RUN report (rough sanity).
- Section count (`grep -c '^## '`) of the MANUAL_DRY_RUN report ≥ 17
  (mirrors the template's section count).
- Confirmation that pipeline `git rev-parse HEAD` is unchanged at
  `b019786` between TASK start and RUN_REPORT commit.
- Final RUN_REPORT must explicitly state `requires_human_decision:
  yes` and recommend Human + ChatGPT review the MANUAL_DRY_RUN
  report before any AgentOps-2b runner scoping work.

## Rollback plan

> How to undo this task if the DECISION is `request_changes` or `stop`.

- This task is `green` and works directly on `main` (same pattern as
  AgentOps-2a's policy/doc commits: yellow + green `.agent/`-only doc
  work has historically gone to `main` without a branch).
- If the DECISION requests changes:
  - Edit the MANUAL_DRY_RUN report in a follow-up commit; do not amend
    the original commits.
  - Or, if a full revert is requested:
    `git revert <run_report_commit> <task_report_commit>` — keeps the
    audit trail; never `git reset --hard` on pushed commits.
- If the report is judged unsalvageable: open a new TASK for a
  revised dry-run; the current report stays in the file tree as a
  historical record (do NOT delete it).

## Requires human approval

`no`

> Green `.agent/`-only doc work. Push to `main` (after the matching
> DECISION) is still a separate human approval per global policy, but
> the TASK itself does not require pre-approval.

## Max runtime minutes

`60` — one session, primarily writing the MANUAL_DRY_RUN report.
Executor stops and surfaces in RUN_REPORT if exceeded.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

> Beyond the global policy (`.agent/policies/agent_policy.md` §3 and
> `.agent/policies/automation_policy.md` §11), task-specific stop
> conditions.

- Stop immediately if any path under `src/`, `package*.json`,
  `.env*`, `vercel.json`, `.vercel/`, `.github/workflows/`, or any
  pipeline-repo file appears in `git status`.
- Stop immediately if the report drifts into describing a runner /
  daemon / scheduler / cron as "next implementation" instead of
  "next scoping needed with separate approval".
- Stop if the report tries to mark BLK-0001 / BLK-0002 / BLK-0003 as
  resolved.
- Stop if more than 5 files would be committed across this task
  (allowed: 1 TASK + 1 report + 1 RUN_REPORT + optionally 1 queue
  update = max 4).
- Stop if `wc -l` on the MANUAL_DRY_RUN report exceeds ~600 lines —
  the policy says reports should be ChatGPT-paste-friendly, so a
  bloated report indicates scope creep.
