# TASK · AgentOps 4b promote Fixture B baseline

## Metadata

- **task_id**: `2026-07-19_run_03`
- **date**: `2026-07-19`
- **run_number**: `03`
- **title**: AgentOps-4b · promote `20260719T054151Z_fixture-B` as first official Fixture B baseline
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-4b`
- **parent_loop**: `AgentOps-4a-stability` (`2026-07-19_run_02`)
- **grandparent_loop**: `AgentOps-4a` (`2026-07-19_run_01`)

## Objective

Promote the existing validated GREEN run `20260719T054151Z_fixture-B` as the
**first official Fixture B regression baseline** by creating lightweight
baseline artifacts under `.agent/regression_baselines/fixture-B/current/`.
**Reuse** existing run artifacts from
`.agent/regression_runs/20260719T054151Z_fixture-B/` — do **NOT** run the
harness, do **NOT** generate a report, do **NOT** call Anthropic/OpenAI, do
**NOT** commit full `report.md` or screenshots. Total cost for this loop: **$0**.
Mirror **AgentOps-3g-2** exactly (pending-then-approved marker discipline;
6-file lightweight layout; 22-field schema).

## Background

AgentOps-4a-stability DECISION (`.agent/decisions/2026-07-19_run_02_DECISION.md`)
explicitly named `20260719T054151Z_fixture-B` as preferred candidate for the
first official Fixture B baseline. Two consecutive Fixture B GREEN runs
(`20260719T045622Z_fixture-B` @ 10407 chars / 67719 ms + stability-confirmed
`20260719T054151Z_fixture-B` @ 10445 chars / 67608 ms · chars +0.4% ·
duration −0.2%) plus **identical** fixture-specific keyword patterns
(`agent · tool call · tool-call · eval · telemetry` 5/5 both runs) prove B
stability. The chosen candidate has fresher source commit `0341461`.

All 22 baseline metadata schema fields (AgentOps-3g memo §8) can be populated
from the existing artifacts + this loop's promotion metadata plus a small
stability-comparison block mirroring the A baseline.

Relevant files:
- `.agent/design_memos/2026-07-12_AgentOps-3g_baseline_promotion_design.md`
  (17-section governance memo · §7 storage layout · §8 22-field schema · §9
  11-step workflow · §10 4 statuses)
- `.agent/templates/baseline_promotion_checklist.md`
- `.agent/decisions/2026-07-19_run_02_DECISION.md`
- `.agent/run_reports/2026-07-19_run_02_RUN_REPORT.md`
- `.agent/regression_runs/20260719T054151Z_fixture-B/{metadata,structural_checks,verdict}.*`
- `.agent/regression_runs/20260719T045622Z_fixture-B/*` (previous B GREEN,
  read-only reference for stability delta)
- `.agent/regression_baselines/fixture-A/current/*` (Fixture A baseline,
  read-only reference for file layout · **not modified**)

## Allowed files

- `.agent/tasks/2026-07-19_run_03_TASK.md` (this file)
- `.agent/regression_baselines/fixture-B/current/baseline_metadata.json`
- `.agent/regression_baselines/fixture-B/current/baseline_verdict.md`
- `.agent/regression_baselines/fixture-B/current/baseline_structural_checks.json`
- `.agent/regression_baselines/fixture-B/current/source_run_id.txt`
- `.agent/regression_baselines/fixture-B/current/promotion_decision.md`
- `.agent/regression_baselines/fixture-B/current/baseline_summary.md`
- `.agent/run_reports/2026-07-19_run_03_RUN_REPORT.md`

## Forbidden files

- **`report.md`** anywhere in the repo (baseline layer never commits full
  report bodies in v1)
- **`screenshot.png`** anywhere in the repo (v1 policy)
- `scripts/report-regression-local.mjs` (harness stable · no edit)
- `src/**` (any file)
- `src/data/**`
- `src/app/api/**`
- `src/lib/**`
- `.agent/scripts/**` (hard rule per AgentOps-2c Q3-Q8)
- `.agent/blockers.md`
- `.agent/automation_queue.md`
- `.agent/regression_fixtures/**` (frozen)
- `.agent/regression_runs/**` (no edit to existing run artifacts · no new
  run created)
- **`.agent/regression_baselines/fixture-A/**`** (A baseline read-only)
- `.agent/planner_reports/**`
- **`.agent/baselines/**`** (do NOT create — wrong path)
- **`baselines/**`** (do NOT create — wrong path)
- **Other fixture baseline dirs**:
  `.agent/regression_baselines/fixture-C/`,
  `.agent/regression_baselines/fixture-D/`,
  `.agent/regression_baselines/fixture-E/` (out of scope · B only)
- uploaded resume PDFs
- pipeline repo (any file)
- `sources.yaml`, `corpus/**`, `scripts/collector/**`
- `.github/workflows/**`
- `package.json`, `package-lock.json`
- `.env*`, `vercel.json`
- Codex config files, Claude config files
- production deployment config

## Risk level

`yellow` (creates official baseline artifacts · $0 · no generation)

## Constraints

- **No harness run.**
- **No dev server run.**
- **No report generation.**
- **No LLM/API calls.**
- **No new run created.** `.agent/regression_runs/` unchanged.
- **No baseline for other fixtures.** Only Fixture B.
- **No Fixture A baseline modification.**
- **No `report.md` / `screenshot.png` committed.**
- **No production target.**
- **No push. No deploy.**
- **No Eval button click.**
- **No corpus refresh / collector run.**
- **No GitHub Actions changes.**
- **No new npm dependency.**
- **No `.env*` read.**
- **No `src/**` edit.**
- **No `scripts/report-regression-local.mjs` edit.**
- **No `.agent/scripts/**` edit.**
- **No modification of source run artifacts** at
  `.agent/regression_runs/20260719T054151Z_fixture-B/` or
  `.agent/regression_runs/20260719T045622Z_fixture-B/`.
- **`promotion_decision.md` must be a pending pointer**, not a fake
  approval. Real approval lands in
  `.agent/decisions/2026-07-19_run_03_DECISION.md` in a separate DECISION
  loop.
- **No uploaded PDF ingestion.**

## Baseline file contents (spec · mirror 3g-2)

### 1. `source_run_id.txt`
```
20260719T054151Z_fixture-B
```
(single line · no trailing content)

### 2. `baseline_verdict.md`
Adapted from source `verdict.md` with baseline-layer additions:
`baseline_id`, `source_run_id`, `fixture_id=B`,
`baseline_status = current`, source verdict = GREEN, exit_code = 0,
red = _none_, amber = _none_, note about full `report.md` + screenshots
NOT committed in v1, note about promotion approval PENDING DECISION.

### 3. `baseline_structural_checks.json`
Wrap source checks in a small envelope that preserves the original array
verbatim under `checks`, and adds `baseline_id` + `source_run_id` +
`fixture_id` + `source_run_artifact_path` + `copied_verbatim: true`.
**Do NOT modify check results.** **Do NOT edit fixture-specific checks.**

### 4. `baseline_summary.md`
Concise human-readable summary. Includes all fields listed in the TASK
objective, plus intended use and limitations. Explicit that this is a
**narrow Fixture B baseline**, not C/D/E / production / semantic
equivalence / quote integrity coverage. Mentions uploaded PDFs not
ingested.

### 5. `promotion_decision.md`
**Pending pointer**, not a fake approval. Names candidate, proposed
path, and explicit pending status until
`.agent/decisions/2026-07-19_run_03_DECISION.md` lands with approval.
Explicit that this does **not** confer C/D/E / production / quote-integrity
coverage.

### 6. `baseline_metadata.json`
Populate the 22-field schema mirrored from Fixture A baseline plus
stability comparison fields. Use canonical values from source metadata
where available:
- `model_display: "Claude Sonnet 4.6"` (canonical repo form · matches A
  baseline)
- `corpus_snapshot_date: "May 14, 2026"` (from source metadata)
- `harness_script_commit: "0341461"` (per spec · reflects repo HEAD at
  the time of the run · same value as `source_commit_sha` because
  AgentOps-4a's `--fixture` change to the harness was already merged
  into `main` by the time of the 4a-stability run)
- `promoted_by: "Bohao pending DECISION"` before DECISION lands
  (executor cannot pre-populate human approval)
- `promotion_decision_path: ".agent/decisions/2026-07-19_run_03_DECISION.md"`
- `baseline_status: "current"`
- `supersedes_baseline_id: null` (first B baseline)
- Full stability delta fields (chars/duration deltas vs previous B run)
- `fixture_specific_recommendation_keywords: ["agent", "tool call",
  "tool-call", "eval", "telemetry"]`
- `notes` array — 10 items covering scope + deferrals + intent

## Acceptance criteria

- [ ] `.agent/regression_baselines/fixture-B/current/` directory created
      (only that path).
- [ ] All 6 baseline files created (see spec above).
- [ ] `source_run_id.txt` contains exactly `20260719T054151Z_fixture-B`.
- [ ] `baseline_metadata.json` populated with 22 fields per memo §8 plus
      stability comparison + fixture-specific keywords + notes.
- [ ] `baseline_status = "current"`.
- [ ] `promoted_by = "Bohao pending DECISION"` (not falsely
      pre-approved).
- [ ] `promotion_decision_path` points to expected DECISION file
      (`.agent/decisions/2026-07-19_run_03_DECISION.md`).
- [ ] `baseline_verdict.md` explicitly notes full `report.md` and
      screenshots not committed in v1.
- [ ] `promotion_decision.md` is a pending pointer, not a fake
      approval.
- [ ] `baseline_structural_checks.json` preserves source checks
      verbatim.
- [ ] No `.agent/baselines/` created.
- [ ] No `baselines/` created.
- [ ] No `.agent/regression_baselines/fixture-C|D|E/` created.
- [ ] **No `.agent/regression_baselines/fixture-A/**` modified.**
- [ ] No `report.md` committed anywhere.
- [ ] No screenshot committed anywhere.
- [ ] No source run artifact modified (B stability run + B first run
      + all A regression runs).
- [ ] No `scripts/report-regression-local.mjs` change.
- [ ] No `src/**` change. No `.agent/scripts/**` change. No
      `.agent/regression_fixtures/**` change. No pipeline change. No
      `package.json` / lockfile change. No `.env*` read.
- [ ] No uploaded resume PDFs committed.
- [ ] BLK-0001 / BLK-0002 / BLK-0003 still `open`.
- [ ] QUEUE-0002 G2.1d still `blocked_pending_human`.
- [ ] RUN_REPORT includes the 3f `## Regression verdict` section with
      `regression_required=no`, `verdict=not_required`,
      `baseline_promoted=yes (pending DECISION)`.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# preflight
git status
git log --oneline -6
[ ! -d .agent/regression_baselines/fixture-B ] && echo "OK: no fixture-B baseline yet"
[ ! -d .agent/baselines ] && [ ! -d baselines ] && echo "OK: no wrong-path baseline dirs"

# after baseline creation
git diff --name-only
git ls-files --others --exclude-standard
ls -la .agent/regression_baselines/fixture-B/current/

# forbidden sweep (includes A baseline read-only check)
git diff --name-only | grep -E '^(src/|scripts/report-regression-local\.mjs|\.agent/scripts/|\.agent/regression_runs/|\.agent/regression_fixtures/|\.agent/planner_reports/|\.agent/baselines/|baselines/|\.agent/regression_baselines/fixture-A/|package(-lock)?\.json|\.github/workflows/)' || echo "OK"

# report.md / screenshot / PDF sweep
find . -name 'report.md' -path './.agent/*' 2>/dev/null
find . -name '*.png' -path './.agent/*' 2>/dev/null
find . -name '*.pdf' -path './.agent/*' 2>/dev/null

# JSON validity + preserved 25 checks
python3 -c "import json; json.load(open('.agent/regression_baselines/fixture-B/current/baseline_metadata.json')); print('metadata OK')"
python3 -c "import json; d=json.load(open('.agent/regression_baselines/fixture-B/current/baseline_structural_checks.json')); print('checks count=' + str(len(d['checks'])))"
```

## Expected output

RUN_REPORT must include:
- task path
- source run id (`20260719T054151Z_fixture-B`)
- baseline id (`fixture-B_20260719T054151Z_current`)
- baseline path (`.agent/regression_baselines/fixture-B/current/`)
- 6 files created (sizes and roles)
- exact source artifacts reused
- why no new generation was run
- why no `report.md` / screenshot committed
- baseline metadata summary
- baseline verdict summary
- structural checks preserved verbatim (25 checks)
- source run stability context (chars +0.4% · duration −0.2% · identical
  keyword hit patterns · both GREEN)
- comparison to Fixture A baseline as context only (not pass/fail)
- relation to AgentOps-3g-2 / Fixture A baseline workflow — verbatim
  mirror discipline
- limitations (Fixture B only · localhost only · no C/D/E · no A-E full
  suite · no production · no semantic diff · no quote integrity · 20
  PDFs not ingested)
- validation results (full forbidden-file + baseline-path + PDF + A
  baseline read-only sweep)
- confirmation of no harness run / no generation / no LLM call
- the `## Regression verdict` section from 3f template with
  `regression_required=no`, `verdict=not_required`,
  `baseline_promoted=yes (pending DECISION)`,
  `push_implication=no push until DECISION`
- recommendation: human + ChatGPT review, then DECISION

## Rollback plan

- If baseline files are malformed: revert with `rm -rf
  .agent/regression_baselines/fixture-B/` before committing. Keep TASK
  for the log. Mark next DECISION as `request_changes`.
- No push in this loop, so rollback is local-only.

## Requires human approval

`yes` (yellow · first official Fixture B baseline creation · shapes
future comparison logic for the B fixture)

> Approved by: `Bohao msg 2026-07-19 (arriving 2026-07-20) · "Start
> AgentOps-4b: promote 20260719T054151Z_fixture-B as first official
> Fixture B baseline"` and
> `.agent/decisions/2026-07-19_run_02_DECISION.md` (executor preference
> §follow-up: AgentOps-4b baseline promotion).

## Max runtime minutes

15 (writing 7 text files · no run · no generation)

## Max token budget hint

`unset`

## Stop conditions

- Stop if any forbidden file appears in `git status`.
- Stop if any wrong-path baseline dir gets created
  (`.agent/baselines/` / `baselines/` / any non-B fixture baseline).
- Stop if Fixture A baseline files show any modification.
- Stop if a Playwright run is triggered or a dev server started.
- Stop if any source run artifact under
  `.agent/regression_runs/**` gets modified.
- Stop if `report.md`, `screenshot.png`, or any `.pdf` appears in
  `git status`.
- Stop if `promoted_by` is set to anything that implies human approval
  before DECISION lands.

## Do NOT

- Do NOT run Playwright.
- Do NOT run report generation.
- Do NOT call Anthropic/OpenAI.
- Do NOT run another Fixture B stability re-run.
- Do NOT run Fixture A.
- Do NOT run C/D/E.
- Do NOT run A-E full suite.
- Do NOT ingest the 20 uploaded PDFs.
- Do NOT commit uploaded PDFs.
- Do NOT test production.
- Do NOT modify `scripts/report-regression-local.mjs`.
- Do NOT modify `.agent/scripts/**`.
- Do NOT modify `src/**`.
- Do NOT modify pipeline.
- Do NOT modify prompts.
- Do NOT modify model selection.
- Do NOT modify report generation route.
- Do NOT click Eval.
- Do NOT implement Codex planner.
- Do NOT create `.agent/planner_reports/`.
- Do NOT introduce OpenAI API.
- Do NOT run collector.
- Do NOT refresh corpus.
- Do NOT modify GitHub Actions.
- Do NOT modify `package.json` or `package-lock.json`.
- Do NOT push.
- Do NOT deploy.
- Do NOT commit `report.md`.
- Do NOT commit `screenshot.png`.
- Do NOT modify Fixture A baseline.
- Do NOT resolve BLK-0001 / BLK-0002 / BLK-0003.
- Do NOT start G2.1d.
- Do NOT resume full automation.
