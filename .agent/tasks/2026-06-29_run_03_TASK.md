# TASK · AgentOps-2b automation runner design memo

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-06-29_run_03`
- **date**: `2026-06-29`
- **run_number**: `03` (zero-padded sequence within the day)
- **title**: AgentOps-2b automation runner design memo
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only;
  pipeline repo is read-only sanity)

## Objective

Author a design memo at
`.agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md`
that scopes a hypothetical future automation runner coordinating
**Codex CLI** as planner/reviewer/decision proposer and **Claude
Code** as coding executor during Automation Time windows, with
Human + ChatGPT review during Non-Automation Time. The memo
defines what the runner WOULD do, what it WOULD NOT do, and gives
at least three (preferably four) implementation options with a
recommended next step.

**This is a memo only.** No runner / daemon / scheduler / cron /
GitHub Actions / Codex CLI config / Claude Code config / OpenAI
API / app code / pipeline-repo file is created or modified. The
memo describes future behavior; it does not enable it.

## Background

- AgentOps-2a (`f1bab0f`) shipped the load-bearing automation
  policy: `automation_policy.md` v1.1 (after the Executive Digest
  amendment at `6c51db6`), `automation_queue.md`, `blockers.md`,
  `automation_window_report_template.md`, and an updated README.
  See policy §6 (Green / Yellow / Red) and §11 (Stop conditions)
  for the contract this memo's runner must obey.
- QUEUE-0006 MANUAL_DRY_RUN (`475b116`) validated the report shape
  end-to-end without a runner; the matching DECISION's risk #2
  said: "MANUAL_DRY_RUN does not prove an actual Codex + Claude
  automation window can run safely. The MANUAL_DRY_RUN proves the
  *reporting* contract is reviewable; it does not prove the
  *execution* contract." This memo addresses the *execution*
  contract.
- The Executive Digest template update (`6c51db6`) added the
  10-line top section. The memo must reference the new digest as
  the contract every automation window report begins with.
- Daily summary 2026-06-29 (`ce114b0`) lists "AgentOps-2b runner
  design memo (memo only)" as the second recommended next-day
  task. This TASK is the implementation of that recommendation.
- BLK-0002 (full automation activation) explicitly remains `open`
  and is gated on this memo PLUS a separate runner-implementation
  TASK + DECISION PLUS at least one supervised real Automation
  Window. The memo MUST NOT lift BLK-0002.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-06-29_run_03_TASK.md` (this file)
- `.agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md`
  (NEW; the deliverable)
- `.agent/run_reports/2026-06-29_run_03_RUN_REPORT.md` (NEW;
  matching RUN_REPORT)
- `.agent/automation_queue.md` — only if a new queue item recording
  AgentOps-2b memo is appropriate; status MUST be `in_review` (NOT
  `done`, NOT `in_progress`); do NOT lift or transition any other
  queue item.
- `.agent/blockers.md` — only if a genuinely NEW blocker is
  discovered by writing the memo. Do NOT resolve / lift any
  existing blocker. Do NOT invent unnecessary blockers.

## Forbidden files

### Web repo

- `src/**` (all app code)
- `src/lib/prompts.ts`, `src/lib/anthropic.ts`
- `src/data/web_bundle.json`
- `package.json` / `package-lock.json`
- `.env*`
- `.github/workflows/**`
- `vercel.json` / `.vercel/**`
- `.agent/policies/**` (policy is frozen for this memo; the memo
  *cites* §6 / §11 / §12 etc. but does NOT amend the policy)
- `.agent/templates/**` (templates frozen; memo cites the
  Executive Digest contract from
  `automation_window_report_template.md` but does NOT amend any
  template)
- `.agent/README.md` (no link edit this memo)
- `.agent/decisions/**` (DECISION is downstream)
- `.agent/automation_runs/**` (existing MANUAL_DRY_RUN report
  remains untouched)
- `.agent/daily_summaries/**` (today's already shipped)
- `.agent/scripts/**` (helpers only invoked, not edited)
- **Any actual runner / daemon / scheduler / cron / orchestrator
  file**: no `runner.py`, `runner.ts`, `runner.sh`, `daemon.*`,
  `scheduler.*`, `orchestrate.*`, `cron.*`, `automation_loop.*`,
  `dispatch.*`, etc. — neither under `.agent/scripts/` nor
  anywhere else. The memo describes; nothing executes.
- `.claude/**`, `~/.claude/**`, `~/.codex/**` (Codex/Claude
  config)

### Pipeline repo

- **All files**. Pipeline read-only. At most one read-only `git
  status` / `git log` / `git rev-parse HEAD` check.

### Process-level

- No new GitHub Actions / workflow / cron job
- No new deployment hooks
- No new npm / Python dependencies
- No OpenAI API SDK / key / `.env` entry / HTTP call
- No `python -m scripts.collector …` invocation
- No `npm run …` invocation
- No LLM call by this task (no `anthropic` / `openai` SDK)
- No Codex CLI config or Claude Code config mutation
- No force push / history rewrite / destructive reset
- No `git push`
- No manual `vercel deploy`
- No executable runner / shell script / config that enables
  automatic execution

## Risk level

`yellow`

> The memo may shape future automation behavior (a runner could be
> built from it later), which is why it is yellow rather than
> green. Implementation is documentation-only — no executable
> code is created. Yellow + `.agent/`-only doc pattern matches
> AgentOps-2a policy commit (`798bf69`) and the Executive Digest
> amendment (`5560dae`).

## Constraints

- No automation runner / daemon / scheduler / cron / GitHub
  Actions edit / Codex CLI config edit / Claude Code config edit
  / OpenAI API integration / new dependency.
- No `python -m scripts.collector …` / `npm run …` invocation.
- No LLM call by this task.
- No prompt / classifier / extractor / schema / model selection
  edits.
- No deploy. No `git push`. No pipeline-repo file edited.
- The memo is `status: draft_for_human_chatgpt_review` —
  authored, not adopted. Approval to *implement* anything in the
  memo is a separate downstream TASK + DECISION.
- The memo MUST explicitly recommend that the next step is NOT a
  runner implementation. Either MANUAL_DRY_RUN-2 OR a follow-up
  design memo (AgentOps-2c) covering a supervised-only
  prototype, OR a different non-automation task entirely (e.g.
  P1.7b / P1.7c).
- The memo MUST include at least 3 options (A no-runner, B
  supervised dry-run, C orchestrator with hard gates) plus a
  4th option (D GitHub Actions / cron) which it explicitly
  recommends AGAINST.
- The memo MUST NOT lift or resolve BLK-0001 / BLK-0002 /
  BLK-0003. QUEUE-0002 G2.1d must stay
  `blocked_pending_human`.
- The memo MUST state that OpenAI API remains blocked, G2.1d
  remains blocked, and full automation remains inactive.
- The memo MUST reference the Executive Digest contract from
  `automation_window_report_template.md` and require any future
  runner to emit reports starting with that digest.
- If `.agent/automation_queue.md` is updated, the new queue item
  for AgentOps-2b MUST be marked `in_review` (NOT `done`, NOT
  `in_progress`, NOT `candidate`). Do NOT add a queue item for a
  runner; only for the memo.
- If `.agent/blockers.md` is touched, it can ONLY be to ADD a
  new blocker if a genuine one surfaces; do NOT resolve / close
  any existing blocker.

## Acceptance criteria

- [ ] `.agent/tasks/2026-06-29_run_03_TASK.md` exists (this file).
- [ ] `.agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md`
      exists, with the 16 listed sections.
- [ ] Memo `Status` field reads `draft_for_human_chatgpt_review`.
- [ ] Memo `Scope` field reads `design only` (or equivalent).
- [ ] Memo `Non-goal` field explicitly says no runner
      implementation.
- [ ] Memo defines runner purpose, Codex role, Claude Code role,
      Human + ChatGPT B-time review behavior, automation window
      report contract (referencing the new Executive Digest),
      queue selection rules, blocker handling, stop conditions,
      merge/push/deploy rules.
- [ ] Memo presents at least 4 implementation options (A, B, C,
      D) with one explicitly safest minimal option (A) and one
      explicitly recommended AGAINST (D).
- [ ] Memo's "Recommended path" section explicitly says the next
      step is NOT a runner implementation.
- [ ] Memo's Risks section enumerates at least the 10 listed risks
      (agent drift, quota burn, red-zone mutation, queue ambiguity,
      long reports, false safety after dry-run, accidental push /
      deploy, hidden config changes, OpenAI API introduction,
      G2.1d started too early).
- [ ] Memo's Explicit non-goals section enumerates: no code, no
      runner, no daemon, no scheduler, no cron, no GitHub Actions,
      no OpenAI API, no Codex/Claude config, no app code, no
      pipeline code, no full automation activation, no G2.1d, no
      blocker resolution.
- [ ] Memo's Decision questions section lists ≥6 open questions
      for Human + ChatGPT.
- [ ] `git diff --stat` shows changes only under `.agent/tasks/`,
      `.agent/design_memos/`, `.agent/run_reports/`, and
      optionally `.agent/automation_queue.md` / `.agent/blockers.md`.
      No other path.
- [ ] No forbidden file modified.
- [ ] No `git push` performed.
- [ ] No DECISION file created.
- [ ] No executable runner / shell script / config / cron / hook
      file created anywhere.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check
git status --short
git diff --stat

# memo exists + reasonable size + non-empty
ls -l .agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md
wc -l .agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md
grep -c '^## ' .agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md

# explicit non-activation strings must appear in the memo
grep -c 'design only' .agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md
grep -c 'draft_for_human_chatgpt_review' .agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md
grep -c 'BLK-0001\|BLK-0002\|BLK-0003' .agent/design_memos/2026-06-29_AgentOps-2b_automation_runner_design.md

# forbidden audit
git diff --stat HEAD -- src/ src/lib/prompts.ts src/lib/anthropic.ts \
    src/data/web_bundle.json package.json package-lock.json \
    .github/workflows/ vercel.json .vercel/ \
    .agent/policies/ .agent/templates/ .agent/README.md \
    .agent/automation_runs/ .agent/decisions/ \
    .agent/daily_summaries/ .agent/scripts/ 2>&1 || true

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

Beyond the standard RUN_REPORT template:

- Exact paths of files touched (TASK + memo + RUN_REPORT;
  optionally queue).
- Exact commit hashes for the "TASK + memo (+ queue)" commit and
  the RUN_REPORT commit.
- Memo line count (rough sanity; target: 500–900 lines, with hard
  stop at 1200 — memo can be long because it's design, but should
  not be a novel).
- Memo H2 section count = **16** (matching the 16 spec sections).
- Confirmation that pipeline HEAD = `b019786` at run start and
  end.

## Rollback plan

- Yellow `.agent/` doc work, direct on `main` (same pattern as
  AgentOps-2a / MANUAL_DRY_RUN / Executive Digest).
- If DECISION requests changes: edit the memo in a follow-up
  commit; do not amend the original commits.
- If full revert is requested: `git revert <run_report_commit>
  <memo_commit>`. Never `git reset --hard` on pushed commits.
- The memo is `status: draft_for_human_chatgpt_review` until a
  DECISION approves it; even after approval, the memo does not
  cause anything to execute. Reverting it is safe.

## Requires human approval

`no`

> Yellow `.agent/`-only memo work, no red-zone file. Push (after
> the matching DECISION) is still a separate human approval per
> global policy.

## Max runtime minutes

`90` — memo authoring is the bulk of the work.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

- Stop if any path under `src/`, `package*.json`, `.env*`,
  `vercel.json`, `.vercel/`, `.github/workflows/`, or any
  pipeline-repo file appears in `git status`.
- Stop if any executable runner / shell script / config file
  appears in `git status`.
- Stop if the memo line count exceeds 1200 (memo became a novel).
- Stop if the memo's `Status` is anything other than
  `draft_for_human_chatgpt_review` at end of run.
- Stop if any of BLK-0001 / BLK-0002 / BLK-0003 is marked
  resolved.
- Stop if a queue item for "AgentOps-2b runner" (not the memo) is
  added or activated.
- Stop if more than 5 files would be committed across this task
  (allowed: 1 TASK + 1 memo + 1 RUN_REPORT + optional 1 queue +
  optional 1 blocker = max 5).
- Stop if a DECISION file appears in `git status`.
