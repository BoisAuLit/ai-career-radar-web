# TASK · AgentOps 5e implement structural lint harness integration

## Metadata

- **task_id**: `2026-07-24_run_07`
- **date**: `2026-07-24`
- **run_number**: `07`
- **title**: AgentOps 5e implement structural lint harness integration
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`
- **loop**: `AgentOps-5e-followup-baseline-lint-integrate-implement`
- **parent_loop**: `AgentOps-5e-followup-baseline-lint-integrate-design` (`2026-07-24_run_06` · Phase 2 design)
- **authorizing_decision**: `.agent/decisions/2026-07-24_run_06_DECISION.md`

## Objective

Integrate the standalone structural-evidence validator into the local regression harness `scripts/report-regression-local.mjs` as non-blocking sibling telemetry. Preserve strict separation between legacy regression verdict, quote-integrity telemetry, and structural-evidence telemetry. Emit `structural_evidence_summary.json`, `structural_evidence_context.json`, `metadata.structural_evidence`, `metadata.combined_telemetry`, and four sibling sections in `verdict.md` — while leaving legacy `checks[]`, `classify(checks)`, process exit, and baseline metadata untouched. **No generation. No A/B. No paid API. $0.**

## Background

Phase 1 (`2026-07-24_run_05`) shipped the standalone deterministic validator `scripts/structural-evidence-check.mjs` and 26/26 deterministic tests. Phase 2 design (`2026-07-24_run_06`) selected integration point B (after `runQuoteIntegrity` returns, before first `checks.push`), a `--context <path>` JSON contract (schema `0.1-phase2` · 9 fields), SHA-256 checker_hash, 5s timeout, `combined_telemetry_verdict` display-only, and a 20-item deterministic test matrix I1-I20. This TASK implements that design.

## Allowed files

- `scripts/structural-evidence-check.mjs` (extend with `--context` support)
- `scripts/test-structural-evidence-check.mjs` (add context-mode tests, preserve all 26 existing)
- `scripts/report-regression-local.mjs` (patch harness at line ~828)
- `scripts/lib/structural-evidence-integration.mjs` (**new** helper module)
- `scripts/test-structural-evidence-integration.mjs` (**new** integration tests I1-I20)
- `scripts/fixtures/structural-evidence/` (synthetic contexts/reports as needed)
- `.agent/tasks/2026-07-24_run_07_TASK.md`
- `.agent/design_memos/2026-07-24_AgentOps-5e-followup-baseline-lint-integrate-implement.md`
- `.agent/run_reports/2026-07-24_run_07_RUN_REPORT.md`

## Forbidden files

- `scripts/quote-integrity-check.mjs` (checker unchanged)
- `src/**` (including `src/lib/prompts.ts`)
- `src/app/api/**`
- `.agent/regression_baselines/**` (no baseline mutation)
- `.agent/regression_runs/**` (source artifacts frozen)
- `.agent/regression_fixtures/**` (frozen)
- `.agent/scripts/**` (hard rule)
- `package.json` / `package-lock.json` / workflows / env / `vercel.json`
- Pipeline (`/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar/**`)

## Risk level

`yellow`

## Constraints

- **No new dependencies** (Node stdlib only).
- **No prompt / classifier / extractor edits.**
- **No model / R1 / R2 changes.**
- **No baseline mutation.**
- **No legacy `checks[]` addition · no `classify()` mutation.**
- **No process-exit change.**
- **No structural telemetry blocking.**
- **No retries.**
- **No report rewriting.**
- **No A/B generation · no paid API.**
- **No deploy · no push.**
- 5s validator timeout.
- SHA-256 `checker_hash` format `sha256:<hex>`.
- Schema `0.1-phase2` for context; schema unchanged for structural summary (`0.1-phase1`).

## Acceptance criteria

- [ ] `scripts/structural-evidence-check.mjs` accepts optional `--context <path>`; Phase 1 two-argument invocation still works.
- [ ] `scripts/test-structural-evidence-check.mjs` — all 26 Phase 1 tests + new context-mode tests PASS.
- [ ] `scripts/lib/structural-evidence-integration.mjs` exports deterministic `runStructuralEvidence({...})` returning stable envelope.
- [ ] `scripts/test-structural-evidence-integration.mjs` — all 20 tests I1-I20 PASS.
- [ ] `scripts/report-regression-local.mjs` invokes `runStructuralEvidence` immediately after `runQuoteIntegrity` returns AND before first legacy `checks.push`.
- [ ] `metadata.structural_evidence` block with 13 fields including `affected_legacy_verdict: false` and `blocking_mode: telemetry_only`.
- [ ] `metadata.combined_telemetry` with `display_only: true` and `affected_legacy_verdict: false`.
- [ ] `verdict.md` displays four sibling sections (Legacy · QI · Structural · Combined).
- [ ] Static assertion: no `checks.push` uses structural variable.
- [ ] Static assertion: no `process.exit` uses structural/combined value.
- [ ] Static assertion: no baseline path touched.
- [ ] `npx tsc --noEmit` exit 0.
- [ ] `scripts/quote-integrity-check.mjs` unchanged.
- [ ] No forbidden files modified.
- [ ] `git status` clean after commits, no push.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web
node scripts/test-structural-evidence-check.mjs
node scripts/test-structural-evidence-integration.mjs
npx tsc --noEmit
git diff --name-only
git diff --stat
```

## Expected output

- Test totals `26/26` (Phase 1, may exceed with context-mode additions) + `20/20` (integration).
- `npx tsc --noEmit` exit 0.
- 2 commits: implementation + RUN_REPORT.
- Web ahead by 2 (no push).
- Pipeline `b019786` untouched.
- Cost $0.

## Rollback plan

- Revert implementation commit → `--context` extension may remain (backward-compat) OR be reverted.
- Remove `metadata.structural_evidence` + `metadata.combined_telemetry` + `## Structural evidence telemetry` + `## Combined telemetry` sections.
- No prompt rollback. No baseline restoration. Zero downstream impact.

## Requires human approval

`yes`

> Approved by: `.agent/decisions/2026-07-24_run_06_DECISION.md`

## Max runtime minutes

60

## Max token budget hint

`unset`

## Stop conditions

- Stop if a forbidden file appears in `git status`.
- Stop if `node scripts/test-structural-evidence-check.mjs` regresses (any of 26 Phase 1 tests fails).
- Stop if `node scripts/test-structural-evidence-integration.mjs` fails after fixups.
- Stop if `npx tsc --noEmit` reports errors.
- Stop if structural verdict enters legacy `checks[]` or `classify(checks)` shape changes.
