# TASK · P1.8a sample report credibility copy audit

> Fill every field. Empty fields mean "not yet decided" — surface them in the
> RUN_REPORT instead of guessing.

## Metadata

- **task_id**: `2026-07-05_run_02`
- **date**: `2026-07-05`
- **run_number**: `02` (zero-padded sequence within the day)
- **title**: P1.8a sample report credibility copy audit
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web` (web-only;
  pipeline repo is read-only sanity)

## Objective

Do a narrow **copy-only** audit of `src/app/sample-report/**` and
`src/app/methodology/**`. Make small edits where the wording is
too mock-like, overpromises, implies unsupported freshness, or
carries a stale hardcoded metric that P1.7b already fixed
elsewhere. Preserve the product's evidence-grounded positioning
and the honest, whitepaper-style methodology copy that already
works. No runtime / prompt / API / model / pipeline / data
changes.

**Preflight `grep` findings + verdict per site**:

- **`src/app/sample-report/page.tsx:32`** — navbar chip reads
  `443 real JDs`. **Must-fix**: (a) tie to
  `WEB_BUNDLE_STATS.totalJds` for P1.7b consistency, and (b)
  soften `real` → `tracked` — "tracked" aligns with the
  evidence-grounded framing and doesn't imply live freshness.
- **`src/app/sample-report/page.tsx:63`** — disclosure banner
  says "The real tool reads your actual resume and the **live**
  JD corpus." **Must-fix**: the corpus is a
  ~6-week-old May 2026 snapshot per methodology page's own
  admission (line 121-122); "live" overpromises. Softer:
  "tracked" or "current corpus snapshot" (matches P1.7b's
  language).
- **`src/app/sample-report/page.tsx:93`** — Header chip reads
  `Based on 92 applied_ai JDs`. **Must-fix**: this is the same
  stale mock number P1.7b corrected on the homepage (actual =
  47). Wire through `WEB_BUNDLE_STATS.appliedAiJds`.
- **`src/app/sample-report/page.tsx:342-345`** — "Honest note"
  says "classifies your stated target into one of **8**
  archetypes". **Must-fix (softening)**: the G2.1 taxonomy has
  11 included archetypes + 2 reject sidecars (per
  `corpus/taxonomy/G2.1_spec.md`), so "8" is stale.
  Conservative: **remove the specific number** — say
  "classifies your stated target into an archetype" without
  the count. The count claim is not load-bearing for the
  paragraph's argument.
- **`src/app/methodology/page.tsx` lines 117, 143, 309** —
  "approximately 40 AI / AI-adjacent technology companies" /
  "40 companies is a curated sample" / "40 curated companies".
  **Leave unchanged**: these likely refer to the source-registry
  size (companies the pipeline is *set up to fetch from* via
  `sources.yaml`), not to the count of companies actually
  present in the current `web_bundle.json` (35 per P1.7b). The
  distinction is real and I don't have visibility into
  `sources.yaml` in this task to verify; substituting `35`
  could silently break a correct claim. The qualitative point
  ("small, curated, not a census") lands regardless.
- **`src/app/sample-report/page.tsx` percentages** (38%, 55%,
  48% etc. across the strengths / gaps sections) — **leave
  unchanged**. The page has a prominent "Sample · fictional"
  banner (line 57-65) that discloses all numbers on the page
  are illustrative. Consistent with the framing.
- **`src/app/page.tsx`** — not in scope this loop unless a
  sample-report teaser CTA has stale wording. Preflight
  showed the hero and sample-report link copy are consistent
  with P1.7b/c framing. **Leave unchanged**.
- **`src/components/**`** — not in scope. `SiteChrome.tsx` is
  the only file there; it's chrome, not sample-report /
  methodology copy.

## Background

- P1.7 series shipped: hero shell (P1.7 `2026-06-27_run_01`) →
  hero numbers grounded in `web_bundle.json` (P1.7b `1c912f8`
  via new helper `src/lib/web-bundle-stats.ts`) → model-string
  SSOT (P1.7c `c0a0df3` via new helper `src/lib/models-display.ts`).
- Sample-report page still carries the pre-P1.7b hardcoded
  `92` and the raw `443` — clean-up scope leaked to the
  homepage but not to `sample-report`.
- Methodology page is already well-written whitepaper-style
  copy; nothing there to "fix" for credibility.

## Allowed files

> Paths the executor MAY edit. Glob patterns OK.

- `.agent/tasks/2026-07-05_run_02_TASK.md` (this file)
- `.agent/run_reports/2026-07-05_run_02_RUN_REPORT.md` (NEW;
  matching RUN_REPORT)
- `src/app/sample-report/page.tsx` (edit ONLY the 4 sites
  called out above: navbar chip on line 32; disclosure
  "live" on line 63; header chip `92` on line 93; "Honest
  note" archetype count on line 342-345). No unrelated
  refactor.

## Forbidden files

### Web repo

- **`src/app/methodology/page.tsx`** — untouched. Preflight
  verdict is "leave" for all methodology sites, and modifying
  it would risk breaking well-written whitepaper claims about
  the sources registry.
- **`src/app/api/**`** — untouched. Runtime model selection
  and invocation logic frozen.
- **`src/lib/prompts.ts`** — untouched. Prompts frozen.
- **`src/lib/anthropic.ts`** — does not exist in this repo;
  trivially satisfied.
- **`src/lib/corpus.ts`** — untouched. Server-only corpus
  logic frozen.
- **`src/lib/types.ts`** — untouched.
- **`src/lib/models-display.ts`** — untouched (frozen from
  P1.7c).
- **`src/lib/web-bundle-stats.ts`** — untouched (frozen from
  P1.7b). This TASK **imports** it in sample-report but does
  NOT modify it.
- **`src/lib/eval-report.ts`, `src/lib/extract-pdf.ts`** —
  untouched.
- **`src/data/**`** — untouched (`web_bundle.json`,
  `web_bundle_pipeline.json`, `lab/**` all frozen).
- **`src/app/layout.tsx`, `src/app/opengraph-image.tsx`,
  `src/app/lab/**`, `src/app/snapshot-pipeline/**`** —
  untouched.
- **`src/app/page.tsx`** — untouched this loop. Preflight
  confirmed the homepage sample-report teaser copy is
  consistent with P1.7b/c framing.
- **`src/components/**`** — untouched (chrome only, not
  audit target).
- **`package.json` / `package-lock.json`** — untouched (no
  new deps).
- **`.env*`** — untouched.
- **`.github/workflows/**`** — untouched.
- **`vercel.json` / `.vercel/**`** — untouched.
- **`.agent/policies/**`** — untouched (policy at v1.1
  stays).
- **`.agent/templates/**`** — untouched.
- **`.agent/scripts/**`** — untouched. **Hard rule** per
  AgentOps-2c DECISION Q3-Q8.
- **`.agent/automation_queue.md`** — untouched (queue
  transitions are B-time).
- **`.agent/blockers.md`** — untouched.

### Pipeline repo

- **All files**. Pipeline read-only. At most one read-only
  `git status` / `git log` / `git rev-parse HEAD` check.

### Process-level

- No new GitHub Actions / workflow / cron job.
- No new deployment hooks.
- No new npm / Python dependencies.
- No OpenAI API SDK / key / `.env` entry / HTTP call (per
  Q7 blocked sense).
- No Codex CLI or Claude Code config mutation.
- No `python -m scripts.collector …` invocation.
- No `npm run collect` or similar corpus regeneration.
- No LLM call by this task.
- No force push / history rewrite.
- No `git push`.
- No manual `vercel deploy`.
- No automation runner / daemon / scheduler / cron file
  creation.

## Risk level

`yellow`

> User-visible product-copy change. Scope narrow (4 sites in
> a single file `src/app/sample-report/page.tsx` + 1 new
> import). Same risk class as P1.7b / P1.7c.

## Constraints

- No model / prompt / runtime-selection / API-route edits.
- No pipeline-repo / `src/data/**` changes.
- No `.agent/scripts/**` changes (**hard rule**).
- No new dependency (npm or Python).
- No `python -m scripts.collector …` / `npm run collect` /
  LLM call.
- No `git push`. No `vercel deploy`.
- No new metrics invented. Every substituted number MUST
  trace to `WEB_BUNDLE_STATS` (which was validated by
  P1.7b's DECISION).
- No layout / structural / component-extraction change.
  Text substitutions only within existing JSX.
- No changes to the fictional percentages (38%, 55%, etc.)
  — they're consistent with the "Sample · fictional"
  disclosure banner.
- No changes to `src/app/methodology/page.tsx` — the
  preflight verdict is "leave" for all methodology sites.

## Acceptance criteria

- [ ] `src/app/sample-report/page.tsx:32` navbar chip
      renders `{WEB_BUNDLE_STATS.totalJds}` (443) instead of
      hardcoded `443`, and word `real` softens to
      `tracked`.
- [ ] `src/app/sample-report/page.tsx:63` disclosure text
      no longer contains the word `live` in the JD-corpus
      phrase; substitutes softer language.
- [ ] `src/app/sample-report/page.tsx:93` header chip
      renders `{WEB_BUNDLE_STATS.appliedAiJds}` (47) instead
      of hardcoded `92`.
- [ ] `src/app/sample-report/page.tsx:342-345` "Honest
      note" no longer contains the specific count `8
      archetypes`; softens to a countless equivalent.
- [ ] New `import { WEB_BUNDLE_STATS } from
      "@/lib/web-bundle-stats"` at the top of
      `src/app/sample-report/page.tsx`.
- [ ] `src/app/methodology/page.tsx` — **empty diff**.
- [ ] `src/app/page.tsx` — empty diff.
- [ ] `src/lib/web-bundle-stats.ts` — untouched (only
      imported).
- [ ] `src/lib/models-display.ts` — untouched.
- [ ] `src/lib/prompts.ts` / `src/lib/corpus.ts` /
      `src/app/api/**` / `src/data/**` untouched.
- [ ] No forbidden file modified.
- [ ] `npm run lint` passes with only pre-existing
      baseline errors unchanged.
- [ ] `npm run build` passes (14/14 static routes,
      TypeScript clean).
- [ ] `git diff --stat` shows changes only under
      `.agent/tasks/`, `.agent/run_reports/`, and
      `src/app/sample-report/page.tsx`.
- [ ] No `git push` performed.
- [ ] No DECISION file created.
- [ ] Pipeline HEAD = `b019786` unchanged.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web

# scope check
git status --short
git diff --stat

# sample-report spot-checks
grep -n 'WEB_BUNDLE_STATS' src/app/sample-report/page.tsx
grep -n '443 real\|"443"\|"92"\|>92<\|Based on 92\|live JD corpus\|8 archetypes' src/app/sample-report/page.tsx

# methodology must be empty diff
git diff --stat HEAD -- src/app/methodology/

# forbidden audit
git diff --stat HEAD -- \
    src/lib/prompts.ts src/lib/corpus.ts src/lib/types.ts \
    src/lib/eval-report.ts src/lib/extract-pdf.ts \
    src/lib/web-bundle-stats.ts src/lib/models-display.ts \
    src/data/ package.json package-lock.json \
    src/app/api/ src/app/layout.tsx src/app/opengraph-image.tsx \
    src/app/lab/ src/app/snapshot-pipeline/ src/app/page.tsx \
    src/components/ .agent/policies/ .agent/templates/ \
    .agent/scripts/ .agent/blockers.md \
    .agent/automation_queue.md .github/workflows/ vercel.json \
    .vercel/ 2>&1 || true

# lint + build
npm run lint 2>&1 | tail -12
npm run build 2>&1 | tail -25

# pipeline read-only sanity
cd /Users/bohaoli/Desktop/tuto/tuto_ai_career_radar
git status
git rev-parse HEAD
```

## Expected output

- Exact commit hashes for the TASK+impl commit and the
  RUN_REPORT commit.
- `npm run lint` result (pass / warnings-only-unchanged-from-
  baseline; line-number shift OK if a new import bumps
  everything down by 1).
- `npm run build` result (pass, TypeScript clean, 14/14
  static routes).
- Confirmation that pipeline HEAD = `b019786` at start and
  end.
- Confirmation of empty diff on `src/app/methodology/`.

## Rollback plan

- Yellow product-code task. Same direct-on-`main` pattern
  P1.7b / P1.7c used.
- If DECISION requests changes: edit in a follow-up
  commit; do not amend.
- If full revert: `git revert <run_report_commit>
  <impl_commit>`. Never `git reset --hard` on pushed
  commits.

## Requires human approval

`no`

> Yellow small copy-audit change, no red-zone file. Push
> (after the matching DECISION) is a separate human
> approval per global policy; the push will trigger Vercel
> auto-deploy (same as P1.7b / P1.7c). Unlike P1.7c, the
> **rendered characters will differ** (e.g. `92 applied_ai
> JDs` → `47 applied_ai JDs` on the sample-report page,
> matching what P1.7b did on the homepage) — this is the
> intended user-visible fix.

## Max runtime minutes

`30` — 4 line-edits in one file + build + RUN_REPORT.

## Max token budget hint

`unset` — advisory only.

## Stop conditions

- Stop if any path under `src/lib/prompts.ts`,
  `src/lib/corpus.ts`, `src/lib/anthropic.ts` (if
  created), `src/lib/web-bundle-stats.ts` (mod, not just
  import), `src/lib/models-display.ts` (mod), any
  `src/app/api/**`, `src/data/**`, `package*.json`,
  `.env*`, `vercel.json`, `.vercel/`, `.github/workflows/`,
  `.agent/scripts/`, `.agent/policies/`, `.agent/templates/`,
  `.agent/blockers.md`, `.agent/automation_queue.md`, or
  any pipeline-repo file appears in `git status`.
- Stop if `src/app/methodology/page.tsx` shows any diff.
- Stop if `src/app/page.tsx` shows any diff.
- Stop if `npm run build` fails.
- Stop if `npm run lint` introduces NEW errors (baseline
  warnings are OK; line-shift OK).
- Stop if a new metric is introduced that doesn't trace to
  `WEB_BUNDLE_STATS`.
- Stop if any fictional-percentage number on the sample
  report is changed (they belong to the "fictional" banner
  framing).
- Stop if more than 3 files would be committed in the
  impl commit (allowed: 1 TASK + 1 `sample-report/page.tsx`
  + 1 RUN_REPORT = 3 total).
- Stop if a DECISION file appears in `git status`.
