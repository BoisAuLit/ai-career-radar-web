# TASK · P1.7 premium detail pass

> Example TASK file. Read alongside the matching RUN_REPORT and DECISION
> examples to see one complete loop.

## Metadata

- **task_id**: `2026-06-27_run_01`
- **date**: `2026-06-27`
- **run_number**: `01`
- **title**: P1.7 premium detail pass — homepage + sample report polish
- **repo**: `/Users/bohaoli/Desktop/ai-career-radar-web`

## Objective

Lift the web app from "looks fine" to "looks premium" across the homepage hero,
generator workbench, sample report, and the secondary pages (snapshot,
methodology, lab). Add a dedicated static sample-report page so first-time
visitors can preview the product without running the generator.

## Background

Earlier phases (P1.5, P1.6) consolidated the design tokens (`.eyebrow`,
`.surface-hero`, etc.) and tightened the methodology TOC. The remaining gap
is detail-level polish on the user-facing surfaces. ChatGPT signed off on the
direction in conversation `2026-06-26 chat 14:30`. No structural changes; no
new dependencies; no API or prompt work.

## Allowed files

- `src/app/page.tsx`
- `src/app/sample-report/*`
- `src/app/snapshot-pipeline/*`
- `src/app/methodology/*`
- `src/app/lab/*`
- `src/app/globals.css` (token additions only — no removals)
- `src/components/SiteChrome.tsx` (header/footer parity only)

## Forbidden files

- `.github/workflows/*`
- `src/lib/prompts.ts`
- `src/data/web_bundle.json`
- `package.json` (no new deps)
- `package-lock.json` (no new deps)
- `.env*`
- `**/secrets*`
- Any file under the pipeline repo `/Users/bohaoli/Desktop/tuto/tuto_ai_career_radar/`

## Risk level

`yellow`

> Visual polish on production UI. No prompt/model/data changes, but the
> homepage IS the production surface so the build must pass and screenshots
> must be reviewed before merge.

## Constraints

- No new npm dependencies.
- No prompt, classifier, extractor, or model selection changes.
- No deploy.
- No push to `main`.
- The static sample report must be obviously labeled "sample / fictional"
  per the existing pattern.

## Acceptance criteria

- [ ] Homepage hero feels more premium (clearer hierarchy, better spacing rhythm).
- [ ] Generator workbench improved (clearer call-to-action, less visual noise).
- [ ] `/sample-report` static page exists and renders without errors.
- [ ] Snapshot, methodology, and lab pages are lightly unified with the homepage chrome.
- [ ] `npm run build` passes.
- [ ] `npm run screenshot` produces 15/15 captures with no visual regressions
      worse than the previous run.
- [ ] No prompt / model / pipeline / dependency changes.
- [ ] All changed files listed in the RUN_REPORT.

## Validation commands

```bash
cd /Users/bohaoli/Desktop/ai-career-radar-web
git status
npm run build
npm run screenshot
git diff --stat main..HEAD
git status
```

## Expected output

- `git diff --stat` showing only files under `src/app/`, `src/components/`, and `src/app/globals.css`.
- `npm run build` output ending with `✓ Compiled`.
- 15 screenshots under `temporary/screenshots/{desktop,tablet,mobile}/`.
- A side-by-side description in the RUN_REPORT for any page whose visual
  hierarchy changed.

## Rollback plan

- Branch name: `agent/2026-06-27_run_01`.
- Discard locally: `git checkout main && git branch -D agent/2026-06-27_run_01`.
- Nothing is pushed in this run, so no remote rollback needed.

## Requires human approval

`no`

> Yellow task with no red-zone file touches. Approval is captured in the
> standing P1.7 direction message and via the DECISION step on the
> RUN_REPORT.

## Max runtime minutes

`60`

## Max token budget hint

`unset`

## Stop conditions

- Stop if any red-zone file appears in `git status`.
- Stop if `npm run build` fails.
- Stop if `npm run screenshot` reports >1 failed capture.
- Stop if more than 8 source files modified.
